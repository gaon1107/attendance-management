# Plan: 데모 검증 결과의 근태 적용 (2026-07-11 저장) — 상태: ①번 착수 승인됨(다음 세션 진행)
> 데모(사진판독) 테스트로 확인된 것을 근태 webapp에 반영하는 계획. 사장님 "다음 세션에서 이어서 작업" 지시로 저장.
> **사장님 확정(7/11 밤): 판정 기준값도 "관리자 [설정]에서 조정" 방식으로 구현 — ①번 다음 세션 착수 승인.** (얼굴 크기 기준은 이미 [설정]에 있음)
> 이전 계획(라이브니스 이식 — 완료, 커밋 8471d05~32804ac)은 git 이력에 보존.

## 0. 데모 테스트에서 확인된 사실 (계획의 근거)
- 폰 화면 공격: 진짜 확률 0% → **잘 잡힘**
- **오려서 구부린 인쇄 사진: 78~85%로 "진짜" 오판** — 경량 모델의 알려진 한계 (모델A(넓게 봄)가 잘 속고, 모델B(좁게 봄)는 60~77%로 의심함)
- 얼굴 크기: 폰사진을 가까이 들면 화면의 29~36%까지 나옴 → 크기 검사만으로는 못 막고 판독·사진열람이 보완
- 사장님 결정: **차단 기준은 운영하며 조정** / 최종 안전망 = 관리자 사진 열람(오린 사진 든 손이 그대로 찍힘)

## 1. 작업 항목 (우선순위 순 — 각각 독립적으로 완성 가능)

### ① 판정 기준값을 관리자 [설정]으로 (개발 1순위)
- 지금: webapp은 .env `LIVENESS_THRESHOLD=0.5` 고정 → "운영하며 조정"이 불가능(개발자만 만질 수 있음)
- 할 일: Company에 `livenessThreshold`(기본 50, 30~90%) 추가 → [설정]에 "본인 확인 재검토 기준" 폼(얼굴 크기 기준 폼과 같은 모양) → recordClockPhoto가 회사 설정값으로 suspect 판정
- 주의: 차단이 아니라 **배지 기준**이므로 기존 출퇴근 동작 무영향. faceMinPercent 때와 동일 패턴(스키마 추가만+폼+액션) 재사용
- 참고 파일: webapp/app/settings/FaceRuleForm.tsx(복제 원형), app/actions/face.ts recordClockPhoto·livenessThreshold()

### ② "둘 다 통과(AND)" 판정 비교 스위치 (데모 먼저 → 검증 후 근태)
- 근거: 오린 사진 공격 때 평균(78~85%)은 뚫렸지만 모델B는 60~77% — "두 모델 모두 기준 이상"이면 걸렸음
- 할 일(1단계, 데모): DemoClient에 판정 방식 토글(평균/둘 다) + 이력 재계산 반영 → 사장님이 진짜 얼굴(밝게/어둡게/역광)로 오탐 여부 비교
- 할 일(2단계, 근태 — 데모 합격 시): ClockPhoto 판정에 방식 적용(설정 or 고정은 그때 결정)
- ⚠️ 위험: AND는 위조를 더 잡는 대신 **진짜 얼굴 오탐(어두운 조명에서 배지 남발)**이 늘 수 있음 — 반드시 데모 비교 후 결정

### ③ 재동의 유도 배너
- 문제: 사진보관 동의 문구가 7/11 추가라 **그 전 동의자(사장님 계정 홍길동 7/9 포함)는 사진이 저장 안 됨**(PHOTO_CONSENT_SINCE 가드 — 법적으로 올바른 동작)
- 할 일: 얼굴인증 사용자 중 동의일 < 7/11인 사람에게 [출퇴근]/[인증방식] 화면 배너 "동의 내용이 갱신되었습니다 — 다시 동의해 주세요" → consent 화면으로 유도(재동의 시 faceConsentAt 갱신, 얼굴 등록은 유지됨)
- 참고: 임시 경로는 이미 있음([인증방식]→철회→재동의) — 배너는 안내 편의

### ④ 사장님 실테스트 후 마무리 (개발 아님)
- 얼굴 크기 기준(30 vs 35) 확정 → [설정→얼굴 인식 기준 크기] 입력
- 재동의 → 얼굴 출퇴근 → 관리자 근태상세에서 배지·점수·사진 열람 확인
- 검증용 시드(박성헌 7/10 기록+사진 2건) 확인 후 "지워줘" 지시 → 삭제
- (선택) 판독 모델 훈련 소스(Apache-2.0) 백업 보관

## 2. 🛡️ 사이드 이펙트 방어 (공통)
- 수정 금지 유지: lib/liveness.ts 전처리 상수 / clockIn·clockOut 본체 / 촬영 화질(1280·0.9 — 기준값 측정 조건)
- ①은 배지 계산만 변경(출퇴근·사진 저장 무영향), ③은 화면 배너만(기존 동의 로직 무수정)
- 회귀 테스트: 얼굴 출퇴근 / GPS·일반 출퇴근 / 관리자 근태상세 / 내근태(직원에 판독 미노출)
- 데모 수정 시 **D:\사진판독에 동기화 필수**(2026-07-11부터 규칙)

## 3. 구현하지 않을 것
- 상용 라이브니스 엔진·모델 파인튜닝 — 기술 동향으로만 파악(사장님과 논의 완료), 고객 증가 후 재검토
- 집중도(rPPG) 등 감시성 기능 — 노동 감시 이슈, 제품 방향과 불일치
- 3조각(실근무 확인) — 계속 보류(사장님 지시 시 재개)

## 📌 사용자 메모 공간
-
