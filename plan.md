# Plan: 2차-3번 "실시간 현황판"(회사 중심) — 2026-07-16 — 상태: **✅ 완료(검수 반영)** · 2차 화면 3종 종료

> 근거·영향분석: research.md(2026-07-16). 사장님 결정(7/16): **회사 중심 현황판**(세계지도형은 배포 후로 보류 — GeoIP·비용·검증 불가).
> 2차 화면 3종의 **마지막**. 끝나면 마스터 플로우(얼굴인증 2phase/라이브니스 웹캠 실테스트)로 복귀.

---

## 1. 무엇을 만드나 (쉬운 말)

관리자가 **"지금 회사가 어떻게 돌아가는지"를 지도와 함께 한 화면에서** 보는 현황판입니다.

**① 사무실 지도** — 설정에서 찍으신 **사무실 위치**(진짜 좌표)를 지도에 깃발+반경원으로 보여줍니다.
**② 지금 근무 중** — 현재 일하는 사람을 **사무실 / 재택 / 외근**으로 나눠 보여줍니다(오늘 출퇴근 기록 기반).
**③ 오늘 접속 요약** — 오늘 접속을 **사내망 / 외부**로 세어 보여줍니다(이미 있는 판정 재사용).
**④ 30초마다 자동 새로고침** — "실시간"이라는 이름값. 켜고 끌 수 있고, 수동 새로고침 버튼도 둡니다.

## 2. 🔴 정직하게 지킬 선 (안 지키면 가짜 데이터가 됨)

- **직원을 지도에 점으로 안 찍는다.** 우리는 직원 실제 좌표를 저장하지 않는다(프라이버시 원칙, PROGRESS.md).
  지도에 찍을 수 있는 진짜 좌표는 **사무실 하나뿐.** 직원은 **목록/집계로만** 보여주고, 지도엔 "직원 위치는
  표시하지 않습니다"라고 명시한다. (세계지도형을 접은 이유와 같은 선)
- **"지금 접속 중인 사람"이라고 말하지 않는다.** 우리는 실시간 접속(하트비트)을 추적하지 않는다.
  · "지금 근무 중" = **출퇴근 기록**(출근했고 아직 퇴근 안 함) — 진짜, 대시보드와 같은 계산.
  · "오늘 접속(사내망/외부)" = **오늘 있었던 접속 집계** — "지금 몇 명 접속 중"이 아니라 "오늘 접속 건수".
- **사내망/외부는 officeIps 있을 때만 판정.** 없으면 "판정 불가"(지어내지 않음).
- **IP 신뢰 한계(XFF)** — 6단계와 같은 각주로 화면에 명시(배포 시 nginx 설정 전엔 판정이 부정확).
- 사무실 좌표 미설정 → 지도 대신 "[설정]에서 사무실 위치를 먼저 정해주세요" 안내 + 링크.

## 3. 수정/생성 파일

### 생성
- `webapp/app/live/page.tsx` — 서버: 사무실 좌표 + 오늘 근무자(근무형태별) + 오늘 접속 사내망/외부 집계. 관리자 전용.
- `webapp/app/live/LiveClient.tsx` — 클라이언트: 레이아웃 + **자동 새로고침**(router.refresh 30초, 토글·수동버튼).
- `webapp/app/live/StatusMap.tsx` — **읽기 전용** Leaflet 지도(사무실 깃발+반경원, 드래그·클릭 없음).

### 수정 (add-only)
- `webapp/app/components/Sidebar.tsx` — **공통 컴포넌트**. NavKey에 `"live"` + ICON·LABEL 1줄 + 관리자 "회사관리" 묶음에 추가.
  → **추가만**(기존 항목·순서·동작 무변경). 모든 AppShell 페이지가 쓰므로 렌더 회귀만 확인하면 됨.

### 변경 없음 (🚧)
- `OfficeMap.tsx`(설정 전용 대화형 — 안 건드림, 읽기전용은 StatusMap 신설) · `lib/ip.ts` · `lib/location.ts`(라벨 재사용만) · `lib/access-log.ts` · `actions/*` · 스키마(변경 없음 = 마이그레이션 없음 = EPERM 없음).

## 4. 🛡️ 사이드 이펙트 방어

| 위험 | 대응 |
|---|---|
| **Sidebar가 공통** | add-only(enum·map·리스트 추가). 기존 12개 항목 렌더·하이라이트 회귀 확인 |
| **자동 새로고침이 서버를 때림** | 30초 간격(짧지 않게) + **끌 수 있음** + 화면 벗어나면 정지. 쿼리는 대시보드 수준(가벼움) |
| **지도 SSR 충돌** | Leaflet은 브라우저 전용 → StatusMap은 `"use client"` + 클라이언트에서만 로드(OfficeMap과 동일 방식) |
| **좌표 미설정 크래시** | lat/lng null이면 지도 대신 안내 카드(빈 지도·가짜 좌표 금지) |
| **직원 위치 오해** | 지도에 "직원 개인 위치는 표시하지 않습니다" 명시 |
| **회사 격리** | 모든 쿼리 companyId = 내 회사 강제 |

### 구현 후 반드시 테스트할 기존 기능(회귀)
- [ ] 사이드바 — 기존 관리자/직원 메뉴 전부 그대로 뜨고 하이라이트 정상(대시보드·설정·보안로그 등)
- [ ] 대시보드·설정(OfficeMap) 정상 — 지도 두 개가 안 충돌하는가
- [ ] 직원 계정으로 `/live` 직접 접근 → 차단

### 새 기능 검증
- [ ] 사무실 좌표 있음 → 지도에 깃발+반경원 / 없음 → 안내 카드+링크
- [ ] 지금 근무 중 = 대시보드 "현재 근무 중"과 **같은 숫자**(두 화면 일치)
- [ ] 근무형태별(사무실/재택/외근) 분류 정확
- [ ] 오늘 접속 사내망/외부 집계 — officeIps 있을 때 판정 / 없을 때 "판정 불가"
- [ ] 자동 새로고침 30초 동작 + 토글로 정지 + 수동 버튼
- [ ] 회사 격리(하늘테크↔뉴가온) / 모바일 375px

## 5. 작업분해 TODO
- [x] 3-a: `StatusMap.tsx` 읽기전용 지도 — 커밋 d6ce713
- [x] 3-b: `/live` page + LiveClient(레이아웃·자동새로고침) — 커밋 a694401
- [x] 3-c: Sidebar 메뉴 추가(add-only) — 커밋 a694401
- [x] 3-d: 회귀·신규 검증(실행 증거) — 두 화면 숫자 일치·예외경로·직원차단·모바일·회사격리
- [x] 3-e: code-reviewer 검수 → 수정 → 문서 갱신 — 커밋 832f573 (치명1·중간1·경미2)

## 6. 구현하지 않을 것 (+ 이유)
- **세계지도에 접속 점 찍기** — 사장님 결정. GeoIP·비용·검증 불가·국내 제품엔 값 낮음. 배포 후 재검토.
- **"지금 접속 중" 실시간 presence** — 하트비트/웹소켓 미추적. 오해 소지라 "오늘 접속 집계"로만.
- **직원 개인 위치 지도 표시** — GPS 원본 미저장 원칙.
- **웹소켓 실시간 푸시** — 30초 새로고침으로 충분. 필요 시 후속.

## 📌 사용자 메모 공간 (검토 후 여기에 적어주세요)
-
