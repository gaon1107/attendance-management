# Plan: 5단계 — 차단 IP (+ 4단계 잔여: 동의 개시 기록) — 2026-07-16 — 상태: **✅ 완료(검수 반영)**

> 유효기간 결정(사장님, 7/16): **영구 차단만** = 관리자가 [해제]를 누를 때까지 유지(자동 해제 없음).
> 이유: 시간이 지나 저절로 풀리면 관리자가 "풀린 줄 모르는" 상태가 생겨 오히려 위험. 해제는 버튼 한 번.

> 근거·영향분석: research.md(2026-07-16). 전체 6단계 중 **5단계**. 1~4단계 완료.
> 사장님 결정(7/16): **자동차단 = "보여주기만"**(수상한 IP를 후보로 표시, 차단 여부는 사람이 결정).
> 남은 단계: 6=이상접속 알림.

---

## 1. 무엇을 만드나 (쉬운 말)

**① 동의 개시 기록**(4단계 잔여) — *"박성헌이 7/16에 얼굴 정보 수집에 동의했다"* 를 기록에 남깁니다.
지금은 계정에 시각만 저장돼 **재동의하면 이전 기록이 덮여 사라집니다.**

**② 차단 IP** — 수상한 IP를 관리자가 **명단에 올려 로그인을 막습니다.**
> 예: *"1.2.3.4에서 밤새 비번을 두드림 → 차단"*

**③ 차단 후보 표시** — 여러 번 실패한 IP를 화면에 **"이 IP 수상합니다"로 보여주기만** 합니다.
자동으로 막지 않습니다(사장님 결정) — 사무실은 보통 **직원 전원이 같은 IP**를 쓰기 때문에, 한 명이 비번 틀렸다고 자동으로 막으면 **회사 전체가 못 들어옵니다.**

## 2. 🔴 조사에서 나온 중요한 사실 3가지

**① Next 16은 미들웨어가 `proxy`로 바뀌었습니다** (`node_modules/next/dist/docs/.../16-proxy.md` 확인).
Node 런타임이라 DB 조회가 **가능은** 하지만, 문서가 명시적으로 경고합니다:
> *"Proxy는 인증·권한 용도로 쓰지 말 것, 느린 데이터 조회에 부적합"*
→ **전역 proxy 안 씀.** 모든 요청에 DB 왕복을 더하는 대신 **로그인 진입점에서만** 검사.

**② 로그인 시점엔 아직 어느 회사인지 모릅니다.** 이메일로 사용자를 찾아야 회사를 압니다.
→ **사용자를 찾은 뒤** 차단 검사. 한계: *존재하지 않는 이메일*로 두드리는 공격은 회사를 몰라 차단 대상이 안 됨 → 그건 **기존 5회 실패 잠금**이 담당(이번 범위 밖, 정직하게 기록).

**③ 출퇴근은 차단 안 합니다.** clockIn/clockOut은 🚧 본체 무수정 원칙 + 이미 로그인한 직원을 막으면 이유를 알 수 없어 혼란.
**로그인이 막히면 새 세션을 못 만들므로 실질적으로 충분**합니다.

## 3. 🛡️ 자기잠금 방어 — 사장님이 스스로 못 들어오는 사고 막기

이 기능의 **가장 큰 위험**입니다. 3중으로 막습니다:

1. **회사 허용 IP(사내망)는 절대 차단 안 함** — 규칙에 뭘 넣든 사내망이면 무조건 통과.
2. **지금 내 IP를 막는 규칙은 저장 자체를 거부** — "그 규칙을 넣으면 사장님이 잠깁니다" 하고 막음.
3. **차단은 로그인에만 적용** — 차단 관리 화면은 차단 대상이 아니라, 이미 들어온 관리자는 안 잠김.
- 최후의 수단(문서화): 그래도 잠기면 DB에서 `BlockedIp` 행 삭제.

## 4. 수정/생성 파일 목록

### 생성
- `webapp/lib/ip-block.ts` — `isIpBlocked(ip, company, blocks)` (**화이트리스트 우선**)
- `webapp/app/security/blocked/page.tsx` + 클라이언트 + `actions/ip-block.ts`(추가·해제 서버액션)
- `prisma/schema.prisma`에 **`BlockedIp` 모델** + 마이그레이션

### 수정 (add-only)
- `actions/authmethod.ts` — `agreeBiometric`에 동의 개시 기록 1줄(**redirect 앞**)
- `lib/access-labels.ts` — 라벨 추가(`biometric_consent` 등)
- `actions/auth.ts` — 사용자 조회 **뒤** 차단 검사 + 거부 시 `kind:"blocked"` 기록
- `app/security/SecurityTabs.tsx` — 탭 3개째("차단 IP")

### 변경 없음
- 🚧 `lib/ip.ts`(호출처 5곳 — `ipMatches` 읽기만 재사용) · `lib/access-log.ts` · `actions/attendance.ts` · `Sidebar.tsx`

## 5. 🛡️ 사이드 이펙트 방어

| 위험 | 대응 |
|---|---|
| **관리자 자기잠금** | 위 3중 방어 + 최후 DB 복구 문서화 |
| **로그인 흐름 깨짐** | 차단 검사는 **사용자 조회 뒤·비번 검사 전** add-only. 기존 잠금·실패카운트 로직 무수정 |
| **차단 조회가 로그인을 느리게** | `BlockedIp`는 회사당 소수 + `@@index([companyId])`. 조회 1회 추가 |
| **공격자에게 정보 제공** | 차단 시 메시지는 기존과 동일한 모호한 문구(무엇이 막혔는지 안 알림) |
| **사내망 판정 회귀** | `ipMatches` 무수정 — 출퇴근 위치확인 영향 0 |
| **마이그레이션 EPERM** | 3000 서버 꺼져 있는지 먼저 확인 후 진행 |

### 구현 후 반드시 테스트할 기존 기능
- [ ] 정상 로그인 / 비번 틀림 / 5회 실패 잠금 / 퇴사자 차단 — 전부 그대로인가
- [ ] **사내망 IP를 차단 규칙에 넣어도 로그인이 되는가**(방어 1)
- [ ] **내 현재 IP를 차단하려 하면 거부되는가**(방어 2)
- [ ] 차단된 IP로 로그인 시 거부 + `blocked` 기록
- [ ] 차단 해제 후 로그인 복구
- [ ] 동의 개시 → 기록 남고 **화면 이동 정상**(redirect 함정)
- [ ] 출퇴근·접속로그·감사로그 회귀

## 6. 작업분해 TODO

- [x] 5-a: (4단계 잔여) `agreeBiometric` 동의 개시 기록 + 라벨 — 커밋
- [x] 5-b: `BlockedIp` 모델 + 마이그레이션 + `lib/ip-block.ts`(화이트리스트) — 커밋
- [x] 5-c: 차단 IP 관리 화면(목록·추가·해제) + 탭 + **자기잠금 방어 2** — 커밋
- [x] 5-d: 로그인 진입점 차단 적용 + `blocked` 기록 — 커밋
- [x] 5-e: **차단 후보 표시**(최근 N일 실패 많은 IP 집계 → 화면에 표시, 클릭하면 차단 폼에 채움)
- [x] 5-f: 위 7항목 회귀 테스트(실행 증거)
- [x] 5-g: code-reviewer 검수 → 수정 → 문서 갱신

## 7. 핵심 로직 샘플 (계획용 — 실제 구현 아님)

```ts
// lib/ip-block.ts — 화이트리스트가 항상 이긴다(자기잠금 방지)
export function isIpBlocked(ip: string | null, officeIps: string | null, blocks: BlockedIp[]): boolean {
  if (!ip) return false;                          // IP를 모르면 막지 않는다(오탐 방지)
  if (ipMatches(ip, officeIps)) return false;     // 사내망은 무조건 통과 ← 최우선
  return blocks.some((b) => ipMatches(ip, b.pattern));
}

// actions/auth.ts — 사용자를 찾은 뒤(=회사를 안 뒤) 검사. 기존 로직은 무수정.
  const user = await prisma.user.findUnique({ where: { email } });
  if (user) {
    const blocked = await checkBlocked(user.companyId, ip);
    if (blocked) {
      await recordAccess({ ..., kind: "blocked", result: "blocked", ip, userAgent });
      return { error: "이메일 또는 비밀번호가 올바르지 않습니다." }; // 모호하게(정보 노출 금지)
    }
  }
```

## 8. 구현하지 않을 것 (범위 제외 + 이유)

- **진짜 자동차단** — 사장님 결정: 후보 표시까지만. 오탐 시 사무실 전체 잠김 위험, 운영 데이터로 오탐률 본 뒤 재검토.
- **전역 proxy 차단** — Next 16 문서가 인증 용도 사용을 권고하지 않음 + 전 요청 DB 왕복.
- **출퇴근 차단** — 본체 무수정 원칙 + 로그인 차단으로 충분.
- **미상 이메일 공격 차단** — 회사를 특정할 수 없음. 기존 5회 실패 잠금이 담당.
- **해외/국가(GeoIP) 차단** — 외부 데이터 필요(6단계 이후).

## 📌 사용자 메모 공간 (검토 후 여기에 적어주세요)
- 차단 규칙에 **유효기간**(예: 24시간 임시차단)이 필요하신가요? (없으면 영구차단만)
-
