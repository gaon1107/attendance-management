# Plan: 라이브니스(사진 위조 판독) — 2026-07-10 · 상태: 검토 대기
> **진행 방식 변경(사장님 2026-07-10)**: 근태관리에 바로 적용하지 않고, **동일 개발 사양의 별도 데모 앱을 먼저 만들어 테스트** → 성능 합격 판정 후 → 근태관리 적용(2단계, 별도 승인).

## 0단계 구성 — 데모 앱 (이번 작업 범위)
- 위치: 프로젝트 안 `liveness-demo/` 폴더 — **근태 webapp과 동일 사양**(Next.js + 같은 판독 모듈 코드) · webapp 코드는 전혀 건드리지 않음
- 화면(1페이지): 웹캠 → [판독하기] → **얼굴 위치 박스 + 진짜/사진 점수(모델 2개+합산) 즉시 표시** + 시도 이력(시각·점수·판정 목록)
- **임계값 슬라이더**: 기준값을 조절해가며 진짜 얼굴/휴대폰 사진/모니터 화면/동영상으로 공격 테스트
- 판독 경로도 본 적용과 동일: GaonFR detect(얼굴 위치) → 크롭 → MiniFASNet 2모델 (동일 파이프라인 검증)
- 사진 저장 없음(화면 표시만) — 데모라도 원본 미저장 원칙 유지
- 합격 판정(사장님): 진짜 얼굴은 통과하고, 휴대폰 사진·모니터 화면은 낮은 점수로 구분되는가

## (합격 후) 1단계 — 근태관리 적용: 아래 기존 계획 그대로, 별도 승인 후 진행

> 확정 설계(2026-07-10 사장님): 출퇴근=조용한 표시 / 등록=부드러운 차단 / 의심건 사진 보관(암호화·90일 파기) / 엔진=MiniFASNet

## 1. 접근 방식
- **MiniFASNet(V1SE+V2) ONNX 모델을 웹앱 서버에 내장** (별도 프로그램·서버 없이 지금 웹앱 안에서 판독. 코드 격리로 추후 분리 용이)
- 얼굴 위치는 **GaonFR 응답의 FaceRect 재사용** → 추가 얼굴검출기 불필요 (실측으로 확인된 값)
- 판독 실패(모듈 오류·시간초과)는 **통과 처리**(출퇴근을 막지 않음, 로그만) — 가용성 우선

## 2. 수정/생성 파일
| 구분 | 파일 | 내용 |
|---|---|---|
| 생성 | webapp/models/*.onnx (2개) | MiniFASNet 공개 가중치(Apache-2.0, 출처 명기) |
| 생성 | webapp/lib/liveness.ts | 판독 모듈: FaceRect 크롭(sharp) → 2모델 추론(onnxruntime-node) → 진짜확률 점수 |
| 생성 | webapp/lib/suspect-store.ts | 의심 사진 암호화 저장/복호 열람/90일 자동 파기 |
| 수정 | prisma/schema.prisma | Attendance.livenessStatus·Score + SuspectCapture 모델 (추가만) |
| 수정 | app/actions/face.ts | 등록=판독→의심 시 등록 롤백+재촬영 안내 / 출퇴근=판독→의심 시 표시+사진 저장 (기존 판정 무수정) |
| 수정 | app/face-enroll·attendance 캡처 | 판독 정확도 위해 캡처 해상도 480→720p 상향 |
| 수정 | app/records/page.tsx·[userId] | "본인 확인 재검토 필요" 배지 + 상세(점수·시각·사진 열람, 열람 기록) |
| 생성 | app/api/suspect-photo/[id]/route.ts | 관리자 전용 사진 열람(복호화 스트림 + 열람 로그 + 회사 격리) |

## 3. 🛡️ 사이드 이펙트 방어
- 기존 얼굴 등록/출퇴근 성공·실패 판정 로직 무수정 (판독은 뒤에 붙는 후처리)
- 판독 모듈 장애 시 자동 통과 → 얼굴 기능이 판독 때문에 멈추는 일 없음
- GPS 직원·미사용 화면 영향 없음. 스키마 추가만(마이그레이션+dev 재시작)
- 구현 후 회귀 테스트: ① 얼굴 등록(정상) ② 얼굴 출퇴근(정상) ③ GPS 출퇴근 ④ 근태현황 기존 표시

## 4. 법적 안전장치 (확정안 그대로)
- 표기 문구: **"본인 확인 재검토 필요"** (단정 금지) · 직원 화면에는 아무 표시 없음
- 사진: 의심 건만 · AES-256 암호화(키는 env) · 웹 공개경로 밖 저장 · 관리자만 열람+열람기록 · **90일 자동 파기**
- face-spec §3-2에 예외 명시 + 동의서 문구 항목은 전문가 검토 목록에 이미 등록됨

## 5. TODO — 0단계: 데모 앱 (이번 범위)
- [x] 1) liveness-demo 생성 + onnxruntime-node·sharp 설치 OK + MiniFASNet ONNX 2종 확보(크기 원본 일치) ✅
- [x] 2) 판독 모듈 완성 — 원 구현과 전처리 라인 단위 대조(검수자), 보간=linear 고정(기준값 캘리브레이션 전제) ✅
- [x] 3) 데모 화면(웹캠→판독→박스+점수+이력+슬라이더) ✅
- [x] 4) 검증 — 정답지 실측: 진짜 예시 99.5% / 위조 예시 6.7% (원 저장소 기대 판정과 일치). 에러 경로(검출 0·용량·서버 오류) 확인. tsc 통과. code-reviewer 검수: 치명 0·중간 3(보간·용량 폴백·에러 문구) 즉시 수정 ✅
- [ ] 5) 🧪 사장님 테스트 (localhost:3100): 진짜 얼굴 / 휴대폰 사진 / A4 인쇄 / 모니터 화면 / 동영상 → 합격 판정 + 기준값 결정
- [ ] (합격 후, 별도 승인) 1단계: 근태관리 적용 — 스키마·암호화 사진보관·관리자 배지 등 아래 기존 계획 실행

## 6. 구현하지 않을 것
- 3조각(실근무 확인) — 보류 (사장님 지시)
- 눈 깜빡임(Active) 확인 — 조용한 표시 방식과 상충(직원이 판독을 모르게), 필요 시 등록 화면만 추후
- OpenVINO mn3 앙상블 — PoC 미달 시에만
- 정식 PoC 30회×4종 공격 통계 — 파일럿 단계에서 (이번엔 사장님 약식 테스트로 임계값 1차 조정)

## 📌 사용자 메모 공간 (검토 후 여기에 적어주세요)
-
