# Plan: 6단계 — 이상접속 알림 — 2026-07-16 — 상태: **✅ 완료(검수 반영)** · 2차-2번 종료

> 근거·영향분석: research.md(2026-07-16). 2차-2번 **마지막 단계**(1~5단계 완료).
> 사장님 결정(7/16):
> · **이메일·문자 스위치 = 안 만듦** — 실발송이 안 되는 스위치는 관리자를 속이는 장식. 배포 때 진짜로 만든다.
> · **감지 규칙 = 3가지**(차단 IP 재시도 · 연속 로그인 실패 · 심야 로그인). **"처음 보는 기기"는 제외**
>   → 기기 이력 조회(90일 distinct)가 통째로 사라짐. 도입 첫날 알림 폭탄 문제도 함께 사라짐.

---

## 1. 무엇을 만드나 (쉬운 말)

지금은 관리자가 [보안로그]를 **직접 열어 수천 줄을 눈으로 훑어야** 이상을 발견합니다.
6단계는 **"수상한 것만 골라서 먼저 보여주는"** 화면입니다.

**① 이상 접속 화면** — [보안로그] 4번째 탭. 수상한 것만 모아 보여줍니다.
**② 대시보드 알림 한 줄** — 대시보드 "오늘 알림" 카드에 `🔒 이상 접속 3건`. 클릭하면 위 화면으로.
**③ [확인함] 버튼** — 다 보고 누르면 배지가 꺼집니다. 새 이상이 생기면 다시 켜집니다.
**④ 설정** — [설정] 화면에 심야 시간대·실패 횟수 조정 + 규칙 끄기.

## 2. 감지 규칙 3가지 (확정)

| 규칙 | 판정 | 기본값 | 끄기 |
|---|---|---|---|
| **차단 IP 재시도** | `kind="blocked"` 발생 → **IP별로 묶어** 1줄 | 항상 켬 | ❌ 없음 |
| **연속 로그인 실패** | `kind="login_fail"`을 **IP+날짜별로 묶어** N회 이상 | 5회/하루 | ✅ |
| **심야 로그인** | `kind="login"` 성공이 심야 시간대 → **건별** 1줄 | 22시~06시 | ✅ |

**왜 "차단 IP 재시도"는 못 끄나**: 사장님이 직접 나쁘다고 판단해 막은 IP입니다. **오탐이 0**이라 끌 이유가 없습니다.

**⚠️ 반드시 묶어서 보여준다**: 차단된 IP가 1분에 한 번씩 두드리면 하루 1,440줄입니다. 그대로 뿌리면 화면이 마비됩니다.
→ **"1.2.3.4 · 47회 시도 · 마지막 14:22"** 한 줄로 묶습니다.

**심야 판정은 자정을 넘어간다**(22시~06시). `시작 > 끝`이면 `h >= 22 || h < 6`, 아니면 `h >= 시작 && h < 끝`. **시작 = 끝은 저장 거부**(하루 종일이 심야가 되어버림).

## 3. 🛡️ 사이드 이펙트 방어

| 위험 | 대응 |
|---|---|
| **대시보드가 느려짐**(가장 자주 열리는 화면) | 조회 **1회**(색인 `[companyId, createdAt]` 탐) + 메모리 계산. `take` 상한 2000. 화면·대시보드가 **같은 함수**(`lib/anomaly.ts`)를 씀 — 두 곳이 다른 숫자를 말하면 안 됨 |
| **마이그레이션 EPERM** | 사장님 3000 서버 **끄고** 진행(1단계에서 실제 겪음) |
| **기존 기록·판정 훼손** | 6단계는 **읽기 전용**. `recordAccess`·`logAdminAction`·`ipMatches`·출퇴근 한 줄도 안 건드림 |
| **배지가 안 꺼짐 / 잘못 꺼짐** | `securityCheckedAt` **이후** 이상건만 카운트. [확인함]은 **그 시점 이후 새 이상은 다시 켬** |
| **오탐 폭탄** | 규칙별 끄기 + 임계값 조정. 심야는 교대 회사에서 100% 오탐 → 끌 수 있어야 함 |
| **`securityCheckedAt`이 감사 없이 바뀜** | [확인함]도 관리자 동작 → **`logAdminAction`으로 기록**(누가 언제 확인했나) |
| **설정 변경이 감사에 안 남음** | `saveAlertRules`에 `logAdminAction(me, "config", "alert_rules")` |

### 구현 후 반드시 테스트할 기존 기능(회귀)
- [ ] 대시보드 — KPI 4종·오늘 출퇴근 표·미출근·지각·휴가·공지 **그대로인가** (+ 느려지지 않았나)
- [ ] [설정] 기존 폼 5개(위치·사내IP·얼굴기준·판독기준·근무규칙) 저장 정상
- [ ] [보안로그] 기존 3탭(로그인 이력·접속 로그·차단 IP) 정상
- [ ] 로그인·출퇴근·차단 회귀

### 새 기능 검증
- [ ] 심야 로그인 → 알림 1건 / 규칙 끄면 사라짐 / 시간대 바꾸면 판정 바뀜
- [ ] 같은 IP 실패 5회 → 알림 1건(5줄 아님) / 4회면 안 뜸(경계값)
- [ ] 차단 IP로 여러 번 시도 → **묶여서 1줄 + N회**
- [ ] 자정 넘김(22~06) 판정 — 23시·02시=심야 / 12시=정상
- [ ] 시작=끝 저장 거부
- [ ] [확인함] → 배지 0 → 새 이상 발생 → 배지 다시 켜짐
- [ ] 대시보드 배지 숫자 = 알림 화면 건수 (**두 곳 일치**)
- [ ] 회사 격리(하늘테크 이상이 뉴가온에 안 보임)
- [ ] 직원 계정으로 `/security/alerts` 직접 접근 → 차단
- [ ] 모바일 375px

## 4. 수정/생성 파일

### 생성
- `webapp/lib/anomaly.ts` — 판정 격리(4단계 `audit.ts`와 같은 방식). `detectAnomalies(companyId, from, to, rules)` → 화면·대시보드 공용
- `webapp/app/security/alerts/page.tsx` + `AlertsClient.tsx` — 이상 접속 화면
- `webapp/app/actions/security-alerts.ts` — `markSecurityChecked()`([확인함])

### 수정 (전부 add-only)
- `prisma/schema.prisma` `Company` — 컬럼 **6개 추가**(전부 기본값 → 기존 데이터 무손실)
  `securityCheckedAt DateTime?` · `alertNightOn Boolean @default(true)` · `alertNightStart Int @default(22)` · `alertNightEnd Int @default(6)` · `alertFailOn Boolean @default(true)` · `alertFailCount Int @default(5)`
- `app/security/SecurityTabs.tsx` — 탭 1줄 + 타입에 `"alerts"`
- `app/dashboard/page.tsx` — 알림 카드에 줄 1개(**있을 때만 표시** — 기존 비번재설정 줄과 같은 패턴)
- `app/settings/*` + `actions/settings.ts` — 폼 1개 + `saveAlertRules` 1함수(기존 5함수 무수정)
- `lib/access-labels.ts` — `alert_rules`·`security_checked` 라벨

### 변경 없음 (🚧)
`lib/ip.ts` · `lib/access-log.ts` · `lib/audit.ts` · `lib/device.ts` · `lib/ip-block.ts` · `actions/attendance.ts` · `actions/auth.ts` · `Sidebar.tsx`

## 5. 작업분해 TODO

- [x] 6-a: 스키마 6컬럼 + 마이그레이션(**서버 끄고**) — 커밋 063cdfc
      ※ 계획엔 "컬럼 추가만"이라 썼으나 Prisma가 **RedefineTables**(복사→DROP→RENAME)를 생성 — SQLite 표준 동작.
        실행 후 데이터 보존을 직접 검증(회사 2·직원 5·근태 25·접속 31 + 설정값 전부 무사).
- [x] 6-b: `lib/anomaly.ts` 판정 함수 + 경계값 검증 — 커밋 e10602f
- [x] 6-c: `/security/alerts` 화면 + 탭 + [확인함] — 커밋 c13ee3b
- [x] 6-d: [설정] 규칙 폼 + `saveAlertRules` + 감사로그 — 커밋 cb0a042
      🐛 구현 중 자체 발견: **규칙을 끄면 저장 불가**(disabled input은 폼 전송에서 빠짐 → 서버가 "값 없음" 거부).
         하필 이 기능이 가장 필요한 상황(24시간 교대 회사)에서 터진다. opacity+pointerEvents로 교체.
- [x] 6-e: 대시보드 배지 1줄 — 커밋 5f87991
- [x] 6-f: 위 회귀·신규 검증(실행 증거)
- [x] 6-g: code-reviewer 검수 → 수정 → 문서 갱신 — 커밋 73bb128 (치명3·중간5·경미5 반영)

## 6. 구현하지 않을 것 (+ 이유)

- **이메일·문자 발송/스위치** — 사장님 결정. 외부 서비스·도메인 필요 → 배포 때 진짜로
- **처음 보는 기기** — 사장님 결정. `deviceLabel`이 "Windows PC" 수준으로 거칠어 신뢰도 낮고, 도입 첫달 오탐 과다
- **해외/GeoIP** — 외부 데이터 필요 + 🚨 XFF 신뢰 문제 미해결 상태에선 무의미
- **자동 차단 연동** — 5단계 결정("보여주기만") 유지
- **실시간 푸시** — 2차-3번 영역

## 7. ⚠️ 정직한 한계 (화면에 명시할 것)

- **판정은 지금 규칙으로 매번 다시 계산**한다 → 설정을 바꾸면 과거 판정 표시도 바뀐다(3단계와 동일 구조).
- **IP를 못 믿으면 이 알림도 못 믿는다** → 🚨 배포 필수 조건(nginx XFF 덮어쓰기)에 그대로 걸려 있다.
- **미상 이메일 공격은 회사를 특정할 수 없어** 회사별 알림에 안 잡힌다(5단계와 동일 한계 — 기존 5회 잠금이 담당).
