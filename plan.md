# Plan: 3조각 — 실근무 확인(자리 유무 자동 체크) — 2026-07-10 · 상태: 검토 대기

## 1. 접근 방식
- 기획 확정안(옵션 B) 그대로: **detect(얼굴 개수)만 사용, 이미지 미저장, 사전 고지, 주기 샘플링.**
- 사장님 신규 결정 반영: **회사 켜기/끄기(관리자)** + **직원 참여 선택(거부 시 출퇴근만, 불이익 없음)**.
- 기존 실근무시간 계산은 **건드리지 않고**, "자리 확인" 결과를 **별도 지표**로 관리자에게 보여준다(산식 반영은 파일럿 후 결정 — 기획 §10).

## 2. 동작 규칙 (사용 흐름)
1. 관리자: [설정]에서 **"실근무 확인 사용"** 켬 + 확인 주기(기본 10분) 설정
2. 직원(얼굴인증 선택+등록 완료자만): [인증방식]에 **"실근무 확인 참여"** 카드가 나타남 → 참여/거부 선택, 언제든 철회 가능
3. 참여 직원이 출근하면 출퇴근 화면 상단에 고지 배너: *"근무 중 10분마다 자리확인용 카메라가 짧게 작동합니다"*
4. 주기마다 웹캠 1프레임 → 서버 → 얼굴서버 detect → **있음/없음만 기록** (탭이 닫혀 있으면 "확인 안 됨"=결측, 자리비움으로 치지 않음)
5. 관리자: [근태현황 상세]에서 직원별 **자리확인 타임라인**(있음/없음/미확인)과 **확인된 재실시간** 보조 지표 확인

## 3. 수정/생성 파일
| 구분 | 파일 | 내용 |
|---|---|---|
| 수정 | prisma/schema.prisma | Company.presenceCheckEnabled·presenceIntervalMin / User.presenceOptInAt / **PresenceSample 모델**(attendanceId, at, present) |
| 수정 | lib/face.ts | `detectFace(image)` 추가 — Faces 개수만 반환 (추가만) |
| 생성 | app/actions/presence.ts | 참여/철회 액션 + recordPresence(근무 중인 본인만, 참여+회사켜짐 검증, 10초 타임아웃) |
| 수정 | app/settings/page.tsx (+새 폼) | "실근무 확인" 켜기/끄기 + 주기 카드 |
| 수정 | app/auth-method/page.tsx | 참여 선택 카드 (회사 켜짐 + 얼굴등록 완료자에게만 노출) |
| 생성 | app/attendance/PresenceChecker.tsx | 근무 중 주기 캡처 클라이언트(고지 배너 포함, 참여자만 렌더) |
| 수정 | app/attendance/page.tsx | 참여자에게 PresenceChecker 장착 |
| 수정 | app/records/[userId]/page.tsx | 자리확인 타임라인 + 확인 재실시간 지표 |

## 4. 🛡️ 사이드 이펙트 방어
- `workedMinutes`·리포트·정정·대시보드: **실근무 계산 무변경** → 영향 없음 (구현 후 회귀 확인 목록에 포함)
- lib/face.ts·스키마: 추가만. **마이그레이션 후 dev 서버 재시작 필수** (Prisma 구클라이언트 함정)
- 미참여/GPS 직원·기능 꺼진 회사: 화면·동작 완전 동일 (분기 렌더)
- 구현 후 테스트: ① GPS 직원 출퇴근 그대로 ② 얼굴 등록/출퇴근 그대로 ③ 리포트 숫자 불변 ④ 참여 직원만 배너·캡처 동작

## 5. 법적 가드레일 반영
- 참여는 **명시적 선택**(기본=미참여), 거부·철회 시 어떤 기능도 제한 없음 → 문구에 명시
- detect만 사용(신원 식별 X), 이미지 미저장, 고지 배너 상시 표시
- 동의 문구는 초안으로 넣고 **전문가 검토 목록에 이미 포함**(생체정보 동의서 갱신 시 함께)

## 6. 구현하지 않을 것 (범위 제외)
- 실근무시간 **산식에 자리확인을 반영**하는 것 — 파일럿 실측 후 결정(기획 §10). 지금은 보조 지표 표시까지만
- 라이브니스(사진판독) — 별도 확정안대로 파일럿 전 착수
- 모바일 백그라운드 확인(앱 필요) — 2차. 이번엔 PC 브라우저 탭 열림 전제
- 자리비움 자동 외출 처리·경고 알림 — 추후(감시 프레임 위험, 신중히)

## 7. TODO
- [ ] 1) 스키마 3종 추가 + 마이그레이션 + dev 재시작
- [ ] 2) lib/face.ts detectFace + actions/presence.ts
- [ ] 3) 설정 카드(관리자) + 인증방식 참여 카드(직원)
- [ ] 4) PresenceChecker(주기 캡처+배너) + 출퇴근 화면 장착
- [ ] 5) 근태 상세에 자리확인 타임라인·지표
- [ ] 6) 검증(브라우저 E2E: 켜기→참여→출근→샘플 기록→관리자 확인) + 회귀 4종 + tsc
- [ ] 7) code-reviewer 검수 → 수정 → 문서 갱신 → git 커밋

## 📌 사용자 메모 공간 (검토 후 여기에 적어주세요)
-
