# Plan: 회사 일정관리 캘린더 + 대시보드 중복 정리 (2026-07-17) — 상태: 검토 대기

> ⚠️ 이건 마스터 플로우(얼굴인증 웹캠 실테스트)에서 벗어난 **곁가지**(사장님 지시). 규모 있어 2단계로 나눔. 끝나면 마스터 복귀.

## 0. 결정 사항(사장님)
- MES 일정관리 화면처럼 **날짜를 클릭해 직접 등록하고 막대로 보여주는 풀페이지 월 캘린더** 신설.
- 범위 = **회사 일정관리까지 포함(확장)**: 휴무일뿐 아니라 회사 일정/메모를 날짜에 등록.
- 대시보드 = **중복 표만 교체**: 요약 KPI는 유지, [근태현황]과 겹치던 "오늘 직원 목록 표"만 빼고 그 자리에 "이번 달 일정 캘린더(요약)".

## 1. 접근 방식 (+이유)
- **새 인터랙티브 캘린더 컴포넌트**를 만든다(기존 MonthCalendar는 읽기전용 근태표시라 재사용 부적합 — 그리드 레이아웃만 참고).
- **휴무일은 기존 CompanyHoliday 재사용**(이미 지각·결근·연차에 연결됨). 캘린더는 그걸 **추가/해제하는 UI**가 됨 → 로직 중복 없음, 일관성 유지.
- **일반 일정은 새 표 CompanyEvent**(근태 판정에 영향 없는 표시 전용). 둘을 한 캘린더에서 함께 관리.
- 법정공휴일(Holiday)은 **읽기전용 표시**(빨강).

## 2. 데이터 모델 (신규 1표)
```
model CompanyEvent {           // 회사 일정/메모(표시 전용 — 근태 판정 무관)
  id String @id @default(cuid())
  companyId String
  company   Company @relation(...)
  date      String   // "YYYY-MM-DD" (MVP=하루짜리)
  title     String   // 예: "월례회의", "출하: 진양오일씰"
  color     String?  // 막대 색(선택, 기본 파랑)
  createdBy String?
  createdAt DateTime @default(now())
  @@index([companyId, date])
}
```
- ⚠️ 마이그레이션 = **사장님 서버 정지 필요**(EPERM).
- CompanyHoliday·Holiday는 그대로 사용(신규 없음).

## 3. 1차 — 회사 일정 캘린더 페이지 신설
**파일**
- 신규 `app/schedule/page.tsx`(관리자 전용, 월 데이터 조회) + `app/schedule/ScheduleCalendar.tsx`(클라이언트, 클릭·모달)
- 신규 `app/actions/schedule.ts`(addEvent/deleteEvent — admin 확인+감사로그). 휴무일 토글은 **기존 actions/holidays.ts의 add/deleteCompanyHoliday 재사용**
- 신규 스키마 `CompanyEvent` + 마이그레이션
- 수정 `app/components/Sidebar.tsx`(NavKey "schedule" 추가 — 관리자 "회사관리" 그룹에 add-only)

**화면 구성(MES 스타일)**
- 상단: 월 이동(◀ ▶ 오늘) + 제목 "일정 캘린더"
- 큰 월 그리드: 각 날짜 셀에 ①법정공휴일(빨강 배지, 읽기전용) ②회사 휴무일(보라 배지) ③일정 이벤트(막대들) ④오늘 하이라이트
- **날짜 클릭 → 패널/모달**: 그 날 항목 목록 + [일정 추가](제목 입력) + [이 날 회사 휴무일 지정/해제] 토글 + 각 일정 삭제
- 하단 범례

## 4. 2차 — 대시보드 중복 정리
**파일**: 수정 `app/dashboard/page.tsx`
- **제거**: "오늘 출근한 직원 목록 표" 섹션(라인 ~180-240, 근태현황과 중복)
- **유지**: 요약 KPI(지각·미출근·근무중·알림 배지) + 미출근/지각 **명단**(이름 — 실시간 핵심)
- **추가**: 그 자리에 "이번 달 일정 캘린더(요약·읽기전용)" — 공휴일·휴무일·일정·오늘 표시. 클릭 시 /schedule로 이동
- 상세 오늘 출퇴근은 [근태현황](이미 오늘이 기본)에서 봄

## 5. 🛡️ 사이드 이펙트 방어
| 위험 | 대응 |
|---|---|
| Sidebar 공통 수정 → 전 화면 영향 | NavKey 1개 add-only(라벨·아이콘·그룹에 추가만). 기존 항목 무수정 |
| 대시보드 표 제거로 정보 손실 | KPI·미출근/지각 명단은 유지. 상세는 근태현황에 이미 있음(중복 제거일 뿐) |
| 휴무일 토글이 지각·결근 로직 건드림 | 기존 add/deleteCompanyHoliday 재사용 → 판정 로직 무수정, 이미 검증된 경로 |
| 마이그레이션 EPERM | 사장님 서버 정지 게이트 |
| 이벤트 title XSS/길이 | 서버에서 trim+길이상한(예: 100), 텍스트로만 렌더(dangerouslySetInnerHTML 금지) |
| 회사 격리 | CompanyEvent 모든 조회·삭제 companyId 격리, 액션 admin 확인 |
| 직원 열람 여부 | 1차는 **관리자 전용**(권한 단순). 직원 열람은 범위 제외(원하면 2차 후 별도) |

**구현 후 테스트할 기존 기능**: 대시보드 KPI·미출근/지각 수 동일, 근태현황·설정·공휴일 무영향, 사이드바 다른 메뉴 정상.

## 6. 작업분해 TODO
- [x] 1단계: 스키마 CompanyEvent + 마이그레이션(20260717044316) 완료
- [x] 2단계: actions/schedule.ts(addEvent/deleteEvent) — admin+격리 완료(일정은 감사로그 제외)
- [x] 3단계: /schedule + ScheduleCalendar(클릭·모달·막대) 완료
- [x] 4단계: Sidebar "일정" 메뉴 add-only 완료
- [x] 7단계(1차분): code-reviewer 치명1(휴무토글 revalidate 누락) 수정·재검증 완료
- [x] 1차 실화면 검증: 렌더·일정 추가/삭제·휴무 지정/해제 즉시반영·콘솔0·tsc/eslint0
- [ ] ⏳ **5단계(2차): 대시보드 중복 표 제거 + 이번 달 캘린더 요약 위젯** — 사장님 확인 후 진행
- [ ] 2차 회귀 테스트 + 검수

## ✅ 1차 완료 (2026-07-17)
회사 일정 캘린더(/schedule) 신설 완료. 커밋 fb8ce15(신설)·7154ec5(치명수정).
다음: 사장님이 1차 확인 → 2차(대시보드 정리) 진행 여부 결정.

## 7. 구현하지 않을 것 (범위 제외)
- 다중일 막대(기간 스패닝), 반복 일정, 리마인더/알림
- 드래그로 이동, 직원 개인 일정
- 직원(비관리자) 열람 화면(1차는 관리자 전용)
- 공지(Announcement)와의 통합(별개 유지)

## 📌 사용자 메모 공간 (검토 후 여기에 적어주세요)
-
</content>
