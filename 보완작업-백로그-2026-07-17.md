# 보완작업 백로그 (2026-07-17 검증본)

> 목적: "메뉴별 보완 목록"을 **실제 코드와 전수 대조**해 검증하고, 디자인 목업과도 대조한 결과.
> 다음 세션은 이 문서에서 항목을 골라 **리서치→계획→승인→구현** 절차로 진행하면 된다.
> ⚠️ **옵션 B(웹캠 자리확인) = PC 활동 데스크톱 앱**은 개발 재개 여부 **미결정** → 이 백로그에서 제외(별도 결정 사안).
> 검증 방법: code-reviewer/general-purpose 에이전트 3종 + 직접 grep. 각 항목에 근거 파일 경로 표기.

---

## 0. 현재 위치 (마스터 플로우)
- 곁가지(공휴일·일정캘린더·공지통합·공지표시날짜·근태상세 기본'일') **전부 완료**.
- 얼굴인증 2phase 웹캠 테스트: **사장님 확인 완료**(출근 정상 99%·위조 잘 잡힘).
- 지금은 이 백로그에서 **다음 작업 선택 대기** 상태.

---

## 1. 목록 정정 (검증 중 바로잡은 것)
1. **옵션 B(웹캠 자리확인)** = "간판 기능 미구현"이 아니라 **2026-07-15 폐기 → PC 활동 데스크톱 앱으로 전환**(리서치 완료·개발 보류). `presence_sample` 없는 건 폐기된 설계라 맞음. 근거: [실근무-PC활동-데스크톱앱-리서치](docs/07_ai/실근무-PC활동-데스크톱앱-리서치-2026-07-15.md).
2. **얼굴 원본 파기** = "미구현(2phase 대기)"이 아니라 **unenroll 함수는 이미 있음**([webapp/lib/face.ts](webapp/lib/face.ts):227), 직원 [얼굴삭제]([webapp/app/actions/face.ts](webapp/app/actions/face.ts):346)에선 이미 호출. → 파기 3곳에 **연결(배선)만** 하면 됨.
3. **파기 이력 로그** = "미흡"이 아니라 **이미 있음**(AccessEvent kind=purge, [webapp/lib/audit.ts](webapp/lib/audit.ts):35 logAdminAction, 조회화면 [webapp/app/security/access](webapp/app/security/access) + CSV). 단, result="성공"은 로컬 사진 파기만 반영(GaonFR 원본삭제 성공여부는 미반영 → 1-A와 연결).

---

## 2. 디자인 목업(34개) vs 실제 개발
- 폴더: `근태 관리 디자인 스타일/리뉴얼_화면/*.dc.html`
- **✅ 완료 24개**: 대시보드·근태현황·직원별상세·리포트·휴가승인·연차정산·생체정보관리·동의파기·설정홈·근무규칙·근무제기준시간·로그인이력·차단IP·이상접속알림설정·실시간지도·공지·월간캘린더·직원관리·**부서관리**·직원프로필·직원대시보드·근태현황조회 등
- **🟡 부분 4개**: 법정근로기록조회(엑셀만·PDF/전용화면 없음) · 초과근무관리(계산·표시만, 전용화면 없음) · 휴게시간관리(외출기록은 됨, 회사별 설정화면 없음) · 관리자 알림센터(대시보드+보안알림으로 흩어짐, 통합센터 없음)
- **❌ 미개발 6개**: 결재선 설정 · 야간 특근 신청 · 근무 스케줄표 · 급여 연동 · **교육 이수 관리(목록에 없던 새 항목)** · **조직도(부서관리는 됐으나 조직도 시각화 없음, 새 항목)**

---

## 3. 추가개발 백로그 (옵션 B 제외) — 우선순위 + 근거

### 🔴 A. 운영(파일럿) 전 필수
- ~~**A-1. 생체정보 파기 시 GaonFR 원본삭제 배선**~~ **✅ 코드완료(2026-07-17, 사장님 실검증만 대기)**
  - 파기 3곳(chooseGps·withdrawBiometric·adminRevokeBiometric)에 `unenrollFaceSafely`(신규 헬퍼) 연결 → GaonFR 원본 삭제. 감사로그 result = `로컬사진파기 && GaonFR삭제` 둘 다 성공해야 "성공".
  - code-reviewer 치명0. 반영: ①call-site 10초 타임아웃(face.ts 무수정) ②"GaonFR 먼저 삭제→성공 시에만 등록표시 해제"(실패 시 재시도 서명 보존) ③관리자 파기가 "동의 없는데 등록만 남은 고아 상태"도 정리 ④타이머 clearTimeout.
  - ⚠️ **사장님 실검증 남음**: 얼굴서버(gaonfrdev) 켜고 등록→철회→GaonFR 실제 삭제·감사로그 result 확인. (이 환경엔 실카메라·서버 미가동)
  - 🟡 **후속(A-1-b, 별도)**: GaonFR 삭제가 **실패**했을 때 실제로 다시 지울 **운영 장치**가 없음. 현재는 DB에 재시도 서명(`authMethod=gps && faceEnrolledAt≠null`)만 남고, 파기 3버튼이 전부 봉인돼 자동 재시도가 안 걸림. → **정리(reconciliation) 배치**(그 서명 가진 유저를 주기적으로 재삭제) 또는 **감사 `result=fail`(purge)를 관리자 화면에 눈에 띄게 노출**해 수동 재처리. 배포를 막는 결함은 아니나 생체정보 파기 완결 위해 권고.
- **A-2. 이메일/문자 발송 연동** *(중~큼, 발송계정 필요)*
  - 지금 발송 인프라 0(패키지 없음). 초대·임시비번을 화면표시+수동전달.
  - 근거: 초대 [webapp/app/actions/invites.ts](webapp/app/actions/invites.ts) `createInvite`:16 + [InviteLink.tsx](webapp/app/employees/InviteLink.tsx) / 비번재설정 [webapp/app/actions/password-reset.ts](webapp/app/actions/password-reset.ts) `issueTempPassword`:43(주석에 "2차" 명시).
  - 결정 필요: 이메일(SMTP/SES 등) vs 문자(문자·알림톡) 어느 채널부터.
- **A-3. (코드 아님)** 동의 문안 전문가(노무사·개인정보) 검토 → [webapp/app/consent/page.tsx](webapp/app/consent/page.tsx):2,48 "초안" 표기 제거. / 얼굴 임계값 현장 실측 확정(화면은 완비: [LivenessRuleForm.tsx](webapp/app/settings/LivenessRuleForm.tsx)·[FaceRuleForm.tsx](webapp/app/settings/FaceRuleForm.tsx), 모델A 기준은 코드상수 face.ts:140).

### 🟡 B. 실사용 개선 (중간)
- **B-1. 조퇴 판정** — 지각([webapp/lib/worktime.ts](webapp/lib/worktime.ts):33 `isLate`)과 대칭으로 `workEndTime`(schema.prisma:36) 前 퇴근 판정 신설. 현재 완전 부재.
- **B-2. 연차 자동발생** — 현재 100% 수동([webapp/app/actions/leave.ts](webapp/app/actions/leave.ts):98 `setAnnualLeave`, `annualLeaveDays` default 15). 입사일(`hireDate` 미사용) 기반 자동부여 도입.
- **B-3. 초과근무 알림** — 계산은 됨([webapp/app/reports/page.tsx](webapp/app/reports/page.tsx):86 `overtimeMinutesOf`), 기준초과 시 관리자 알림 트리거 없음(보안 alert `anomaly.ts`는 보안전용이라 재사용 불가).
- **B-4. 법정기록 PDF** — 현재 엑셀만([webapp/app/reports/export/route.ts](webapp/app/reports/export/route.ts) ExcelJS). PDF 라이브러리 없음.
- **B-5. 휴가 유형 확장** — 3종 하드코딩([webapp/lib/leave.ts](webapp/lib/leave.ts):6 `LEAVE_TYPES` annual/half/sick). 경조·공가·반반차·시간단위·회사별 커스텀 없음.
- **B-6. 외출 사유 커스텀** — 4종 하드코딩([webapp/app/attendance/page.tsx](webapp/app/attendance/page.tsx):19 `REASONS`). 회사별 편집 불가.

### 🟢 C. 빠른 개선 (작음)
- ~~**C-1. 사번 중복검사**~~ **✅ 완료(2026-07-18, 커밋 473003e)** — 공용 헬퍼 `employeeNoTaken`(lib/employee-profile.ts)으로 관리자수정·초대가입 두 경로에서 회사내 중복 차단(앱단, 스키마 무변경). null 미검사·활성직원만(퇴사자 재사용 허용)·본인제외·사번변경시에만. tsc/eslint 0, 실DB 9종 PASS, code-reviewer 치명0. ※ 경미(추후): 대소문자/전각 정규화 없음(사번은 숫자·하이픈 위주라 영향 적음), DB 유니크 제약 없어 동시제출 완벽차단은 아님.
- **C-2. 알림 센터 통합** — 흩어진 알림(대시보드 오늘알림 + 보안알림)을 한 화면으로. (디자인 "관리자 알림 센터" 있음)

### ⚪ D. 큰 덩어리 / 2차 (지금 안 해도 됨)
- 결재선(다단계 승인) · 야간특근 사전신청 · **교육 이수 관리** · **조직도 시각화** · 근무 스케줄표(교대) · 급여 연동 · 연차촉진(사용독려)
- (별도 결정) **PC 활동 데스크톱 앱** = 실근무 정직측정 간판기능. 리서치 문서 1~3단계 계획 있음.

---

## 4. 추천 다음 스텝
파일럿(실사용) 준비가 목표라면: **A-1(파기 배선) → C-1(사번)·B-1(조퇴) 빠른 개선 → A-2(발송 연동)** 순서.
PC 활동 앱(D)은 투자 규모가 커서 "재개 여부"를 먼저 별도 결정.

> 다음 세션 진입 시: 사장님이 이 문서에서 항목을 고르면 → 그 항목만 리서치→계획→승인→구현.
