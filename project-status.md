# 프로젝트 현황판 (최종: 2026-07-11)

> 세부 이력은 PROGRESS.md가 원본. 이 파일은 "지금 하는 작업"의 현황판.

## 현재 상태: 라이브니스 데모 검증 중 (사장님 테스트 진행)
- 마지막 작업: 라이브니스 데모(liveness-demo, 포트 3100) 완성 + 더블클릭 배치파일(CP949 한글 수정) + **D:\사진판독에 독립 패키지로 내보냄**(다른 프로젝트 재사용용, 사용방법.md 포함)
- 사장님 1차 테스트: **휴대폰 사진 판별 잘 됨** 확인. ⏳ 남은 것: 기준값 결정 테스트 — 본인 얼굴(밝게/어둡게/역광) 최저 점수 vs 위조(폰사진 등) 최고 점수 확인
- 다음 할 일: ① 사장님 기준값 테스트 결과 수령 → ② 합격 시 **근태 webapp 이식(1단계, 별도 승인)** — plan.md의 "1단계" 계획 그대로 (통합 방식 확정: 판독 모듈·모델을 webapp에 내장, 근태 DB에 의심 표시·점수·의심건 사진 저장)

## 확정된 설계 (2026-07-10 사장님)
- 출퇴근 = **조용한 표시**(직원 모름, 관리자에 "본인 확인 재검토 필요" + 점수·시각·의심건 사진) / 등록 = 부드러운 차단
- 의심건 사진: 암호화·관리자만 열람(기록)·90일 자동 파기 — "얼굴원본 미저장" 원칙의 유일한 예외, 동의서 명시 필요
- 아키텍처 = 웹앱 내장(B안, 별도 서버·DB 없음), 엔진 = MiniFASNet V1SE+V2 (Apache-2.0)

## 대기/보류 중
- **2조각(얼굴 출퇴근) 사장님 웹캠 실테스트** 아직 안 됨 — 얼굴 등록부터 다시 필요(DB상 등록 0)
- **3조각(실근무 확인)**: 계획 완료 상태로 보류(커밋 fa940b1) — 사장님 지시로 라이브니스 우선
- 결제(아이원/KICC): 정소장 확인 대기

## ⚠️ 알려진 이슈
- 라이브니스 기준값 미확정(데모 기본 50%) — 사장님 실측으로 확정 예정
- 개발 얼굴서버가 조잡한 합성 이미지도 얼굴로 등록한 사례 → 운영 전환 시 검출 기준 확인 항목
- 테스트 직원(박성헌) 근무요일 토·일(테스트 흔적)

## 🚧 건드리면 안 되는 부분
- liveness-demo/lib/liveness.ts의 전처리 상수(배율 4.0/2.7·80×80·BGR·보간 linear): 원 모델 재현 — 변경 금지(점수 왜곡)
- webapp actions/attendance.ts clockIn/clockOut, lib/face.ts 기존 함수: 검증 완료 — 수정 금지
- .env(FACE_*): 비밀값 — webapp·liveness-demo·D:\사진판독 세 곳에 복사돼 있음, git 제외 유지

## 영향 범위 지도
- liveness-demo ↔ webapp: **현재 완전 분리**(공유는 가온 계정뿐). 이식 시 lib/liveness.ts+models/ 가 webapp으로 복사됨
- D:\사진판독 = liveness-demo의 복사본(독립) — 근태 저장소 밖, 여기 수정해도 근태와 무관
