# 프로젝트 현황판 (최종: 2026-07-21)

> 세부 이력은 PROGRESS.md가 원본. 이 파일은 "지금 하는 작업"의 현황판.

---

## ▶▶ 다음 세션 시작점 (여기부터 읽기) — 2026-07-21

### ✅ 완료(신규·백로그): 결재이력 '직원 드롭다운 → 통합검색' 전환 — 2026-07-21 (커밋 08c0a03·7ec496e, 미푸시)
> 사장님 피드백: 직원 드롭다운은 수백 명 회사에서 스크롤 비현실적 → 공통 통합검색(SearchBox+lib/search)으로 교체. dev-workflow 전 절차 수행.
- **신규 `ApprovalHistoryClient.tsx`**: 필터바(유형·상태 제어형 select + 기간 RangeCalendar + **SearchBox**) + 결과헤더 + 표를 **한 클라이언트 컴포넌트로 통합**(정정승인 패턴). 검색은 로드된 행을 신청자·사번·부서·유형·내용·사유·상태·처리자로 **즉시 OR 필터**(URL 변경 없음).
- **`page.tsx`**: userId 서버필터·전 직원 emps 조회 **제거**(부하↓), 각 행에 검색문자열 부여. **`ApprovalHistoryFilters.tsx` 삭제**(신규로 흡수).
- **공통(SearchBox·lib/search·RangeCalendar·listApprovalHistory) 무수정. DB 없음. URL 하위호환**(userId= 있어도 무시). 직전 개선(유형·상태 제어형·조회기간 헤더) 보존.
- **검증**: tsc·eslint 0 / **3001 실서버 PASS**(처리자·유형 검색 즉시필터·결과0안내·지우기·유형 서버조회·뒤로가기 제어형복귀·조회기간헤더·콘솔0). **code-reviewer 치명0·중간0**(중간1=검색이 회사전체 300건 상한 안에서만 동작→대량회사서 특정직원 오래된기록 '조용한 누락' → **상한도달 시 빨간 경고 항상 노출**로 해소·재검토 통과, 경미2=날짜검색제외(의도)·이중소문자(무해) 보고만).
- ⚠️ **알려진 한계(사장님 보고필요·후속 옵션)**: 통합검색은 현재 로드된 행(유형·상태·기간+최대 300건) 안에서만 필터. **월 신청 300건 넘는 대량 회사**에선 특정 직원 오래된 기록이 로드에서 잘릴 수 있어(경고로 안내) 기간을 좁혀 조회해야 함. 실불편 시 **"검색어를 서버 조회로 전환"**이 근본해결(이번 승인 범위 밖, 별도 착수).
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: [결재이력] → 통합검색창에 이름/사번/부서/내용/처리자 입력 시 즉시 필터되는지.

### ✅ 완료(신규·백로그): 결재이력 날짜필터 수정 2건 — 2026-07-21 (커밋 a803f0a·4b7393b·92eb210·7e42e58, 미푸시)
> 사장님 지시로 백로그 착수. 관리자 [결재이력](감사·내부통제 조회) 화면의 code-reviewer 지적 2건(미반영) 해소. dev-workflow 전 절차(research→plan→승인→구현→검수) 수행. **사장님 피드백 반영해 '전체 기간' 버튼은 최종 제거**(7e42e58).
- **①비제어→제어형 select**: 유형·상태·직원 드롭다운을 `defaultValue`(비제어)→`value`(제어형)로 전환. 브라우저 **뒤로/앞으로** 이동 시 표시가 실제 결과와 어긋나던 감사화면 신뢰성 문제 해소. **(최종 유지)**
- **②기본기간 '이번 달' 조용한 숨김 해소**: 결과 헤더에 **"조회 기간(신청일 기준): 시작~종료" 항상 표기**. **(최종 유지)** 과거를 보려면 달력에서 기간을 넓게 잡는다.
- **↩️ 되돌린 것(사장님 피드백)**: 처음엔 "전체 기간" 버튼(all=1→from/to null 전체조회)+all모드 달력 흐림도 넣었으나, ⓐ달력으로 넓게 잡으면 되어 버튼 불필요 ⓑ흐림 처리가 달력 팝업까지 반투명하게 만드는 부작용 → **버튼·all=1·흐림 span 전부 제거**. 헤더 표시(②핵심)와 제어형(①)만 남김.
- **수정 파일 2개만**(page.tsx·ApprovalHistoryFilters.tsx). **RangeCalendar(공통)·listApprovalHistory(공통) 무수정. DB/마이그레이션 없음. 기존 URL 하위호환.**
- **검증**: tsc·eslint 0 / **사장님 3000 실서버 검증 PASS**(버튼제거·달력 팝업 opacity1=반투명흠 해소·헤더 조회기간 유지·뒤로가기 시 드롭다운 '전체' 복귀·콘솔0). **code-reviewer 치명0**(초기 중간1=흐림 잔존모순 → 최종 흐림제거로 원천 해소, 경미3=보고만 동의). ⚠️스크린샷 도구가 이 세션 내내 타임아웃(앱 아닌 도구 문제, JS·읽기 검증은 정상).
- 🅿️ **연계 백로그 소진**: 아래 🅿️의 "[결재이력] 날짜필터 후속 2건" **모두 처리 완료**(①제어형 ②조회기간 노출). 남은 백로그=관리자 결재 메뉴 통합.


## ▶▶ 이전 세션 시작점 — 2026-07-20 (후반)

### ✅ 완료(신규·모델 전환): 결재선 "혼용 B" → **"직급 단일"**로 교체 — 2026-07-20 (커밋 e89af28, 미푸시)
> 사장님 지시: 부서장이 상무·전무·대표까지 결재선을 타야 하는데 조직도(부서장/상위부서) 방식이 헷갈림 → **조직도 없이 "직급"만으로 직원·부서장 모두 직접 결재선을 고르게** 통일.
- **후보 규칙(공용)**: "나보다 직급 높은 상급자" = ① 같은 부서 팀 상급자(대리→과장→부장) + ② **내 부서 최고직급보다 높은 전사 임원**(부장 위 상무→전무→대표). `candidateScope`/`candidateWhere` 헬퍼.
  - 김준수(사원): 영업팀 대리·과장·부장 + 상무·대표 / **회계 이주은 과장은 제외**(내부통제 유지). 박성헌(부장): 팀 상급자 없음 → **상무·대표 직접 선택**.
- `createApprovalStepsIfNeeded` custom: **조직도 자동상향·전결병합 제거** → 직원이 고른 순서 그대로. 미설정=관리자 폴백. MAX 5→8(대리~대표), 편집기 상한도 8로 일치.
- **custom 모드에선 조직도(부서장·상위부서) 설정 UI 숨김**(deptline 모드만 사용). 2단계 "혼용" 로직(부서장 위 조직도 자동)은 이걸로 대체됨.
- **검증**: tsc·eslint 0 / 임시라우트 **13/13 PASS**(후보범위·회계과장 제외·부장의 임원선택·상한8·대표 폴백·**single/deptline 회귀0**)·잔존0. code-reviewer 치명0(중간=서버8·UI5 불일치 수정, 경미=에러문구 갱신).
- ⚠️ **실화면 검증 남음(사장님 세션)**: 뉴가온 결재방식 custom → 직급표(사원~대표) → 직원마다 직급 배정 → 직원/부서장이 "내 결재선"에서 상급자(임원 포함) 골라 저장 → 신청 → 그 순서로 결재 도달.


### ✅ 완료(신규): 직급 서열 기반 "혼용 결재선" — 2026-07-20 (커밋 8413d63·f648132, 미푸시)
> 사장님 지시로 custom 결재선을 재설계. **부서 안(대리→과장→부장)은 직원이 직급 상급자 중 고르고, 부서장 위(→대표)는 조직도로 자동 연결(혼용 B).** 설계서=[docs/04_architecture/직급서열_결재선_설계.md]. (기능①"같은 부서원"은 이 방식으로 대체됨.)
- **1단계(8413d63)**: 신규 `JobGrade` 표(회사별 직급 사다리·level) + `User.jobGradeId`(nullable,add-only)+마이그레이션. 관리자 [직원관리→직급 관리](추가·이름·↑↓서열·삭제). 직원 상세 '직급 서열' 배정. **검수 치명(uncontrolled select로 재저장 시 직급 조용히 삭제)→controlled 수정·재검증.**
- **2단계(f648132)**: `listApprovalCandidates`/`filterActiveApprovers`=같은 부서+상급 직급(level gt)만. `createApprovalStepsIfNeeded` custom=[직원 지정]+[조직도 자동(buildApprovalChain,최대3)] 병합·중복제거. 부서장 신청 상위로, 최상위/미설정 관리자 폴백. 조직도(부서장·상위부서) UI를 custom에도 노출. **검수 중간(전결권자를 지정에 고르면 isFinal 유실→전결종결 깨짐)→전결 보존+그 지점 종결 수정.**
- **검증**: tsc·eslint 0 / 임시라우트 **혼용 11/11 + 전결 4/4 PASS**(혼용체인·부서장상향·최상위폴백·동급하급타부서거부·전결보존종결·**single/deptline 회귀0**)·잔존0 / 1단계 실화면(직급추가·서열이동·직원배정·재저장유지). code-reviewer 2회 반영.
- ⚠️ **미설정 처리=안내**(사장님 결정): 편집기가 "부서 내 결재자 안 고르면 조직도대로 자동" 안내. 강제 차단 아님(혼용이라 조직도가 바탕).
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: [직원관리]에서 ①결재방식 "상신자 지정" ②직급 관리에 사다리(사원→대리→과장→부장) ③직원마다 직급 배정 ④부서·부서장·상위부서(조직도) 지정 → 직원으로 신청화면 "내 결재선"에서 상급자 고르기 → 신청 → 지정+조직도 순서로 결재 도달 확인.
- ⚠️ **알려진 한계(보고만)**: 조직도 자동부 최대 3단계(팀→부→본부→대표 4계층+는 대표 누락 가능, deptline과 동일) / custom [결재함] 메뉴는 대기건 있을 때 노출.


### 🅿️ 보류·백로그 (사장님 지시로 다음에)
- **관리자 결재 메뉴 통합 검토(보류·2026-07-20)**: 유형별 승인 5개(휴가·외출·재택·야근·출장) + 결재함 + [결재이력]이 많아 보임. 통합안(①"결재 처리" 1개 + "결재 이력" 1개로 7→2 ②완전 통합 1개 ③현행 유지+그룹핑) 논의했으나 **"다음에 다시 검토"로 보류**. ※이력=조회전용(승인 불가), 유형별 승인=처리(승인/반려)로 역할은 다름. 재개 시 유형별 승인화면의 고유기능(첨부·결재선 진행표시·반려사유·검색·기간달력) 보존이 관건(회귀 위험).
- ~~**[결재이력] 날짜필터 후속(code-reviewer 중간 2건, 미반영)**~~ → **✅ 2026-07-21 처리 완료**(커밋 a803f0a·4b7393b): ①제어형(value) 전환 ②결과 헤더 "조회 기간" 노출 + "전체 기간" 버튼. 상세는 위 "다음 세션 시작점" 참조.


### ✅ 완료(신규): custom 결재선 "같은 부서원 제한" + 관리자 "결재 이력 조회 화면" — 2026-07-20 (커밋 3634fe9·d28ec08, 미푸시)
> 사장님 지시: ①custom 모드 결재자 후보를 **같은 부서원**으로 제한(동료/타부서/상호승인 방지) ②직원 신청·승인 이력을 **관리자 페이지에서 조회**. (직급 서열 방식은 "나중에 필요하면".)
- **①같은 부서원 제한**: `listApprovalCandidates`(같은 부서 재직자만)·`filterActiveApprovers`(저장·신청 공용에 같은-부서 강제 → **폼 위조도 차단**). `saveApprovalLine` 무효선택 시 조용히 삭제 않고 에러(+deleteMany companyId 병기). 편집기 "같은 부서만" 안내·후보0명 안내. **부서 미배정/1인 부서=관리자 단독 폴백**(안전).
  - 검증: tsc·eslint 0 / 임시라우트 **8/8 PASS**(같은부서·순서보존·자기/타부서/퇴사/중복 제거·최대5·미배정 폴백·테넌트격리)·잔존0. **code-reviewer 치명0**(m-1 companyId 병기 반영). ⚠️운영DB에 기존 custom 라인이 타부서 결재자 포함 시 신청 때 자동 제외→관리자 폴백(신규라 실데이터 거의 없음).
- **②관리자 결재 이력 화면**: 새 `lib/approval-history.ts`(6종 신청 회사격리·필터 조회→신청일 내림차순 정규화·처리자 이름 해석·상한300) + 새 `app/approval-history/page.tsx`(관리자 전용, 유형·상태·직원·기간 필터 + 표: 신청일시·유형·신청자·내용·상태·처리자·처리일시·결재의견) + 사이드바 관리자 **[결재이력]** 메뉴. **읽기 전용·엔진 무접촉**. custom 상호승인 등 사후 발견용.
  - 검증: tsc·eslint 0 / 임시라우트 **12/12 PASS**(정렬·타사격리·4종 필터·처리자이름·결재의견)·잔존0 / 미인증 307차단. **code-reviewer 치명0**(중간=퇴사자도 필터 대상 포함, 경미=날짜 롤오버 차단·문구 완화 반영). 보고만(범위 외): leave/correction `@@index` 부재는 별도 성능 과제.
- ⚠️ **실화면 육안검증 남음(사장님 세션·로그인 필요)**:
  1. [직원관리] → 결재방식 **"상신자 지정 결재선"** 저장 → 직원 계정 [외출/외근] 상단 "내 결재선 설정"에서 **같은 부서 직원만** 후보로 뜨는지.
  2. 관리자 **[결재이력]** 메뉴 → 표·필터(유형/상태/직원/기간) 동작 육안확인.


### ✅ 완료(신규): 결재선 "상신자 종류별 저장"(custom 모드) — 2026-07-20 (커밋 7c87162·f01a9ee, 미푸시)
> 사장님 지시: 부서장 자동 방식이 제한적 → **직원이 신청 종류별로 자기 결재선을 한 번 저장하면 이후 자동 결재**(그룹웨어 표준). 회사 결재방식에 3번째 옵션 custom 추가(single/deptline/custom).
- **새 표 `ApprovalLineTemplate`**(직원·종류당 1개, add-only)+마이그레이션. `createApprovalStepsIfNeeded` custom 분기(저장 라인 로드·재직검증·퇴사 자동 건너뜀·미설정/전원퇴사=관리자 폴백). 결재 엔진·신청 6액션 **무수정 재사용**(회귀0).
- 관리자 [직원관리]에 "상신자 지정" 라디오 / 직원 6개 신청 페이지에 **"내 결재선 설정"**(순서·최대5·↑↓·저장) `ApprovalLineEditor`.
- **검증**: tsc·eslint 0 / 임시라우트 **9/9 + 8/8 PASS**(저장→단계·순서·필터(중복/본인/퇴사/타사)·퇴사건너뜀·폴백·후보목록·single/deptline 회귀 / 결재함 도달·진행표시·전결없음·체인완주), 잔존0.
- **code-reviewer 치명1·중간3 반영**: [치명] 표시/내비 경로(사이드바 결재함·배지·진행표시 12곳)가 deptline 전용이라 custom 결재자가 결재함 진입 못 하던 것 → multiStep로 확장. [중간] saveApprovalLine custom 가드. [경미] custom 전결 배지 제거(isFinal=false).
- ⚠️ **미해결(보고): 내부통제 리스크** — custom은 상신자가 결재자를 자유 지정하므로 A↔B 상호 지정 시 상호승인 가능. 관리자가 저장된 결재선을 검토·잠글 수단 없음. 필요 시 "관리자 검토/승인" 절차는 향후 과제(사장님 판단).
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: 관리자로 custom 켜기 → 직원으로 [외출] "내 결재선 설정" 저장 → 신청 → 지정 결재자 로그인 → [결재함] 도달·승인.

### 🎯 ✅ 완료(사장님 지시): 전자결재 확장 — "근태 결재 세트" (5/5 전부 완료, 각 구현+검수+커밋)
> 사장님이 전자결재 필수기능 리스트를 주심. **기존 결재선 엔진(ApprovalStep·advanceApproval·결재함·진행표시·반려사유·전결) 재활용**해 새 신청유형 5종을 붙임. 순서: ①외출/외근 ②재택 ③초과근무 사전신청 ④출장 ⑤첨부파일.
> **진행률: 5/5 완료.** 미푸시 커밋 다수(0af42fe~84d548d). **⚠️ 실화면 육안검증(사장님 세션)만 남음** — 각 유형 신청→대시보드 대기카드→승인 화면 승인→직원 화면 반영, 첨부 업로드/다운로드 육안확인.

#### ✅ ⑤신청서 첨부파일 — 완료(2026-07-20, 커밋 0c5c942·84d548d, 미푸시)
> 4종(외출/재택/초과/출장) 신청에 파일 첨부(최대 5개·개당 10MB·PDF/JPG/PNG). 다운로드=신청자 본인/관리자/결재자, 회사격리. 새 표 `RequestAttachment`(다형).
- 기존 lib/company-doc 저장패턴 재사용(웹 공개경로 밖·매직바이트 검증·난수 파일명·경로조작 차단), 저장폴더는 `storage/request-attachments/`(gitignore). 텍스트는 서버액션 유지, 파일은 route로 업로드(4MB 한도 회피). request* 반환에 id 추가, cancel* 첨부정리.
- **검증**: tsc·eslint 0 / **임시라우트 17/17 PASS**(저장 라운드트립·경로조작 차단·업로드/열람 권한 6종·타사격리·승인후 차단·정리) + 미인증 401.
- **code-reviewer 치명0·중간2 반영**: ①업로드 Content-Length 가드(DoS) ②개수 상한 트랜잭션 원자화. 보안강화=강제 다운로드(attachment). 경미(삭제순서 고아파일)=보고만.
- ⚠️ **실업로드/다운로드 육안검증 남음(사장님 세션, 로그인 필요)**.

#### (이전 4종은 아래에 각각 기록) 각 유형은 leave/corrections 패턴 + createApprovalStepsIfNeeded 훅 복제 + 공통 3곳(listMyApprovals·countMyPendingApprovals·approvals/page.tsx TYPE_META) add-only 확장. **알림센터·대시보드 대기카드도 유형마다 확장(single 대칭).**

#### ✅ ④출장 신청 — 완료(2026-07-20, 커밋 4297c2f·5fddaa0, 미푸시)
> 직원 [출장] 메뉴에서 기간(시작~종료)·**출장지(필수)**·목적 신청 → 승인. 승인=허가 기록만. 새 표 `BusinessTripRequest`. lib/trip.ts.
- ②재택 클론 + 출장지. tripRangeLabel(연도인식) + MAX_TRIP_DAYS(366) 선반영. RequestType += trip.
- **검증**: tsc·eslint 0 / **임시라우트 14/14 PASS**(기간라벨·deptline 라운드트립·single 폴백·반려·**5종 결재함 공존 회귀0**·취소정리, 잔존0).
- **code-reviewer 치명·중간 0**(격리·검증·회귀·cascade·동시성·FK 전부 통과). 경미(출장지 required/maxLength) 반영. 경미(과거날짜·기간중첩)는 결재세트 공통정책=보고만.
- ⚠️ **실화면 육안검증 남음(사장님 세션)**.
- 🔜 다음: **⑤첨부파일**(⚠️ 저장정책 결정 필요) — 기존 lib/company-doc.ts 안전업로드 패턴 재사용.

#### ✅ ③초과근무(야근) 사전신청 — 완료(2026-07-20, 커밋 d190c4f, 미푸시)
> 직원 [초과근무] 메뉴에서 야근 예정일·시간대(자정넘김 허용)·사유 신청 → 승인. 승인=허가 기록만, **기존 주52 계산(lib/overtime.ts)과 완전 분리**. 새 표 `OvertimeRequest`. 새 파일은 `lib/overtime-request.ts`(이름충돌 회피).
- RequestType += overtime. 자정넘김 표시(`22:00~익일 02:00`). 알림·대시보드 대기카드 반영.
- **검증**: tsc·eslint 0 / **임시라우트 14/14 PASS**(자정넘김 라벨·deptline 라운드트립·single 폴백·반려·**4종 결재함 공존 회귀0**·취소정리, 잔존0).
- **code-reviewer 치명·중간 0**(주52 무접촉·자정넘김 일관성·격리·cascade·동시성 전부 통과). 경미(역순시각 무경고 등)는 형제와 동일 기존한계=보고만.
- ⚠️ **실화면 육안검증 남음(사장님 세션)**.
- 🔜 다음: **④출장 신청**(기록만, 결정 불필요) → **⑤첨부파일**.

#### ✅ ②재택근무 신청 — 완료(2026-07-20, 커밋 00d86fd·a4e6e23, 미푸시)
> 직원 [재택] 메뉴에서 **기간(시작~종료)**·사유 신청 → 관리자/부서장 결재선 승인. 승인=허가 기록만. 새 표 `RemoteWorkRequest`(add-only).
- ①외출/외근과 동형 클론. RequestType += remote. 알림센터·대시보드 대기카드 처음부터 반영(single 대칭).
- **검증**: tsc·eslint 0 / **임시라우트 12/12 PASS**(deptline 라운드트립·single 폴백·반려·**휴가+외출/외근+재택 3종 결재함 공존 회귀0**·취소정리, 잔존0).
- **code-reviewer 치명0·중간2 반영**: ①기간 라벨이 연도 생략해 다년/크로스이어가 '하루'로 보이던 오판 → `remoteRangeLabel`(연도 다르면 표시) 단일출처화 ②신청 기간 무제한 → 366일 상한. 경미(cancel companyId·parseYmd중복) 반영.
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: 직원 [재택] 기간 신청 → 관리자 대시보드 "🏠 재택근무 신청" 카드/[재택승인] 승인 → 직원 "승인" 표시.
- 🔜 다음: **③초과근무 사전신청** — ⚠️ **결정 필요**(승인분을 초과근무로 '인정' vs '기록만').

#### ✅ ①외출/외근 신청 — 완료(2026-07-20, 커밋 0af42fe·0fc25a9·cf78a35·1828683, 미푸시)
> 직원이 [외출외근] 메뉴에서 종류(외출/외근)·날짜·시각·행선지·사유 신청 → 관리자/부서장 결재선 승인. **승인=허가 기록만**(근태·실근무 판정 무접촉=회귀0). 새 표 `OutingRequest`(add-only).
- 결재 엔진 제네릭 재사용(advanceApproval·전결·진행표시 무수정). RequestType += outing. 결재함·사이드바(관리자 [외출승인]·직원 [외출외근]) 등록.
- **검증**: tsc·eslint 0 / **임시라우트 실코드 15/15 PASS**(deptline 2단계 라운드트립·single 폴백·반려·**휴가와 결재함 공존 회귀0**·취소 정리, 검증데이터 잔존0).
- **code-reviewer 치명0·중간1 반영**: single 모드 외출/외근 pending이 관리자 알림센터·대시보드 대기카드에 안 뜨던 비대칭 해소(notifications.ts·dashboard 추가). 경미(kind 라벨 단일출처화) 반영.
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: 직원 계정으로 [외출외근] 신청 → 관리자 대시보드 "🚶 외출/외근 신청" 카드/[외출승인] 화면에서 승인 → 직원 화면 "승인" 표시까지 눈으로 확인 권장.
- 🔜 다음: **②재택근무 신청**(외출/외근과 동형 → 기록만).
- **이미 있음(✅)**: 연차·반차(오전/오후)·조퇴·병가·경조·공가·특별휴가·근태정정 신청 / 결재선(1·2·최종·대결·전결) / 승인·반려(사유) / 결재이력(최종결정 표시).
- **1순위(추가 필요·저비용, 결재선 재활용)**: ①외출/외근 신청(장소·시간, 현재는 출근시 '외근' 선택·외출시간 집계만) ②재택근무 신청(현재는 출근시 '재택' 선택만) ③초과근무 사전신청(현재는 감지·주52알림만) ④출장 신청(❌ 신규) ⑤신청서 첨부파일.
- **2순위**: 이메일/푸시 알림(현재 앱내 알림·종배지·문자초대만).
- **보류(전략결정)**: 커스텀 양식 빌더(회사별 양식 자유생성=아키텍처 대변화)·지출결의/구매요청/품의서/보고서(근태 밖 그룹웨어 영역).
- **다음 시작**: 사장님이 ①근태결재 세트 착수(외출/재택/초과/출장 중 택1) / ②정식 기획먼저 / ③양식빌더까지 크게 중 택1 → dev-workflow(리서치→계획→승인→구현→검수)로 진행. **새 신청유형은 leave/corrections 패턴 + createApprovalStepsIfNeeded 훅을 그대로 따르면 됨.**

### ✅ 완료(신규): 전체 시스템 E2E 운영 QA (2026-07-20) — 콘솔에러 0, 핵심기능 정상
> 실회사 **가온테크(운영검증)** 만들어 관리자·부서장·직원 6계정으로 처음~끝 클릭 테스트. **테스트 데이터 유지(삭제 안 함, 사장님 지시)**.
- **통과**: 로그인(전 역할)·대시보드·직원관리·리포트(초과근무 계산 수동검산 일치)·연차정산(입사일 기준 자동발생 근속별 정확)·설정·근무표·보안로그·회사정보·생체정보·알림센터·회사정보.
- **다단계 결재 E2E 완주**: 박사원 휴가신청→김팀장(1단계)승인→이본부장(2단계)승인→최종확정→박사원 화면 '승인'+연차 15→13 차감. 근태정정도 결재선 생성 확인.
- **방금 만든 기능 실검증**: 결재함 게이팅(박사원 없음/부서장 보임+배지)·전결 안내문·관리자 결재함 숨김. 콘솔에러 0.
- ⚠️ 자동화 브라우저에서 버튼 합성클릭이 안 먹혀 DOM클릭(requestSubmit/element.click)으로 진행 = 실사용자 클릭은 정상(앱 버그 아님).
- 🔑 **남긴 테스트 계정**(비번 전부 `test1234!`): 가온테크=qa-admin/qa-tim(1단계)/qa-bonbu(2단계)/qa-emp1(박사원)/qa-emp2/qa-emp3. + 모의-반려테스트(mock-admin·mock-emp) + 모의-교대테스트(mock-shift-admin, 현재 2교대).

### ✅ 완료(신규): [결재함] 메뉴를 결재 라인 구성원에게만 표시 (2026-07-20) — 커밋 c0f65e9(미푸시)
> 결재선 켠 회사에서 [결재함]이 전 직원에게 뜨던 것을 **부서장·(부서장없는 부서의)대결자·현재 대기있는 사람**에게만 노출. 일반 직원·미지정 관리자는 숨김.
- `lib/approval-server.ts` `isApprovalLineMember`(cache·회사격리, head OR (head null && deputy)) + `Sidebar.tsx` showApprovals 게이트(admin·employee 공통). 표시로직만, /approvals·결재동작 무변경.
- code-reviewer 반영: 대결자는 부서장 없는 부서일 때만(빈 결재함 방지, buildApprovalChain의 head??deputy와 일치). tsc·eslint 0, 임시라우트 5/5 PASS. **⚠️ origin/main 미푸시 1커밋(c0f65e9)**.

### ✅ 완료(신규): 결재선 2차 — 전결(전결권자 지정) (2026-07-20)
> 결재선 2차 나머지 3후보(전결·조건별·자동에스컬레이션) 중 **①전결**만 이번 조각(사장님 확정: B.전결권자 지정 방식 + 휴가·근태정정 둘 다). 조건별·에스컬레이션은 별도.
> 관리자가 부서에 **"전결권자"**를 지정하면, 결재선이 그 부서장 승인에서 **종결**(상위로 안 올라감). 실제 전결규정과 동일.
> **문서**: [research.md]·[plan.md]는 이후 작업이 덮어쓰므로 이 커밋 이력에 보존.

**설계 핵심 = 체인 조기 종결(truncation)**: `buildApprovalChain`이 전결권자 결재자를 추가하면 그 지점에서 상위 탐색을 멈춘다 → 그 사람이 마지막 단계가 되어 기존 완료판정(isChainComplete)이 자연 처리. **`advanceApproval`(승인/반려/멱등복구) 무수정** = 가장 안전.

**변경 8파일**: schema add-only 2필드(`Department.finalApproval`·`ApprovalStep.isFinal`, 둘 다 기본 false=기존 무영향)+마이그레이션 `20260719155517_approval_final` / `lib/approval.ts`(DeptNode·조기종결·반환 `approvers:{userId,isFinal}[]`) / `lib/approval-server.ts`(resolveApproverChain·createApprovalStepsIfNeeded isFinal 저장·getApprovalProgressMap nextIsFinal) / `departments.ts`(전결권자 저장) / `employees/page.tsx`·`DepartmentManager.tsx`(전결권자 체크박스+표시) / `leave/page.tsx`·`corrections/page.tsx`(신청자 진행표시 "(전결)" 배지).

**검수(code-reviewer 2회)**: 1차 치명0·중간1(**겸임 시 전결 무효화** 수정) → 재검토 해소·배포이의없음. 추가 경미(비인접 겸임 배지 오해)도 "전결 마커를 남은 마지막 단계에 표시"로 수정. **경미 잔여(보고만)**: ①부서장 없이 전결만 켜면 무효(안전방향) ②대결자 결재 시도 전결발동(라벨 "부서장") ③결재함(승인자 화면)엔 전결 표시 없음(신청자 화면엔 있음).

**검증**: tsc·eslint 0 / **실제 컴파일한 buildApprovalChain + 실prisma 롤백검증(무커밋)**: 최초 15/15 → 겸임수정 8/8 → 최종 9/9 PASS(회귀 미지정·single·전결1단계·전결권자=본인 / 겸임 인접·비인접·겸임+본인·겸임 non-final). 임시데이터 잔존 0.
- ✅ **실화면 E2E 검증 완료(Claude, 인앱 크롬)**: 격리 테스트회사(deptline)에 세션주입(무비번)으로 3단계 실행 — ①[직원관리]에서 팀A 전결권자 체크→저장="전결권자 예" ②신청자 휴가신청→진행표시 **"결재 0/1 · 다음: 이팀장 (전결)"**(단계 1개=상위 미포함) ③이팀장 결재함 승인→결재함 0건, 신청자 화면 **"승인"**. DB확증: 휴가 approved, 결재단계 1개(이팀장·approved·isFinal=true), 최본부 단계 미생성. 검증 후 테스트데이터 전삭(잔존 0).
- ⚠️ **운영 주의**: dev/운영 서버는 이 마이그레이션 적용 후 **Prisma 클라이언트 재생성+서버 재시작** 필요(안 하면 구 클라이언트가 finalApproval 몰라 "Unknown argument" 오류). 검증 중 실서버 재시작으로 해소함.
- 🔜 **후속 후보**: 결재선 2차 나머지(조건별 결재선·자동에스컬레이션) · 위 경미 3건 · 결재함 전결 표시.

### ✅ 완료(신규): 교대 축소 시 fixed 측(ShiftPattern·ShiftAssignment) dangling 정리 (2026-07-20)
> 커밋 `a9c2e8a`가 교대 수 축소 시 **순환 측**(ShiftGroup·배정·orderCsv)만 정리해 **fixed 측이 비대칭**으로 남아 있던 선재결함 보완. 교대 3→2 축소로 Shift를 삭제하면 `ShiftPattern.shiftId`(고정 요일패턴)·`ShiftAssignment.shiftId`(날짜 예외)가 schema상 Shift로의 FK가 없어 삭제된 id를 가리키는 dangling 참조로 남던 문제를 정리. 크래시는 원래 없었고(resolveShift가 없는 id를 휴무 처리) 정합성만 개선.
> **문서**: [research.md]·[plan.md]는 이후 작업이 덮어쓰므로 이 커밋 이력에 보존.

**변경 1곳**: `webapp/app/actions/settings.ts` `saveWorkRules` — 기존 트랜잭션 안 `if(shiftMode)` 블록에서 `shift.deleteMany` 한 줄을 "삭제 대상 id 조회 → `shiftPattern`·`shiftAssignment`의 그 shiftId를 null(휴무)로 정리 → 삭제"로 교체. **순환 ShiftGroup 정리와 동일 철학(참조 해제→삭제), companyId 스코프 유지, 스키마·마이그레이션 변경 없음.**

**설계 판단**: 완전 OFF(shiftMode=null)는 블록 미진입 — Shift 행을 의도적으로 남기는(휴면) 현행 설계라 dangling 자체가 안 생김 → 정리 불필요(요청의 "OFF 시" 가설 검토 결과).

**검증**: tsc·eslint **0** / **실DB 롤백검증(무커밋) 11/11 PASS**(고정3→2축소·동일수2→2·OFF·테넌트격리 4시나리오) → 임시데이터 잔존 **0** / code-reviewer **치명·중간 0**(경미 3건 모두 동작 불변·수정 불필요). 실화면 E2E는 로그인+교대데이터 필요라 임시검증으로 대체 — 다음에 브라우저 열면 "3교대 배정→2로 축소→근무표/기록 화면" 육안확인 권장.

### ✅ 완료(신규): 결재선 2차 — 반려사유 + 결재이력 피드백 (2026-07-20)
> 결재자가 반려/승인 시 **사유를 남기고**, 신청자가 결과 화면에서 **"누가·왜·언제"**를 본다. 지금까진 신청자가 "부서장 반려 처리 중"만 보고 이유를 몰랐음. **single·deptline 모두 적용, 기존 승인/반려 흐름 회귀 0.**
> **문서**: [research.md]·[plan.md]는 이후 다른 작업이 덮어써서 git 이력(커밋 aa로 시작 블록)에 보존.

**확정 결정(사장님)**: 결재선 2차 4후보(전결·조건별·자동에스컬레이션·거부피드백) 중 **①반려사유+결재이력 피드백**만 이번 조각. 나머지는 별도. 반려사유=**필수**, 승인사유=선택(이번엔 UI 미노출·서버만 준비), 단계별 타임라인=최종 결정만 표시(간단).

**완료 단계(커밋 7개)**
- **1/7** `9f0e…`(schema): LeaveRequest·AttendanceCorrection에 `decisionComment`·`decidedById` nullable 추가(마이그레이션 `20260719145554`). ApprovalStep.comment는 기존 존재. add-only=회귀0.
- **2·3/7**(서버): `advanceApproval(comment?)` 선택 파라미터 — 처리 단계에 사유 저장. approve/reject 액션 4개가 comment 수신·원본 decisionComment/decidedById 미러(멱등 updateMany 원자화).
- **4/7**(UI): 공용 `app/components/RejectButton.tsx`(반려 클릭→사유 필수 모달·500자) 3곳 적용(결재함·관리자 휴가승인·근태정정승인). **승인은 기존 1클릭 유지**.
- **5/7**(신청자): `leave/page.tsx`·`corrections/page.tsx` 내 신청내역에 "반려 사유/승인 메모: … · 처리자 · 날짜" 표시(처리자 이름 배치조회). 미사용 getApprovalHistory 제거.
- **검수반영**(fix): code-reviewer **치명0**. 중간1(반려사유 서버강제=빈사유 조기return, JS-off·폼위조 방어)·경미2(오버라이드 비원자 2차쓰기 제거·500자 코드포인트 상한) 반영.
- **검증**: tsc·eslint 0 / 임시라우트 **실함수 11종 + 재검증 3종 PASS**(회귀 single·권한격리·빈사유·오버라이드·미러 round-trip) → 검증데이터 전부 삭제(잔존0) / **RejectButton 모달 실화면 확인**(in-app 브라우저: 사유 입력→확정 활성화·글자수·오버레이 정상).
- ⚠️ **실화면 육안검증 남음(사장님 세션)**: 실제 로그인해 반려→신청자 화면에서 사유 표시까지의 E2E는 로그인 필요라 임시검증으로 대체. 다음에 브라우저 열면 관리자로 휴가 반려(사유 입력)→직원 계정 [휴가]에서 "반려 사유: …" 표시 육안확인 권장.
- 🔜 **후속 후보(선택)**: 승인 사유 입력 UI 추가(서버 준비됨) · deptline 단계별 이력 타임라인(getApprovalHistory 재도입) · 결재선 2차 나머지(전결/조건별/에스컬레이션).

### ▶▶ (이전) 다음 세션 시작점 — 2026-07-19

### ✅ 완료(신규): 결재선(부서장 다단계 승인) — Phase 1~4 완성 (2026-07-19)
> 회사마다 다른 결재 체계를 "부서장 자동 결재선"으로. 직원 신청(휴가·근태정정)이 **소속 부서장 → 상위 부서장** 순으로 타고 올라가 승인. **결재선 안 켠 회사(single)는 기존 단일 관리자 승인 100% 그대로(회귀 0).**
> **문서**: 조사·설계 [docs/04_architecture/결재선_설계.md](docs/04_architecture/결재선_설계.md) (경쟁사 조사=국내 그룹웨어 "자동 결재선"이 표준, 사장님 "부서장 방식" 확정)

**확정 결정(사장님)**: 결재자 지정=**부서장 방식**(부서에 부서장 지정 → 조직도 타고 상위로). 범위=부서장 자동 결재선(1~2단계, 최대3)+대결(대리결재)+진행상태 표시. 전결·조건별·에스컬레이션은 2차.

**완료 단계(커밋)**
- **Phase 1**(토대) `c7690be`: schema add-only(Department에 headUserId·parentId·deputyUserId, Company에 approvalMode(기본 single)·approvalStepCount, 신규 ApprovalStep 모델)+마이그레이션. lib/approval.ts 순수함수(buildApprovalChain·본인건너뜀·순환/누락 방어). 경계 18종 PASS.
- **Phase 2**(설정 UI) `6644010`+`321785b`: [직원관리]에 결재방식 토글+부서별 부서장·상위부서·대결자 지정. 순환참조 방어. 실화면 검증(뉴가온)→원상복구. (저장직후 stale 수정=현재저장 라벨+select key).
- **Phase 3**(핵심 동작) `d721e95`+`f5eb446`: lib/approval-server.ts(결재선 생성·advanceApproval 단계진행·회사격리·권한). 신청 시 결재선 생성, 승인/반려 위임(**원본 status는 체인 완료 시에만 approved=소비처 9곳 회귀0**). /approvals 결재함(부서장이 자기차례 승인). code-reviewer 치명0, 중간2 수정(정정 트랜잭션 원자화·부분실패 멱등복구). 실코드 16종 PASS.
- **Phase 4**(진행표시·배지) `f2fbece`: 신청자/관리자 화면에 "결재 N/M · 다음 이름" 진행표시, 사이드바 [결재함] 대기 배지(deptline만). code-reviewer 치명0, 배지 카운트 경량화. helper 7종 PASS.

**⚠️ 실화면 육안검증 남음(사장님 세션 필요)**: Phase 2(부서장 지정)는 뉴가온서 육안검증 완료. **Phase 3~4(실제 결재 흐름·진행표시·배지)는 로그인 2명(신청자+부서장)이 필요해 임시라우트 실코드검증으로 대체**했고, 실브라우저 E2E는 Chrome 확장 끊김으로 미실시 → 다음에 대표님이 브라우저 열면 deptline 켜고 휴가 신청→부서장 결재 흐름 육안확인 권장.

**🔜 남은 후보(결재선)**: ~~결재 거부시 화면 피드백~~ → **완료(2026-07-20, 위 블록)**. 전결·조건별 결재선·자동 에스컬레이션(2차)은 미착수(자동에스컬레이션은 백그라운드 스케줄러 인프라 필요=큰 결정).

### ✅ 완료(대기업무): 교대근무(시프트) 지원 — Phase 1~4 완성. Phase 5(셀프수정)는 범위 제외(2026-07-19 사장님 결정)
> 사무직 주간근무 MVP를 넘어 **교대근무 업체(공장·병원 등)** 지원. 스키마→설정→근무표→판정까지 완성. 비교대 회사는 100% 무영향(회귀 0).
> **문서 먼저 읽기**: 기획서 [docs/02_prd/교대근무_기획서.md](docs/02_prd/교대근무_기획서.md) · 설계 [docs/04_architecture/교대근무_설계.md](docs/04_architecture/교대근무_설계.md)

**확정 결정(사장님)**: ①로테이션(날짜별) ②야간조(자정 넘김) 지원 ③지각유예·기준시간 8h는 회사 공통 ④근무표=기본배정(자동)+날짜예외(덮어쓰기) ⑤~~직원 셀프수정~~ **→ 범위 제외(2026-07-19)** ⑥기본배정 2방식 지원(회사별 선택: 고정 요일패턴 / 순환 규칙) ⑦순환=조 A/B/C 단위. 추천확정: A=야간 출근일 귀속·C=배정일 미출근=결근·D=주52 현행·G=오늘포함 과거잠금·H=주경계 월00시·I=이번엔 조수=교대수(4조3교대 차기).

**완료 단계(커밋)**
- **Phase 0**(기획·설계): PRD `4323698·2efd4d4·735122e`, 설계 `a4ae39c`.
- **Phase 1**(토대) `657dd8d`: schema 6모델(Shift·ShiftPattern·ShiftGroup·RotationRule·ShiftAssignment·ShiftChangeLog)+Company/User 필드, **마이그레이션 적용**, `lib/shift.ts` 순수함수(crossesMidnight·atDate·nextDayISO·elapsedUnits(주=월00시)·rotationShiftOrder·isLateByShift·isEarlyLeaveByShift) — **25종 경계테스트 PASS**. add-only라 기존 무영향.
- **Phase 2**(설정 교대정의) `be73102`: [설정→근무제]에 교대근무 드롭박스(안함/2/3교대)→조별(이름·출근·퇴근) 입력·🌙야간표시 + 근무표방식(고정/순환). `saveWorkRules` 확장(shiftMode·scheduleType·조 order upsert). onboarding 기본값(off) 전달.
- **Phase 3a**(근무표 고정) `9d9cef7`: 신규 **[근무표] 화면 `/shifts`**(사이드바 메뉴 추가) — 고정 요일패턴 그리드(직원×요일→조), `actions/shift.ts` `saveFixedPattern`(트랜잭션 upsert). 미설정/순환은 안내문.
- **Phase 3b**(근무표 순환) `3325b3e`: `/shifts` 순환 분기 = 실제 **[RotationClient]** 화면 — ①순환규칙(단위 매일/매주/매월·시작 기준일) ②미리보기 표(각 조가 이번/다음/다다음 단위에 몇 교대 — 순수함수 rotationShiftOrder/elapsedUnits 재사용) ③직원 조 배정(A/B/C·미배정). 순환 순서는 **표준 고정**(1→2→3, 대표님 결정) — 저장값 아닌 **shiftMode 기준**으로 계산(교대수 바꿔도 미리보기 정확). `saveRotation`(조 upsert로 id유지·교대수 축소시 초과조 FK안전 정리·직원배정·규칙 upsert, 단일 트랜잭션·회사격리·관리자검증). 검증: **순수함수·저장로직 15종 + isRealDate 8종 PASS**(임시회사, 실계정 무오염), tsc·eslint 0, code-reviewer **치명0**(중간3·경미2 반영: 미리보기 shiftMode기준·배정카운트 유효범위·anchorDate 실날짜검증·직원필터 role/재직·트랜잭션 try/catch).

- **Phase 4**(판정 연결·핵심) `07b8a3f·821cfbb·ec2116a`: 교대제면 지각/조퇴/결근을 **그날 조 기준(자정 넘김)**으로 판정, 비교대면 기존 그대로(회귀 0).
  - **4a**(토대) `07b8a3f`: `lib/shift.ts` 순수함수 `resolveShift`(예외→fixed/rotation→휴무)·`judgeByShift`·`dowOfISO` + `lib/shift-server.ts` `loadShiftContext`(교대제면 컨텍스트 Map 1회 로드, 비교대면 null). 화면 미연결.
  - **4b**(중앙 허브) `821cfbb`: `buildDayEntries`에 선택적 `shiftResolver` → 직원상세·내근태 연결. 표시 컴포넌트 무수정.
  - **4c**(인라인 3화면) `ec2116a`: 근태현황목록·리포트(결근)·대시보드(오늘 지각/조퇴/미출근).
  - **검수 반영** `8fb2898`(code-reviewer 치명0, 중간2 수정): 교대 근무예정일 = 조배정(≠휴무) AND 근무요일 AND 쉬는날(공휴일·회사휴무일) 아님. 대표님 결정=근무요일+공휴일 결합. 이전엔 교대제가 offDays 무시(공휴일 결근 오판)·rotation 주말 결근 부풀음 → 해결.
  - 검증: 4a 19종 + 4b 17종 + 수정 6종 실코드 임시라우트(검증후 삭제) + 실DB(뉴가온 rotation/하늘테크 비교대) 통합 + 5개 라우트 307. tsc·eslint 0.
  - **실화면 육안검증 완료(2026-07-19)**: 뉴가온에 야간조(22-06) 근무 임시로 심어 [직원상세] 주간뷰에서 07-07 22:15→익일05:00=**지각·조퇴**, 07-09 22:00→익일06:00=**정상(실근무 8h)**, 07-06·08·10=**결근**, 07-12(일)=**휴일근무**(주말 제외) 확인 + [리포트] 결근3·실근무14:45 확인 → **검증데이터·교대시각 즉시 원상복구**(잔존0).
  - ⚠️ 알려진 한계: ①24h 공장은 [설정→근무요일]을 월~일로 둬야 주말도 근무로 잡힘. ②야간조는 근무시작 전에도 대시보드 "오늘 미출근"에 뜰 수 있음(기존 이른아침과 동일 성격). ③야간조가 자정 넘겨 지각출근하면 날짜귀속(출근일)이 익일로 잡혀 오판 가능(설계 A 한계, 드묾).

**🚫 범위 제외(2026-07-19 사장님 결정)**
- ~~**Phase 5**: 직원 셀프수정(미래만) + ShiftChangeLog 이력 + 알림·되돌리기~~ → **제외.** 영향: 특정 하루짜리 예외(그날만 조 변경/휴무)를 화면에서 설정하는 UI 없음. 정규 쉬는날(주말·공휴일·회사휴무일)은 이미 자동 처리. ShiftChangeLog·ShiftAssignment(self) 스키마는 add-only라 남아도 무해(미사용).

**🔜 남은 후보(교대근무 외)**
- **깃허브 푸시**: 그동안 로컬 커밋분(교대 Phase1~4 포함) 한 번에 `gaon1107/attendance-management`.
- ~~**별도(백그라운드 등록)**: [설정]에서 교대 수 3→2 축소 시 잔존 ShiftGroup·RotationRule.orderCsv·직원 shiftGroupId 정리 = `saveWorkRules`(settings.ts) 소관.~~ → **✅ 완료(2026-07-20 `a9c2e8a`)**. saveWorkRules에 순환 그룹 재조정 블록 추가(shift.ts saveRotation과 동일 패턴): 초과 조(order>=shiftMode) 참조 직원 해제→삭제 + orderCsv 갱신. company.update+shift정리+그룹정리를 단일 트랜잭션 원자화(부분커밋 방지). A안=shiftMode 기준(순환→고정 동일 교대수면 배정 보존). 검증 tsc·eslint 0 + 롤백검증 12체크 PASS + code-reviewer 재검수 치명·중간0. **⚠️ 실화면 육안검증 남음**(설정에서 3→2 바꾼 뒤 /shifts 순환화면 빈칸 없는지 대표님 확인 권장).

**주의/메모**
- **prisma generate 함정**: dev서버가 켜져 있으면 `migrate/generate`가 엔진 DLL 잠금(EPERM)으로 실패. 스키마 바꿨으면 **서버 껐다 켜기**(배치파일이 자동 generate). Phase 1 migrate는 적용됨, 클라이언트도 재생성 확인됨.
- **판정 연결됨(Phase 4 완료)**: 교대제 켠 회사만 조 기준 판정. 비교대 회사는 여전히 기존 로직(회귀 0, 실코드+실화면 검증).
- 검증DB(뉴가온)로 저장경로 시험 시 **반드시 원상복구**(이번 세션 그렇게 함) — 실계정 상태 오염 금지.
- **미푸시 33커밋+**(master, origin/main 대비, Phase 3b `3325b3e` 포함). 깃허브 푸시는 사장님 결정 대기.

### 📌 이번 세션(2026-07-19) 요약 — 다음 세션은 여기부터
- **완료 5건(전부 로컬 커밋, 아직 깃허브 미푸시)**: B-3 주52초과알림(`8bc69ee`) · B-4 법정기록PDF(`9d99b73`) · B-5 휴가유형확장(`3f2e343`) · C-2 알림센터통합(`6c7a8a2`) — (직전 세션 A-2 문자·B-2 연차자동발생 포함 시 origin/main 대비 **미푸시 16커밋**).
- **브랜치**: master (원격 main에 미푸시). 검증: 각 기능 tsc·eslint 0 + 순수함수/실DB 테스트 + 실화면 E2E(하늘테크) + code-reviewer 치명0.
- **🔜 다음 후보(사장님 택1)**: ① **깃허브 푸시**(그동안 로컬분 한 번에 — `gaon1107/attendance-management`) / ② **B-6 외출사유 커스텀**(백로그) / ③ 백로그 [보완작업-백로그-2026-07-17.md]에서 지정 / ④ 사장님 웹캠 실테스트 등 미완 확인.
- ※ 각 기능 상세·알려진 한계는 아래 블록 참고. 작업 규칙: dev-workflow(리서치→계획→승인→구현→검수), 완료는 실행 증거와 함께.

### 🔧 곁가지(2026-07-19): 반복 404 **영구 해결** — 실행 배치파일에 캐시 자동정리
- **증상**: 수정·테스트 반복 중 여러 페이지가 404(예: 알림센터→이상접속→확인 = `/security/alerts`). 코드 아님 = dev서버 `.next` 캐시 꼬임(오래 켜둔 세션에서 누적).
- **지난번 한계**: "수동으로 `.next` 지우고 재시작"이라 적어둬 재발함(방법만, 예방 아님).
- **영구 조치**: `근태관리_실행.bat`(사장님이 더블클릭하는 시작 파일)에서 `npm run dev` **직전에 `rd /s /q ".next"` 한 줄 추가** → 켤 때마다 항상 깨끗한 시작. CP949 인코딩 보존(한글 안 깨짐). webapp 기능코드 무수정.
- **검증(실행 증거)**: `.next` 삭제→새 dev서버(3000)→HTTP 확인 = `/security/alerts`·`/notifications`·`/dashboard`·`/leave/approvals`·`/corrections/approvals`·`/employees`·`/reports` **전부 307(정상, 로그인 안내)**, `/`·`/login` 200, 진짜 없는 주소만 404. 서버 빌드에러 0.
- **대가**: 켤 때마다 캐시 새로 생성 → 첫 화면 10~30초 더 걸림(404 방지 대가, 테스트용 수용).
- ⚠️ 남는 케이스: 깨끗이 새로 시작한 뒤에도 **계속** 404 나는 특정 페이지가 있으면 그건 진짜 코드버그 → 개별 수정 필요.

### 🆕 방금 완료: C-2 알림센터 통합 (헤더 종 배지 + /notifications) — 커밋 6c7a8a2
흩어진 관리자 알림(승인대기 휴가·근태정정·비번재설정·이상접속)을 **한 화면(/notifications)**에 모으고, **모든 관리자 페이지 상단바에 종 배지**(처리 필요 총건수)를 전역 노출.
- **사장님 결정**: 범위 = **처리필요 통합 + 헤더 종 배지**(최근활동 피드 제외) / 읽음 = **실시간 카운트**(저장 없음, 처리하면 자동 감소 — 대시보드와 같은 철학).
- **구성**: `lib/notifications.ts`(공용 `listAdminNotifications(companyId)` — 4종 카운트+total, 이상접속 try/catch 격리, **React cache()로 요청 내 1회만 집계**) / `NotificationBell`(관리자 상단바 종+배지) / `/notifications`(처리필요 카드+CTA·빈상태·대시보드 안내) / AppShell(관리자만 종) / Sidebar([알림] 메뉴).
- **일관성**: 종 배지 total = 알림센터 처리필요 합. 각 항목 카운트 = 해당 화면과 같은 `status:"pending"` 필터(배지 N→클릭 0 불일치 없음). 기존 대시보드 "오늘 알림"·보안화면 **무수정**. 스키마 변경 없음.
- **검수 반영**: 중간2(한 요청서 집계 2번=종+본문) → React `cache()`로 dedup(실시간 유지). 
- **검증**: tsc·eslint 0 / **실화면 E2E**(하늘테크): 임시 대기휴가 심고 → 종 배지·알림센터 6건(이상접속5+휴가1)·CTA·사이드바 [알림]·타 페이지 전역 종 확인, 카운트 일관, cache() 런타임 정상, 콘솔 에러 0 → 데이터 삭제정리 / code-reviewer **치명0**.
- ⚠️ **알려진 한계(사장님 판단)**: ①종이 모든 관리자 페이지에서 이상접속 집계를 실행 → **로그인 매우 많은 회사**면 페이지 이동마다 부하(대상=중소기업 소수라 현재 무리 없음, AccessEvent 인덱싱 백로그와 동일 사안. TTL 캐시는 "실시간" 결정과 어긋나 미도입). ②"처리 필요"엔 미출근·지각·주52초과 미포함(의도 — 대시보드로 유도). ③실시간이라 종/본문 사이 데이터 변경 시 순간 불일치 가능(새로고침 시 정합).
- ▶ **다음 추천**: 그동안(B-2·B-3·B-4·B-5·C-2) 로컬 커밋분 **깃허브 푸시** 또는 백로그 잔여(B-6 외출사유 커스텀 등)·사장님 지정.

### 🆕 방금 완료: B-5 휴가유형 확장 (경조휴가·공가·특별휴가) — 커밋 3f2e343
휴가 종류에 **경조휴가·공가·특별휴가** 3종 추가. 전부 **유급이되 연차 잔여 미차감**(deducts=false), 다일(기간) 신청 가능, 종일 휴가라 그날 출근 시 지각·조퇴 면제(병가 계열).
- **사장님 결정**: 방식 = **고정 유형 추가**(회사별 커스텀 아님) / 유형 = 경조휴가·공가·특별휴가(반반차·시간단위 제외).
- **핵심**: 모든 소비 화면(신청폼·검증·사용일수·차감·결근제외·라벨·승인·달력·연차정산)이 **`lib/leave.ts` 중앙 상수·헬퍼만 참조** → **lib/leave.ts 1파일만** 수정(LEAVE_TYPES·REQUESTABLE_TYPES·leaveSuppressesLate/Early에 3종 추가)로 전부 자동 반영. **스키마 변경·마이그레이션 없음**.
- **검수 반영(중간1)**: 무차감 다일 휴가는 잔여 한도가 없어 상한이 없었음(관리자 오승인 시 장기 결근 은폐 위험) → `actions/leave.ts`에 **1회 신청 근무일수 상한 60일** 가드 추가. 경미: schema·leave.ts 유형 주석 최신화.
- **검증**: tsc·eslint 0 / **실화면 E2E**(하늘테크): 드롭다운 3종 노출 → 경조휴가 07.20~07.22(3일) 신청→관리자 승인 → **승인 후에도 부여15/사용0/잔여15 그대로**(연차 미차감 확정), 검증데이터 삭제정리 / code-reviewer **치명0**.
- ⚠️ **알려진 한계(사장님 판단)**: ①공가·경조를 반나절만 써도 종일 휴가로 처리(그날 출근 시 지각·조퇴 면제) — 반일/시간단위는 별도 작업. ②1일만 쓸 때도 시작일=종료일 두 번 선택(다일형이라). ③승인 후 철회(취소) 액션은 기존부터 없음(별도 사안).
- ▶ **다음: C-2 알림센터 통합**(사장님 지시로 이어서 진행 예정).

### 🆕 방금 완료: B-4 법정기록 PDF (인쇄용 화면 → PDF 저장) — 커밋 9d99b73
관리자 리포트(`/reports`)에 **"🖨 법정기록 PDF"** 버튼 추가 → 새 인쇄용 화면(`/reports/print`)이 기간 내 근태를 **직원별 근로기록부**로 렌더. 상단 "인쇄/PDF로 저장" 버튼으로 브라우저 인쇄 → "PDF로 저장".
- **사장님 결정**: 방식 = **A(인쇄용 화면→PDF 저장)** — 새 라이브러리·한글폰트 **0**(브라우저가 한글 렌더). 구성 = **직원별 근로기록부**.
- **문서**: 머리말(회사명·기간·발행일) → 직원마다 구획(이름·역할·사번 + 날짜·요일·근무형태·위치·출근(시업)·퇴근(종업)·외출(회)·실근무 표 + **소계** 근무일수·실근무 합계·초과근무 합계·외출 합계). `@media print`로 툴바 숨김·A4 여백·**직원마다 새 페이지**.
- **일관성**: 조회·기간(92일 상한)·검색(q)·계산이 **기존 엑셀 내보내기와 동일** → 두 산출물 숫자 일치(실화면에서 리포트 숫자와 정확히 일치 확인). **기존 리포트/엑셀 라우트·근태 판정 무수정**, 새 라우트만 추가.
- **검수 반영(중간1)**: 반복 쿼리(`?q=a&q=b`)로 값이 배열이 오면 500 크래시 → `first()`로 첫 값만 취해 방어. 경미 반영: 미퇴근 행 **"(근무중)"** 표기(확정 안 된 근무임을 명시), 대용량 단일직원 블록 페이지나눔 안정화(`break-inside:avoid` 제거), 중복 회사조회 제거(세션 값 재사용).
- **검증**: tsc·eslint 0 / 실DB 10종 PASS(직원별 그룹·정렬·근무일수·실근무·초과근무·외출 소계) / **실화면 렌더 확인**(하늘테크 관리자: 소계=리포트 일치, 크래시 방어·근무중 표기, 콘솔·서버 에러 0) / code-reviewer **치명0**.
- ⚠️ **알려진 한계**: ①서버 TZ=Asia/Seoul 미고정 시 날짜·시각 어긋남(엑셀·리포트와 동일한 전사 배포조건). ②미퇴근 기록은 인쇄 시점까지로 계산돼 리포트와 시차 가능(꼬리말·"근무중"으로 고지). ③실제 인쇄 다이얼로그의 페이지나눔은 사장님 최종 클릭 확인 몫.
- 🖥️ **사장님 확인**: [리포트]에서 "🖨 법정기록 PDF" → 인쇄 화면에서 **[인쇄/PDF로 저장]** → 대화상자에서 대상을 **"PDF로 저장"**으로.
- ▶ **다음 추천**: B-5 휴가유형 확장 / C-2 알림센터 통합 / 또는 그동안(B-2·B-3·B-4) 로컬 커밋분 **깃허브 푸시**.

### 🆕 방금 완료: B-3 주 52시간 초과근무 알림 (화면 알림만) — 커밋 8bc69ee
관리자 대시보드 "오늘 알림"에 **"⏱ 주 52시간 초과근무"** 한 줄 추가. 이번 주(월요일 00:00~현재) 각 직원 실근무 합계가 위험선을 넘으면 이름과 함께 표시.
- **사장님 결정**: 주 52시간(법정) 기준 / **화면 알림만**(문자·이메일 자동발송 없음 — "죽은 스위치 금지" 원칙과 일치).
- **판정 2단계**: 🟡근접(회사 설정 경고선, 기본 48h↑) / 🔴초과(법정 52h↑=법 위반 위험). **52h는 코드 고정 상수**(`lib/overtime.ts` `LEGAL_WEEKLY_LIMIT_MIN=3120분`), 회사는 **근접 경고선(30~52h)**과 **켜기/끄기**만 조정(5인미만·특례업종 OFF).
- **"1주" = 월요일 00:00~현재**(근로기준법 1주=7일, 서버 TZ=Asia/Seoul 가정 — 전사 공통).
- **스키마**: Company에 `overtimeAlertOn Boolean @default(true)`·`overtimeWarnHours Float @default(48)` 추가(마이그레이션 `20260718145429_overtime_alert`, 기본값=하위호환). 기존 clockIn/clockOut·worktime.ts **무수정**(조회·표시만 add-only).
- **구성**: `lib/overtime.ts`(순수함수 `weekStartMonday`·`weeklyLevel`·`cappedEnd`) / dashboard 이번주 조회+집계(퇴사자·관리자 제외, try/catch로 본체 무영향) / WorkRulesForm·saveWorkRules(켜기+경고선).
- 🔑 **검수 핵심 반영(중간1)**: **잊은 퇴근(미퇴근인데 며칠 열린 기록)이 여러 날로 부풀어 허위 "초과(빨강)" 경보**를 내는 문제 → `cappedEnd`로 미퇴근 기록은 **출근일 자정까지만** 인정. 오늘 진행 중 정상기록은 실시간 유지. 리뷰어 확인: 상한은 값을 "줄이기만" 해 warn/over를 부풀릴 수 없음(수학적 확인).
- **검증**: tsc·eslint 0, 순수함수 **21종 PASS**(주/월 경계·48/52 경계·cappedEnd 경계), 실DB **9종 PASS**(초과54h→over·근접49h→warn·정상30h→ok·퇴사자/관리자 제외·**잊은퇴근 상한으로 허위초과 방지 실증**). code-reviewer **치명0**(재검토까지 신규결함0).
- ⚠️ **알려진 한계(사장님 판단)**: ①직원 많은 회사는 대시보드가 매 렌더마다 한 주치 출퇴근 로드(성능 — Attendance 인덱스 백로그와 동일 사안, 대상=중소기업 소수라 현재 무리 없음). ②미퇴근 기록 1건은 여전히 최대 ~그 날 24h까지 더해질 수 있음(다일 폭주는 해소, 실제 출근시각이면 ~15h). ③서버 TZ=Asia/Seoul 미고정 시 주 경계 어긋남(전사 공통 배포 조건).
- 🖥️ **사장님 육안 확인 1가지(선택)**: 알림 줄은 이번 주 52h 근접·초과 직원이 있어야 뜹니다. 지금 실데이터엔 해당자가 없을 수 있어, 보시려면 [설정→근무제·기준시간]에서 **근접 경고선을 낮게(예 30h)** 두면 대시보드에서 노랑/빨강 표시를 확인할 수 있습니다.
- ▶ **다음 추천: B-4 법정기록 PDF** 또는 사장님 지정(B-5 휴가유형 확장 / C-2 알림센터 통합 등).

### 🆕 방금 완료: A-2 문자(아이원24) 발송 — 초대·임시비번 — 커밋 bce0824~1161a49 (5개)
직원 초대링크·임시비밀번호를 **문자(아이원24 iOne24)로 발송**. 원본(가온출결 Java 가이드 `newgaon-LMS/SMS_연동_이식_가이드.md`)의 **API 규격만 TS로 재구현**(코드 복붙 아님).
- **사장님 결정**: 문자만 / 초대·임시비번만 / 시스템 공용 계정(.env) / **버튼 수동 발송 + 1회 제한**(비용통제).
- **동작**: 초대하면 링크는 화면에 뜸(무료). 관리자가 **"문자로 보내기"** 눌러야 발송(유료). 초대 1건·요청 1건당 **1회**(DB 유니크로 원자 차단, 실패 시 재시도 허용).
- **구성**: `lib/sms.ts`(EUC-KR 인코딩·90byte SMS/LMS 자동구분·응답 비어있으면 성공·10초 타임아웃) / `SmsLog` 테이블(1회제한·발송이력·비용근거, 임시비번 본문 미저장) / `app/actions/sms.ts`(회사격리·초대토큰검증·claim원자화) / InviteLink·PendingResetRequests 버튼.
- **검증**: 순수함수 17종 + 가짜 아이원24 HTTP 왕복 10종(**한글 EUC-KR 왕복 정확**) + 1회제한 DB 4종 PASS. 실화면: 버튼 렌더·계정미설정 안전실패·SmsLog 기록·초대 무영향 확인. tsc·eslint 0.
- **code-reviewer 치명0·중간5**: 3건 반영(1회제한 원자화·타임아웃·초대URL 토큰검증), 2건 문서화(임시비번 서버검증=평문저장 위험이라 보류·영향경미, 평문HTTP=아이원24 벤더사양).
- 🔴 **사장님이 하실 것(실발송 전 필수)**: `webapp/.env`에 **`IONE_SMS_ID`(아이원24 계정ID)·`IONE_SMS_PW`(비번)·`IONE_SMS_SENDER`(발신번호, 아이원24에 사전등록된 번호)** 추가. (선택 `IONE_SMS_SEND_URL`, 기본=가온 엔드포인트) → 그 뒤 실제 문자 1건 발송 확인(발송비 발생). **이 환경에선 실계정·실비용이 없어 실발송 최종확인 불가**.
- ⚠️ **알려진 한계**: ①임시비번 SMS는 직원 phone이 있어야 발송(없으면 안내). ②평문 HTTP 전송(벤더사양). ③임시비번 본문을 서버가 재검증하진 않음(관리자 권한 내·자가교정). ④비용 추가통제(회사별 월한도)는 향후(SmsLog가 근거).
- ▶ **다음 추천: B-3 초과근무 알림** 또는 사장님 지정.

### ✅ 완료: B-2 연차 자동발생 (입사일 기준·간소) — 커밋 f204677~6c636ef (6개)
관리자가 손으로 넣던 연차를 **입사일 기준으로 자동 발생**으로 전환. 근로기준법 제60조 간소형: **1년미만=월1일(≤11) / 1년+=15 / 3년+부터 (근속연수−1)/2 가산, 상한 25**. 관리자는 예외 **수동조정(override)** 가능(특별부여·감액), "자동으로 되돌리기"로 해제.
- **사장님 결정**: 입사일 기준 / 간소(개근·80%출근율은 "충족 가정") / 수동조정 유지 / 기존 annualLeaveDays는 레거시(안 읽음).
- **스키마**: `User.annualLeaveOverride Float?` 추가(마이그레이션 `20260718080904`, nullable=하위호환). null=자동, 값=수동. 기존 annualLeaveDays 컬럼은 보존하되 미사용.
- **공용 헬퍼 `annualLeaveGranted(user)`** = override ?? `grantedAnnualLeave(hireDate)`. **발생 읽는 5곳 전부 이걸로 통일**(신청초과검사·직원[휴가]·직원상세·연차정산 화면·정산 엑셀) → 숫자 불일치 방지.
- **검증**: 순수함수 19종 PASS(경계값·override) + **실화면(하늘테크 관리자)**: 연차정산 발생15/총30, 직원상세 자동15→수동20 저장(‘수동 지정’+되돌리기 등장)→자동15 복귀, /leave 15일 정상. tsc·eslint 0.
- **code-reviewer 치명0·중간1·경미4** — 중간(레거시 커스텀값 백필 없음)은 **실DB 확인결과 커스터마이즈 0건 → 실영향 0**(사장님 승인 결정과 부합). 경미는 아래 ⚠️.
- ⚠️ **알려진 한계/배포 주의(사장님 판단)**: ①**직원 입사일(hireDate) 미입력이면 전원 15일 고정** — 자동 근속가산이 안 나옴. 관리자가 [직원상세]에서 입사일을 채워야 근속 반영됨(UI에 "입사일 입력 시 정확 계산" 안내 있음). ②서버 TZ=Asia/Seoul 고정 필요(자정 경계 하루 오차 방지 — 기존 전사 주의사항과 동일). ③월말/윤년 입사일(1/31·2/29)은 발생일이 최대 하루 밀릴 수 있음(간소·override 보정). ④**운영 배포 시**: DB에 커스텀 연차값(annualLeaveDays≠15) 있으면 마이그레이션에 1회 백필(`UPDATE User SET annualLeaveOverride=annualLeaveDays WHERE annualLeaveDays<>15`) 여부 결정 — 현재 테스트DB엔 0건.
- ▶ **다음 추천: A-2 이메일/문자 발송 연동**(파일럿 필수) 또는 B-3 초과근무 알림, 사장님 지정.

### ✅ 완료: 조퇴·오전/오후 반차 신청→승인→근태 자동반영 (커밋 7986f1e)
사장님 모델: **직원이 [휴가]에서 조퇴·오전반차·오후반차 신청(미래 예약 가능) → 관리자 승인 → 그날 출퇴근 시 자동으로 그렇게 처리**(지각·조퇴 오탐 없이 정식 표기). B-1 자동감지는 유지 → "승인 조퇴 vs 무단 조퇴" 구분 완성. **스키마 무변경**(LeaveRequest.type에 half_am/half_pm/early_leave 추가).
- **규칙**: 오전반차=지각 면제 / 오후반차·조퇴=조퇴 면제 / 조퇴=연차 차감0·반차=0.5 차감 / 승인 없으면 무단 조퇴 그대로 잡힘.
- **수정 12파일**: 공용 lib/leave.ts·dayentries.ts + 신청폼·records·RecordsClient·DetailTable·MonthCalendar·dashboard·reports + my-records·records/[userId].
- **검증**: tsc·eslint 0, 로직 13종 PASS, code-reviewer 치명0·중간2 반영(①조퇴는 하루휴가 아님→무단결근 은폐 방지 ②달력에 승인+미면제위반 병기).
- ⚠️ **알려진 한계(검수 경미, 사장님 판단)**: ①오전/오후 반차 승인 후 그날 아예 무단결근이면 "휴가"로 표시돼 반나절 결근이 안 잡힘(반차의 구조적 한계) ②같은 날 반차+연차 중복신청 방지 없음(1.5일 이중차감 가능) ③pending 여러 건 몰아 승인 시 잔여 초과 가능(기존 한계).
- 🖥️ **미검증**: 실화면 렌더는 서버 재시작+전체빌드 검증 필요(아래). 직원 신청→관리자 승인→근태상세 반영 사장님 실확인 권장.
- ▶ **다음 추천: A-2 이메일/문자 발송 연동** 또는 사장님 지정.

### ✅ 완료: B-1 조퇴(早退) 판정 신설 (커밋 52f2b2f)
지각과 대칭으로, 퇴근 기준시각(`workEndTime`)보다 일찍 퇴근하면 "조퇴"로 판정·표시. **workEndTime은 이미 설정 저장되던 휴면 필드라 DB 무변경(마이그레이션 없음).**
- **수정 11파일**: 공용 `worktime.ts`(isEarlyLeave 추가)·`dayentries.ts`(early/earlyLeaveCount/hasEndRule) + 표시 DetailTable·MonthCalendar·records/page·RecordsClient·dashboard·WorkRulesForm + my-records·records/[userId](workEndTime select) + globals.css(.kpi-grid-5).
- **표시**: 근태상세(KPI "조퇴"+표 "지각/조퇴" 동시뱃지)·달력 조퇴뱃지·전체 근태현황·대시보드 오늘 조퇴 명단. 지각+조퇴 동시 가능.
- **게이팅**: 근무일 + 퇴근기록 있음 + workEndTime 설정 시에만. 미설정 회사는 "—"(영향 0).
- **검증**: tsc·eslint 0, isEarlyLeave 순수함수 7종 PASS, 실DB(뉴가온 18:00) 정확. code-reviewer 치명0·중간2 반영/보고.
- ⚠️ **알려진 한계(사장님 판단 필요)**: ①**오후 반차**로 일찍 퇴근하면 조퇴로 잡힘 — 지각의 오전 반차→지각과 **동일한 기존 한계**. 지각·조퇴 함께 "승인 휴가일은 판정 제외"로 고치는 건 별도 작업(원하면 진행). ②자정 넘김 야간근무 미지원(지각과 동일).
- 🖥️ **사장님 최종 육안 확인 1가지**: 근태상세 상단 KPI가 이제 5장(근무일수·실근무·지각·조퇴·결근)입니다. 데스크톱에서 잘 배치되는지 한 번만 봐주세요(인앱브라우저가 로컬서버 접근 못 해 제가 픽셀 확인은 못 함 — 다만 RecordsClient 4장은 기존 4개 화면과 동일 패턴이라 안전).
- ▶ **다음 추천: A-2 이메일/문자 발송 연동**(초대·임시비번 자동발송) 또는 사장님 지정.

### ✅ 완료: C-1 사번(employeeNo) 회사내 중복검사 (커밋 473003e)
같은 회사에서 사번이 겹치게 저장되던 것을 앱단에서 차단(email 중복검사와 같은 방식, 스키마 무변경=마이그레이션 없음).
- **수정 3파일**: `lib/employee-profile.ts`(공용 헬퍼 `employeeNoTaken` 추가, parseProfile 무수정) / `updateEmployeeProfile`(관리자수정) / `acceptInvite`(초대가입).
- **규칙**: 사번 미입력(null)은 검사 안 함 · 활성직원만(퇴사자 사번 재사용 허용) · 본인제외 · **사번이 실제 바뀔 때만 검사**(레거시 중복 데이터 있어도 전화번호만 수정하는 저장은 안 막힘 — 검수 중간지적 반영).
- **검증**: tsc·eslint 0, 실DB 시나리오 6종+레거시가드 3종 전부 PASS, 기존 중복 사번 0건 확인. code-reviewer 치명0·중간1(반영).
- 경미(추후): 대소문자/전각 정규화 없음(사번=숫자·하이픈 위주라 영향적음), DB 유니크제약 없어 동시제출 완벽차단 아님(관리자1명·간헐가입이라 실위험 극소).
- ▶ **다음 추천: B-1 조퇴 판정** (지각과 대칭, worktime.ts:33 isLate 참고) 또는 사장님 지정.

### ✅ 완료: A-1 생체정보 파기 → 얼굴서버(GaonFR) 원본삭제 배선 (커밋 b832b0a, 실검증 통과 e4d4872)
직원이 얼굴정보를 철회/파기할 때, 지금까지 우리 DB표시·로컬사진만 지우고 **사내 얼굴서버(GaonFR)의 얼굴 원본은 안 지워지던** 미완성(코드주석 "2단계") 부분을 실제 삭제로 연결. 법적 파기 의무 이행.
- **수정 1파일**: `webapp/app/actions/authmethod.ts`. 파기 3함수(chooseGps·withdrawBiometric·adminRevokeBiometric)에 `unenrollFaceSafely` 배선. 스키마 무변경(마이그레이션·서버끄기 불필요).
- **동작**: GaonFR 먼저 삭제 → 성공했을 때만 등록표시(faceEnrolledAt/Count) 해제(실패 시 재시도 서명 남김). 철회 자체(authMethod=gps·consent=null)는 항상. 감사로그 result = 로컬+GaonFR 둘 다 성공해야 "성공".
- **안전장치**: GaonFR 무응답 시 10초 타임아웃(face.ts 무수정, call-site Promise.race)으로 화면 안 멈춤. 유령 "얼굴 등록됨" 표시 버그도 해소.
- code-reviewer **치명0**(2회 검수, 화면·출퇴근 회귀 없음 코드대조 검증). tsc·eslint 0.
- ✅ **사장님 실검증 통과(2026-07-17 23:53)**: test@test.com 얼굴등록(3/3)→출근→[인증방식] 본인 철회 실행 → DB확인 결과 **파기기록 `self_withdraw` 성공 + faceEnrolledAt 해제 + 사진 34장 전부 파기 + 얼굴서버 원본삭제(=result 성공이 곧 GaonFR 삭제 성공의 증거)**. [보안로그→접속로그→설정·파기]에 "생체정보 파기/본인 철회/성공" 표시 확인. → **A-1 완전 종료.**
  - ⚠️ 검증 중 교훈: 서버 껐다 켠 직후 **옛 브라우저 화면**으로 액션 누르면 조용히 무시됨(서버액션 stale). 로그아웃→재로그인→강력새로고침(Ctrl+Shift+R)으로 해결. 한 브라우저 두 계정 동시로그인 금지도 재확인.
- 🟡 **후속(A-1-b, 백로그 기록됨)**: GaonFR 삭제가 실패했을 때 자동 재시도할 운영장치 없음 → 재시도 배치 or fail로그 관리자 노출(배포 필수 아님, 파기 완결용).

### 🧭 지금 할 일 = 보완작업 백로그에서 선택 (최우선 안내)
곁가지(공휴일·일정캘린더·공지통합·공지표시날짜·근태상세 기본'일') **전부 완료**, 얼굴인증 웹캠 **사장님 확인 완료**(정상 99%·위조 잘 잡힘).
→ **다음 작업은 [보완작업-백로그-2026-07-17.md](보완작업-백로그-2026-07-17.md)에서 사장님이 항목 선택 대기 중.**
- 백로그는 "메뉴별 보완 목록"을 **실제 코드·디자인 목업과 전수 대조**해 검증한 결과(근거 파일경로 포함).
- ⚠️ 옵션 B(웹캠) = **PC 활동 데스크톱 앱**으로 전환·개발 보류 → 백로그에서 제외(재개는 별도 결정).
- 추천 순서: **A-1 생체정보 파기 배선(작음·법적) → C-1 사번중복·B-1 조퇴판정(빠른개선) → A-2 발송연동**.
- ※ 디자인 목업 34개 중 24 완료·4 부분·6 미개발. 목록 밖 새 항목 발견: **교육 이수 관리·조직도**.

---


### 🆕 이번 세션 추가 완료(4): 공지 통합 2차 — [공지] 메뉴를 일정 캘린더로 흡수 (곁가지 → 완료)
사장님 승인("2차 진행 해줘")으로 구현. 사장님 결정 = **옵션 A(공지는 "올린 날" 칸에 표시, 데이터 구조 무변경 → 마이그레이션 불필요)**.
- **표시**: 공지(Announcement)를 캘린더 작성일 칸에 📢로 표시, 클릭→모달에서 **본문 읽기**. 대시보드 캘린더/최신공지 연동.
- **관리자**: 캘린더 상단 **"공지 작성"**(오늘 날짜로 등록·전 직원 알림) + 모달에서 **삭제**. 대시보드 최신공지 클릭→**모달 본문**.
- **직원**: 공지 **읽기전용**(작성/삭제 버튼 없음). [일정] 메뉴로만 접근.
- **[공지] 메뉴 제거**(관리자·직원 양쪽). `/notice` 라우트 파일은 **보존**(안전망, 링크만 끊음).
- **NEW 뱃지 유지**: 출퇴근 배너의 "새 알림 N" 유지, 링크만 **공지 작성월의 `/schedule`**로. 그 달 안 읽은 공지가 있을 때만 읽음처리(다른 달 열람으로 뱃지 사라지는 버그 방지 — 검수 지적 반영).
- **데이터 병합 안 함**(Announcement 그대로). 스키마 무변경.
- 실화면 검증: 관리자 작성/삭제·대시보드 모달, 직원 읽기전용·개인일정, 사이드바 [공지]없음, **NEW뱃지(다른달=유지/공지달=해제)·배너 작성월 이동** 전부 확인. tsc/eslint 0.
- code-reviewer: 권한·회사격리·XSS **견고**. 중간3건 중 **①뱃지 가시성·②본문 길이제한 반영**, **③타임존은 배포 설정(TZ=Asia/Seoul) 사안**으로 아래 ⚠️에 기록.

### 🆕 이번 세션 추가 완료(5): 공지 "표시 날짜" 지정 (곁가지 → 완료, 커밋 8ef2d34)
사장님 지적("작성일 말고 원하는 날짜에 표기")으로 옵션 B 구현. `Announcement.noticeDate`(선택) 추가, 마이그레이션 `20260717101408_add_notice_date`(nullable=하위호환).
- 공지 작성 폼에 **공용 DatePicker**(기본 오늘) — 관리자가 표시 날짜 지정. 미지정이면 작성일에 표시(기존 공지 그대로).
- `/schedule`·출퇴근 배너·대시보드 최신공지 전부 **표시날짜 기준**으로 일치. 실화면 검증(25일·28일 지정 → 그 칸 표시, 대시보드 라벨 일치, 성공 시 폼 초기화).
- code-reviewer 치명0. 반영: **무효날짜(2/29·13월 등) 서버 차단**·**성공 시 폼 닫아 초기화**·**대시보드 날짜 일치**.
- ⚠️ **알려진 한계(사장님 "이대로 두기" 결정)**: 읽음여부가 "마지막 확인시각 1개"라, 안 읽은 공지가 여러 달에 흩어지면 한 달만 열어도 NEW 배지가 다 사라질 수 있음. **공지 자체는 캘린더에 남음**(배지 편의기능 한계). 근본해결=공지별 읽음추적(별도 개발, 필요시 진행).
- ▶ **곁가지(공휴일·일정 캘린더·공지 통합·표시날짜) 전부 종료** → 마스터 플로우(얼굴인증 2phase 웹캠 실테스트) 복귀.

### 🆕 이번 세션 추가 완료(2): 일정 캘린더 1차 (곁가지)
MES식 **회사 일정 캘린더** 신설(관리자 [일정] 메뉴, `/schedule`). 커밋 fb8ce15·7154ec5.
- 월 달력에서 **날짜 클릭 → 모달**: 회사 일정 추가/삭제(막대 표시) + 그 날 **회사 휴무일 지정/해제**.
- **법정공휴일 자동 표시**(빨강, 읽기전용) + 회사 휴무일(보라) + 회사 일정(막대) 한 화면에.
- 새 표 `CompanyEvent`(일정, 표시 전용·근태 무관). 휴무일은 기존 CompanyHoliday 재사용(지각·결근 판정 연결 유지).
- code-reviewer 치명1(휴무 토글이 캘린더에 반영 안 됨 = revalidate 누락) 수정·재검증. tsc/eslint 0.
- ✅ **2차 완료(커밋 6a36a9e)**: 대시보드 "오늘 출퇴근 현황" 표(근태현황과 중복) 제거 → **이번 달 일정 캘린더**(DashboardCalendar, 읽기전용) 삽입. 상단 KPI·우측 알림(미출근/지각 명단)·실시간 근무인원 전부 유지. code-reviewer 치명0. 실화면 검증 완료.
- ▶ **곁가지(공휴일·일정 캘린더) 전부 종료** → 마스터 플로우(얼굴인증 2phase 웹캠 실테스트) 복귀 가능.

### 🆕 이번 세션 추가 완료: 공휴일·회사휴무일 반영 (곁가지 → 완료)
사장님 질문(공휴일에 지각 뜸)에서 출발 → **평일 공휴일/회사휴무일에 지각·결근 판정 안 하고 "휴일근무" 처리** 기능 완성.
- **자동 공휴일(A)**: 공공데이터포털 특일정보 API로 수집→`Holiday` 표 저장→화면은 저장분 사용(장애무관). [설정→공휴일·휴무일]의 **[지금 갱신]** 버튼(올해·내년). `.env`에 `HOLIDAY_API_KEY`(사장님 발급, 노출 시 재발급 가능).
- **수동 휴무일(B)**: `CompanyHoliday` 표(회사별). 창립기념일·임시공휴일 즉시 추가. 자동토글과 무관하게 항상 적용.
- **토글**: `Company.holidayAutoOn`(기본 ON). 공휴일 정상근무 업종은 OFF.
- **적용 범위**: 지각·결근·연차 차감 전부(6개 화면 연결). 공통함수 `isWorkDay` 무수정+`isEffectiveWorkDay` 추가(회귀격리).
- **검증**: 정부 API 실호출(2026 22건·2027 24건, 대체공휴일 포함), **내근태 실화면 7/17 전건 "휴일근무" 확인**, tsc/eslint 0, code-reviewer 치명0. 마이그레이션 `20260717030911_add_holidays` 적용됨.
- ⚠️ **실서비스 배포 시**: 서버 TZ=Asia/Seoul 고정 확인(날짜 비교 KST 가정). 신규 회사도 자동반영 ON이나 Holiday 비어있어 [지금 갱신] 전엔 변화 없음.

**지금 위치**: 2차 화면 3종 전부 완료 → **마스터 플로우 복귀 = 얼굴인증 2phase 웹캠 실테스트 진행 중**(사장님 직접).

### 🔴 사장님이 직접 하실 것 (웹캠 필요 — 코드 아님)
서버 켜고(`cd webapp && npm run dev`) **사장님 브라우저**에서 `localhost:3000`, 카메라 허용 후 `test@test.com`/`aaaa1111**`(뉴가온 직원) 로그인.
1. **촬영 오탐 수정 확인(이번 세션 A안 수정)**: 진짜 얼굴 출근이 이제 **"정상"으로 뜨는지**(전엔 "위조 93%" 오탐), 폰화면·정지영상은 **여전히 "위조 의심"으로 걸리는지**. → 결과를 Claude에게 알려주면 오탐 남았을 때 추가 수정.
2. **라이브니스 기준값 확정(기존 대기)**: 진짜/폰사진/오린 인쇄사진 **점수 3개** 모아 [설정 → 본인 확인 재검토 기준] 확정(현재 임시 85%). 모델A 60·모델B 85·밝기 게이트는 데모 실측값 → 현장 카메라로 재확정.
   ※ 관리자로 결과 볼 땐 **뉴가온 관리자 `psh01@newgaon.co.kr`**(하늘테크 `admin@admin.com` 아님 — 회사 다르면 안 보임). **[근태현황]**에서 봄(대시보드는 오늘만).

### 🟡 Claude가 이어서 할 수 있는 것 (사장님 지시 시)
- **촬영 오탐이 남으면** 추가 수정(예: 예열 시간 조정, 안전망 강화).
- **개인정보 ① 전건 저장·90일 보관** = 정책/법무 결정 대기(최소수집상 "위조 의심분만 보관"이 안전하나 사장님·전문가 판단). 동의 문안 초안 → 전문가 검토.
- 그 밖: 실서비스 전 과제(위수탁계약·server03 운영전용계정·PHOTO_CONSENT_SINCE 실배포시각 조정·카카오 REST 키 재발급·🚨 nginx XFF).

### ✅ 이번 세션(2026-07-16~17)에 한 일
- **2차-2번 접속/보안 6단계 완료**(이상접속 알림까지) · **2차-3번 실시간 현황판 완료** → 2차 화면 3종 종료.
- **정상 사진 관리자 열람 차단**(개인정보 ②, 커밋 8bed79e·b86ac9f) — 위조 의심·판독실패만 열람.
- **얼굴 촬영 "3장 동일" 오탐 수정**(A안, 커밋 35dd068·10c886d) — 사장님 웹캠 최종확인 대기.
- 환경 점검: 얼굴서버(gaonfrdev) 살아있음·.env FACE_* 있음·얼굴등록 3건/동의 3명/사진 13건 남음.

### ⚠️ 작업 규칙 리마인더
- Prisma 스키마 변경 시 **사장님 3000 서버 끄고**(EPERM). 한 브라우저 두 계정 동시로그인 금지.
- 코드 수정은 dev-workflow(리서치→계획→승인→구현→검수), 완료는 실행 증거와 함께.

---

## 현재 상태(2026-07-17): 2차 화면 3종 **전부 완료** → 마스터 플로우(얼굴 2phase) 복귀 대기
사장님과 "2차 화면" 3종(①월간캘린더+연차정산 ②접속/보안 모니터링 ③실시간 현황판)을 순서대로 개발 — **3종 모두 완료.**

### ✅ 2차-1번: 월간 캘린더 + 연차 정산 — 완료
- **월간 캘린더**: `records/[userId]`에 표/달력 토글 추가(기존 MonthCalendar 재활용). 커밋 3e6d182.
- **연차 정산(간소형)**: `/leave-summary` 신설 — 전 직원 발생·사용·잔여를 연도별 표+KPI+엑셀. **연도 자유선택(회사 도입연도~올해)**. 과거 연도는 발생 이력이 없어 '사용'만 정확·발생/잔여는 "—". 사이드바 "연차정산"(관리자). 커밋 5e73e57·83faa6f. code-reviewer 치명0(중간·경미 반영 완료).
- DatePicker 컴포넌트 + narrow 레이아웃 제거 마무리(커밋 dd48e32).

### ✅ 2차-2번: 접속/보안 모니터링 — **완료(6단계 전부)**
사장님 결정(7/14): **풀세트**(로그인이력+IP접속로그+이상알림+차단IP·자동차단). 큰 작업이라 6단계 분해(계획: plan.md).
- **1단계 ✅(커밋)**: `AccessEvent` 테이블 신설 + 로그인 성공/실패/로그아웃/가입 기록 시작(auth.ts·invites.ts add-only, 판정로직 무수정). `lib/access-log.ts`(기록 실패해도 본기능 안 막음)·`lib/device.ts`·`lib/access-labels.ts`.
- **2단계 ✅(커밋 daf2ce2까지)**: `/security/logins` 로그인 이력 화면(기간달력+검색+KPI+엑셀), 사이드바 "보안로그"(관리자). code-reviewer 치명0, 중간① 엑셀 상한경고 반영.
- **3단계 ✅(2026-07-16, 커밋 4315fa1·2584f7a·5f7ee55·c6d1420 + 파기정지)**: `/security/access` 접속 로그 화면 — 로그인+출퇴근 통합, IP·기기·**사내망/외부 판정**(기존 `ipMatches`+`officeIps` 재사용), 기간달력·통합검색·동작필터(전체/로그인/출퇴근)·KPI 4종·엑셀. **[보안로그] 안에 탭 2개**(로그인 이력|접속 로그)로 진입 — 사이드바 메뉴 무증설. 출퇴근 기록은 `attendance.ts` clockIn/clockOut **맨 끝 add-only**(사장님 승인=옵션 A) → 호출처 4곳 자동 커버·중복 없음. **스키마 변경 없음**(AccessEvent 재사용 → EPERM 문제 없었음). code-reviewer 치명1(아래)·중간5·경미2 전부 반영.
- **4단계 ✅(2026-07-16, 커밋 b2ace5f·7999e4b)**: **관리자 감사로그** — "누가 언제 무엇을 바꿨나". `lib/audit.ts` 신설(`logAdminAction`, 기존 공통모듈 무수정으로 위험 격리). 기록 대상 = **설정 5종**(위치·**사내IP**·얼굴기준·판독기준·근무규칙) + **생체정보 파기 3종**(관리자 파기·본인 철회·본인 GPS전환). 접속 로그 화면에 **"설정·파기" 필터** 추가. **스키마 변경 없음**(kind에 config/purge 이미 존재). 원칙: ①본기능 무차단(헤더 미리 읽고 `after()`) ②**바꾼 값은 안 남기고 설정 이름만**(로그 유출 방지) ③성공 지점에만(실패는 기록 안 함) ④**사실대로만**(지울 게 없으면 파기로 기록 안 함, 파기 실패 시 result=fail). code-reviewer 치명0·중간6·경미 다수 전부 반영.
- **5단계 ✅(2026-07-16, 커밋 5단계+6604981)**: **차단 IP + 차단 후보 표시** + (4단계 잔여) **생체정보 동의 개시 기록**. `BlockedIp` 모델 신설(마이그레이션 `blocked_ip` — 새 표만). `lib/ip-block.ts`(판정 순서가 곧 안전장치)·`actions/ip-block.ts`·`/security/blocked` 화면(탭 3개째). **자동차단 안 함**(사장님 결정) — 최근 14일 실패 3회+ IP를 **후보로 보여주기만**, 막을지는 사람이 결정(사무실은 전원이 같은 공인 IP라 오탐 시 회사 전체 잠김). 차단은 **로그인 진입점에만**(출퇴근 본체 무수정 유지). **Next 16 = 미들웨어가 `proxy`로 개명**(Node 런타임이라 DB 가능하나 문서가 "인증 용도 금지" 명시 → 전역 proxy 미사용). code-reviewer 치명2·중간5·경미 다수 전부 반영.
  - 🛡️ **자기잠금 방어 5중**: ①사내망(officeIps) 무조건 통과 ②내 현재 IP 포함 규칙 저장 거부 ③차단은 로그인에만(로그인된 관리자는 안 잠김) ④**사내망 미등록 회사는 대역 차단 자체를 거부** ⑤**최근 성공 로그인 IP 20건과 겹쳐도 거부**. 최후 복구 = DB에서 `BlockedIp` 행 삭제.
- **6단계 ✅(2026-07-16, 커밋 063cdfc·e10602f·c13ee3b·cb0a042·5f87991+73bb128)**: **이상접속 알림** — 2차-2번 **완료**. `lib/anomaly.ts` 신설(읽기 전용 격리 — 기존 기록·판정·출퇴근 무수정). `/security/alerts` 화면(탭 4개째) + 대시보드 "오늘 알림"에 배지 1줄 + [설정] 감지 기준 폼 + `securityCheckedAt`으로 [모두 확인]. **감지 규칙 3종**(사장님 결정): ①**차단 IP 재시도**(항상 켬 — 직접 막은 IP라 오탐 0) ②연속 로그인 실패(하루 5회, 끌 수 있음) ③심야 로그인(22시~06시 전까지, 끌 수 있음). **"처음 보는 기기" 제외**(사장님 결정 — deviceLabel이 "Windows PC" 수준으로 거칠어 신뢰도 낮고 도입 첫달 오탐 과다). **이메일·문자 스위치 미제작**(사장님 결정 — 실발송 불가한 죽은 스위치는 관리자를 속임, 배포 때 진짜로). 스키마: Company에 컬럼 6개(전부 기본값). code-reviewer 치명3·중간5·경미5 전부 반영.
  - 🔑 **묶음이 이 기능의 핵심**: 차단 IP가 1분마다 두드리면 하루 1,440건 → 그대로 뿌리면 화면 마비. IP별(+날짜별)로 접어 "N회"로 표시.
  - 🔑 **규칙별 따로 조회**(검수 치명3): 한 덩어리로 긁으면 시끄러운 차단 IP가 상한을 다 먹어 심야·실패 이상이 **통째로 사라진다** = "경보를 많이 울리는 것"이 경보를 죽이는 방법이 됨. 차단 IP는 `groupBy` DB 집계라 **상한 없음**(끌 수 없는 규칙이니 절대 안 놓침). 검증: 차단 3,000건 쏟아부어도 심야·실패 살아남음·3000회 정확·21ms.
  - 🔑 **`badgeWindow()` 하나로 창 통일**(검수 치명1): 배지=시각기준 / 화면=날짜기준이면 매일 "지금 시각"만큼 틈이 생겨 **배지는 켜졌는데 화면에선 못 끄는** 상태가 됨(심야 22~24시가 그 틈에 정확히 들어감). 자정 정렬로 해결 + "하루 N회" 묶음이 창 경계에서 안 잘리는 효과 동반.

### ✅ 2차-3번: 실시간 현황판 — **완료(2026-07-16, 커밋 d6ce713·a694401·832f573)**
사장님 결정(7/16): 원래 구상한 **"세계지도 접속 점 찍기"는 지금 불가·보류**(GeoIP 없음·유료·비용, 접속 IP가 전부 localhost라 검증 불가, nginx XFF 미해결로 IP 신뢰 불가, **국내 제품이라 전원이 서울 한 점에 뭉쳐 값도 낮음**). → **회사 중심 현황판**으로 전환(진짜 데이터·무료·검증 가능).
- `/live` 신설(관리자 전용, 사이드바 "현황판" add-only): **사무실 지도**(StatusMap — 읽기전용 Leaflet, OfficeMap 무수정) + **지금 근무 중**(사무실/재택/외근, 출퇴근 기록 기반 = 대시보드 "현재 근무 중"과 같은 숫자) + **오늘 접속**(사내망/외부, officeIps 재사용). **30초 자동 새로고침**(router.refresh, 토글·수동버튼, 탭 숨김 시 정지). **스키마 변경 없음.**
- 🔑 **정직한 선(가짜 데이터 금지)**: ①직원 개인 위치는 지도에 **안 찍음**(원본 좌표 미저장 원칙) — 화면 명시 ②"지금 접속 중"이라 **안 함**(하트비트 미추적) → "지금 근무 중"(진짜)+"오늘 접속 건수"(집계)로 구분 ③사내망/외부는 officeIps 있을 때만, 없으면 "판정 불가(IP 미설정)".
- code-reviewer 치명1(렌더 중 ref 대입 = ESLint 에러로 배포 빌드 차단 → useEffect로 이동)·중간1(접속 take 잘림 "N+" 표시)·경미2 반영. eslint·tsc exit 0.
- ▶ **다음: 마스터 플로우 복귀** — 얼굴인증 2phase(실제 등록/인식) / 라이브니스 웹캠 실테스트. (사장님 웹캠 실테스트가 오래 대기 중)
- ⚠️ **Prisma 스키마 변경 시**: 사장님 3000 서버가 dev.db·엔진 DLL을 점유 → `migrate`/`generate`가 EPERM으로 막힘. **서버 잠깐 끄고 → generate → 재시작** 필요(1단계에서 실제 겪음).
- 🧭 마스터 플로우 복귀 대기: 2차 화면 3종 끝나면 얼굴인증 2phase/라이브니스 웹캠 실테스트로 복귀.

### 🔴 2차-2번 관련 알려진 사안 (배포·정책 — code-reviewer 지적)
- **✅ 접속기록 보관기간 = 2년 확정·적용(2026-07-16 사장님)**: 사장님이 법 조항을 직접 확인해 결론. 근거 = 「개인정보의 안전성 확보조치 기준」**제8조** — 접속기록 원칙 1년 이상, **고유식별정보·민감정보 처리 시스템은 2년 이상**. 우리 제품은 **얼굴(생체인식정보)=민감정보**를 처리(개인정보 보호법 §23·시행령 §18, CLAUDE.md 가드레일과 일치) → **730일 적용·파기 켬**. 검증: 800일전=파기 / 400일전·오늘=보존 확인. ⚠️ **이 숫자를 줄이면 법 위반** — 변경 전 법무 확인 필수(`lib/access-log.ts` 주석에 근거 박제).
- **🚨 배포 필수 조건 — IP 위조(XFF) 차단**: `lib/ip.ts`의 `getClientIp`는 `x-forwarded-for` 맨 앞 값을 **무조건 신뢰**한다. 5단계에서 **이 헤더 위에 인증 경계(로그인 차단)를 올렸으므로** 이제는 단순 표시 오류가 아니라 **보안 통제 무력화** 사안이다. 공격자가 `X-Forwarded-For: <회사 사내망 IP>` 한 줄을 보내면 **자기잠금 방어 1(사내망 우선 통과)이 그대로 우회 도구**가 되어 차단 명단을 절대 안 탄다. 같은 헤더가 출퇴근 사내망 판정도 좌우함.
  → **반드시 운영 배포 시 리버스 프록시(nginx 등)가 XFF를 덧붙이지 말고 덮어쓰게** 할 것: `proxy_set_header X-Forwarded-For $remote_addr;` (신뢰 프록시 1홉). **이 전제가 없으면 차단 IP 기능은 보안 통제가 아니라 장식**이다. `lib/ip.ts`는 호출처 5곳 공통 함수라 무수정 유지 — 인프라에서 해결.
- **IPv4-mapped IPv6 사내망 오탐 위험(신규 지적)**: 프록시 뒤에서 IP가 `::ffff:192.168.0.5` 형태로 오면 `ipMatches`의 문자열 prefix 비교가 허용IP `192.168.0`과 안 맞아 **사내 PC가 "외부 접속"으로 잡힘**. 지금까지는 출퇴근 위치판정에서 조용히 넘어갔지만, 3단계로 **관리자에게 "외부 접속 N건" 경보로 보이게 승격**됨. `lib/ip.ts`는 공통 함수(호출처 5곳)라 **손대지 않고 보고만** — 운영 배포 시 실제 XFF 형태 실측 후 별도 승인받아 처리.
- **파기 트리거가 관리자 화면 열람 의존**: 아무도 보안 화면을 안 열면 파기가 영영 안 돎. 반대로 한 회사 관리자가 열면 **전 회사 기록이 함께 파기**됨(보관기간은 전사 공통 정책이라 의도는 맞음). 파기를 켤 때 로그인 시점 등 더 자주 도는 지점이나 실제 스케줄러로 옮길지 재검토.
- ~~**파기 성능**~~ **✅ 해결(2026-07-16)**: ① `@@index([createdAt])` 추가(마이그레이션 `access_event_createdat_index` — 인덱스 1줄, 테이블·데이터 무변경) ② 배치 삭제(5,000건씩, 1회 최대 20배치=10만건, 남으면 다음 날 이어서). **검증: 만료 12,000건 + 보존 500건 심고 실행 → 12,000건 전부 파기·500건 무사, SQLite 실행계획이 `SEARCH ... USING INDEX AccessEvent_createdAt_idx` 확인**(풀스캔 아님). ※ "2년 뒤 일"이라며 미룰 뻔했으나 사장님 지적으로 지금 처리 — 지금은 데이터가 없어 인덱스 생성이 즉시 끝나지만 2년 뒤엔 그 작업 자체가 부담이 됨.
- **접속 경로 판정이 소급 재계산됨**: `AccessEvent`에 사내망/외부 판정 결과를 저장하지 않고 **조회 시점의 허용IP로 매번 다시 계산** → 허용IP를 바꾸면 과거 기록 표시도 바뀜(출퇴근은 `locationStatus`를 저장해 이 함정을 피함). 현재는 **화면에 이 사실을 명시**해 대응. 근본 해결은 판정 결과 스냅샷 저장(추후).
- **얼굴 출퇴근 중복 시 화면/로그 불일치(기존 버그, 범위 밖 보고만)**: 이미 출근한 상태로 `faceClockIn` 하면 `clockIn`이 조용히 return하는데 `face.ts`는 "출근 처리되었습니다"를 반환 → 접속로그엔 아무것도 안 남음.

## (이전) 2026-07-13: UI 공통화 대장정 — ✅ 완료(커밋 a5fa8af~cf62ece, code-reviewer 치명0)
사장님과 앱을 직접 테스트하며 도출한 UI 개선을 대거 반영. 화면마다 제각각이던 **기간·시간·검색 UI를 공통 컴포넌트로 표준화**.
- **공통 3종 추출** → `webapp/app/components/`: `RangeCalendar`(커스텀 달력 기간검색, 서버용 래퍼 `RangeCalendarNav`) · `TimePicker`(시/분) · `SearchBox`+`lib/search`(공백/쉼표 OR 필터). **디자인룰 스킬 §5.6에 "직접 만들지 말고 이걸 써라" 규칙 박제**.
- **근태현황**: 일/주/월→달력 기간검색 + 실시간 통합검색(OR) + 오래된순. 상세표 **외출 열에 사유+시각 이력**(횟수 대신).
- **6개 화면 확대**: 내근태(기간달력, 달력토글 유지)·휴가승인·정정승인(기간+검색; 대기=전체·처리내역=**decidedAt 처리시점 기준+92일캡**)·직원관리·생체정보·공지(검색). 목록만 클라이언트 분리, 서버액션·회사격리·권한 유지.
- ⚠️ **로그인 404는 코드 아님** — 오래 켜둔 dev서버 `.next` 캐시 꼬임. `.next` 삭제+재시작으로 해결. 수정 많은 날은 재시작 권장.
- **미반영(요청 밖·보고만)**: 리포트 일/주/월 복붙 중복(공통화 가능) · 정정신청 time입력 TimePicker화(분 단위 확인 후).
- ▶ 다음: (a) 사장님이 관리자 브라우저에서 6개 화면 📅기간·🔍검색 실검증 (b) 리포트 공통화 원하면 착수 (c) 마스터 플로우 **얼굴인증 2phase/라이브니스 웹캠 실테스트** 복귀.
- ⚠️ 계정: 실테스트=뉴가온 `psh01@newgaon.co.kr`(관리자 박성헌)/`test@test.com`(직원, `aaaa1111**`). 더미 `admin@admin.com`(하늘테크)도 테스트차 `aaaa1111**`. **한 브라우저 두 계정 동시로그인 금지(세션 쿠키 덮임)**.

## (이전) 2026-07-12: 회사정보 관리 + 주소검색 — ✅ 6단계 전부 완료(팝업→지도 최종 클릭만 사장님 확인)
- 관리자 **[회사정보]** 메뉴 신설: 회사 상세정보(사업자등록번호·대표자·업태/종목·주소·연락처·담당자·비고) + 첨부문서 5종(업로드/열람/삭제) + 다음 우편번호 팝업(키 불필요).
- **주소→지도 좌표이동**: [설정]·[온보딩] 위치 폼에 "주소로 위치 찾기"(카카오 지오코딩). .env KAKAO_REST_KEY. 카카오 앱에서 카카오맵 서비스 ON 완료. 실좌표 응답 검증(서울시청).
- 보안: 파일은 공개경로 밖 저장·관리자 인증 다운로드·매직바이트 형식검증·(companyId,kind) 유니크·nosniff. code-reviewer 치명0·중간3 수정, E2E(우회공격 차단 포함) 통과. 커밋 eed1c76~(geocode).
- ⚠️ 카카오 REST 키는 스크린샷 노출 → **운영 배포 전 재발급 권장**. 🚧 lib/company-doc.ts detectMime·storageRoot, storage/company-docs/(git 제외), .env KAKAO_REST_KEY(비밀) 건드리지 말 것.
- ▶ 다음: 마스터 플로우상 **얼굴인증 2phase** 또는 사장님 지정 작업으로 복귀.

## (이전) 라이브니스 후속 3건 완료 — 사장님 웹캠 실테스트만 남음
- **후속 3건(7/11 저녁, 커밋 4d28307~203708f) 완료**: ① **판정 기준값 = 관리자 [설정] "본인 확인 재검토 기준"**(기본 50, 30~90%, Company.livenessPercent, 이후 촬영분부터 적용, 조회 실패 시 .env→0.5 폴백) ② **데모(3100) 판정 방식 스위치**(평균=현 근태 방식 / 둘 다 통과 AND=두 모델 최소값, 이력 즉시 재계산) — 사장님 비교 후 근태 반영 여부 별도 결정 ③ **재동의 배너**(7/11 이전 동의자에게 [내 출퇴근]·[인증방식]에 표시 → [동의 화면]에서 재동의만 하면 됨, 철회 불필요·얼굴 등록 유지. E2E 검증 후 홍길동 동의시각은 7/9로 되돌려 둠 — 사장님이 직접 배너→재동의 체험 가능)
- 검수(code-reviewer): 치명0. 중간 지적 2건 수정 반영. **남은 보고 사항**: ⓐ PHOTO_CONSENT_SINCE가 7/11 자정 기준이라 "7/11 00시~문구 배포(17시) 사이 동의자"는 이론상 틈(현재 해당자 없음 — 운영 배포 전 시각 조정 권장) ⓑ D:\사진판독은 다른 세션이 "연속 촬영" 기능을 추가 중(AND 토글은 유지됨) — 저장소 liveness-demo가 뒤처짐, 그 세션에서 역동기화 필요 ⓒ .env LIVENESS_THRESHOLD는 이제 예비값(회사 설정이 우선)

## (이전) 라이브니스 webapp 적용 — 구현 완료·검수 반영
- 사장님 확정(7/11): 출·퇴근 시에만 / 차단 없음(조용한 표시) / 사진 전건 저장·90일 자동 파기 / 동의 화면 항목 추가 — 전부 구현됨
- 구현 요약: 판독 모듈·모델 webapp 내장(데모와 동일 코드·버전) / ClockPhoto 테이블 / AES-256-GCM 암호화 저장(webapp/storage, git 제외) / 출퇴근 후처리는 응답 후(after) 실행 / 관리자 근태 상세 "본인 확인" 열(배지·점수·사진) / 열람 API(관리자만·회사 격리·열람 기록) / 철회 시 사진 즉시 파기
- 검증: 판독 진짜 92.6% vs 위조 2.3%(기준 50%) · 암호화 왕복 · 철회 파기 · 일반 출퇴근 회귀 · 내근태에 판독 미노출 — 전부 통과. code-reviewer 치명2·중간5 → 전부 수정
- 추가(7/11 오후, 사장님 지시): **얼굴 크기 기준(비율) 검사** — 멀리 든 작은 사진 차단. 얼굴 폭 < 화면 폭의 기준%(회사 설정, 기본 30, 10~50)면 출퇴근 인식·등록 거절("다시 촬영"). 가이드 타원 3곳(출퇴근·등록·데모) + [설정]에 관리자 조정 폼 + 데모에 "얼굴 폭 N%" 표시. 검증: 실서버 43.8% 통과/20.3% 차단, 설정 저장 왕복, detect→enroll 연속호출 성공(429 재시도 보험). 4:3 화면 보정(16:9 카메라 좌우 잘림) 반영.
- 추가(7/11 저녁): **데모에도 크기 게이트** — 기준(10~50%) 슬라이더+타원 연동, 기준 미만이면 판독 미진행(주황박스+측정%), 기준 표기는 촬영 당시 적용값. **D:\사진판독 동기화 완료**(데모 2개 파일)
- 사장님 실측(7/11): 폰사진 = 0% 차단 잘 됨 / **오려서 구부린 인쇄 사진 = 78~85%로 "진짜" 오판 사례**(경량 모델의 알려진 한계 — 화면 공격에 강하고 정교한 인쇄물에 약함). 사장님 결정: **운영하며 기준값 조정**. 안전망 = 관리자 사진 열람(오린 사진 든 손이 그대로 찍힘)

## 🔜 이어하기 목록 (다음 세션 — 상세는 plan.md·PROGRESS.md 시작점 블록)
1. ~~판정 기준값 관리자 설정화~~ ✅ 완료(7/11 저녁) — [설정→본인 확인 재검토 기준]
2. ~~"둘 다 통과(AND)" 데모 비교 스위치~~ ✅ 완료(7/11 저녁) — 데모(3100)에서 사장님 비교 → 근태 반영 여부 결정
3. ~~재동의 유도 배너~~ ✅ 완료(7/11 저녁) — 배너 클릭→[동의 화면] 재동의만 하면 됨(철회 불필요)
4. **[사장님 테스트]** ⓐ 출퇴근 화면 배너로 재동의 → 얼굴 출퇴근 → 관리자 근태상세 배지·사진 열람 확인 ⓑ 데모에서 평균 vs AND를 진짜 얼굴(밝게/어둡게/역광)+오린 사진으로 비교 ⓒ 얼굴 크기(30 vs 35)·재검토 기준(50 vs 80) 값 확정은 [설정]에서 직접 입력 ⓓ 확인 후 시드 데이터(박성헌 7/10 기록+사진 2건) 삭제 지시
5. **[개발 후보]** AND 방식 근태 반영(데모 비교 합격 시) / 운영 배포 전 PHOTO_CONSENT_SINCE 시각 조정
6. **[선택]** 판독 모델 훈련용 원본 소스(Apache-2.0) 백업 보관
- ⚠️ 검증용 시드 데이터 있음: 박성헌 7/10 09-18시 기록+사진 2건(출근 93%/퇴근 2% 재검토 배지 예시) — 사장님 확인 후 "지워줘" 하면 삭제
- .env 추가: LIVENESS_THRESHOLD(0.5), CLOCK_PHOTO_KEY(암호화 키), (선택) CLOCK_PHOTO_DIR

## 확정된 설계 (2026-07-10 사장님)
- 출퇴근 = **조용한 표시**(직원 모름, 관리자에 "본인 확인 재검토 필요" + 점수·시각·의심건 사진) / 등록 = 부드러운 차단
- 의심건 사진: 암호화·관리자만 열람(기록)·90일 자동 파기 — "얼굴원본 미저장" 원칙의 유일한 예외, 동의서 명시 필요
- 아키텍처 = 웹앱 내장(B안, 별도 서버·DB 없음), 엔진 = MiniFASNet V1SE+V2 (Apache-2.0)

## 대기/보류 중
- **2조각(얼굴 출퇴근) 사장님 웹캠 실테스트** 아직 안 됨 — 얼굴 등록부터 다시 필요(DB상 등록 0)
- **3조각(실근무 확인)**: 계획 완료 상태로 보류(커밋 fa940b1) — 사장님 지시로 라이브니스 우선
- 결제(아이원/KICC): 정소장 확인 대기

## ⚠️ 알려진 이슈
- 라이브니스 기준값 미확정(데모 기본 50%) — 사장님 실측으로 확정 예정
- 개발 얼굴서버가 조잡한 합성 이미지도 얼굴로 등록한 사례 → 운영 전환 시 검출 기준 확인 항목
- 테스트 직원(박성헌) 근무요일 토·일(테스트 흔적)

## 📌 백로그
- ~~생체정보 "동의 개시" 미기록~~ **✅ 완료(5단계)** — `biometric_consent`/`biometric_reconsent`로 첫 동의·재동의를 구분해 기록. 이제 재동의해도 과거 이력이 안 사라짐.
- **`data_view` kind는 정의만 있고 미사용** — 쓰려면 `lib/audit.ts`의 `AuditKind` + `security/access/page.tsx`의 `ACCESS_KINDS` + `export/route.ts`의 `KIND_GROUPS`에 **함께** 넣을 것. ⚠️ 5단계에서 `blocked`를 이 규칙대로 안 해서 **화면에 안 보이는 사고를 실제로 냄** — 검수에서 잡힘. 다음엔 반드시 3곳 동시.
- **차단 후보 집계가 메모리 방식**(최근 5000건 take 후 집계) — 정확도가 더 필요하면 `groupBy(['ip'])` + `_count`로 DB 집계 전환.
- **`BlockedIp`에 `@@unique([companyId, pattern])` 없음** — 더블클릭 시 중복 행 가능(해는 없고 목록만 지저분).
- **차단된 IP의 반복 요청이 매번 `recordAccess` INSERT 유발** — 동일 IP·동일 분(minute) 중복 억제 검토.
- **엑셀 수식 인젝션(기존 사안)** — 직원 이름이 `=HYPERLINK(...)`면 엑셀 셀에 들어감. 이름 열에 이미 있던 문제라 범위 밖 보고만.
- **직원관리 감사로그**(퇴사·비번재설정 등) — 4단계 범위에서 제외했으나 같은 방식으로 확장 가능.
- **세계지도형 접속 지도** — 배포 후 재검토(사장님 보류). 전제: ①nginx XFF로 진짜 IP 확보 ②GeoIP(유료 API 또는 DB 내장) ③국내 제품이라 값 낮음 재평가.
- **Attendance 인덱스 없음**(검수 중간 지적) — `model Attendance`에 `@@index` 0개. 대시보드·현황판이 `where:{companyId,clockIn}`로 풀스캔. 현황판은 30초마다 반복이라 누적 부하가 큼. 데이터 커지면 `@@index([companyId, clockIn])` 검토(스키마 변경=마이그레이션=서버 끄고).
- **이상접속 6단계 잔여(전부 검수에서 "구조상 한계"로 정리·화면에 명시함)**:
  - **자정 분할 회피** — 실패 묶음이 "자정 기준 하루"라 23:50에 4회 + 00:05에 4회면 각각 4회로 쪼개져 기준(5회)을 안 넘는다. 정석은 **슬라이딩 24시간**. 지금은 5회 실패 계정 잠금이 함께 막고 화면에 이 사실을 밝혀 둠.
  - **심야 로그인 상한 5,000건** — 성공 로그인만 대상이라 공격자가 부풀릴 수 없고 직원 수에 비례(190명×7일≈2,660)해 여유 있으나, 초과 시 `capped` 경고로 알림. 필요해지면 DB에서 시각 필터(`strftime`) 검토 — 시간대 변환 함정 주의.
  - **`ALERT_BADGE_DAYS=7`보다 오래된 미확인 이상은 배지에 안 잡힘** — 화면에서 기간을 넓혀야 보인다.
  - **자동 테스트 없음**(검수 지적) — `isNightHour`·`badgeWindow`·`dayKey`는 순수 함수라 단위 테스트가 쉽고, 이번 치명1(창 어긋남) 같은 버그를 정확히 잡아준다. 도입 권장.

## 🔒 개인정보/생체정보 관련 (2026-07-16~17 사장님 실테스트 중 조치·논의)
- **✅ 정상 사진 관리자 열람 차단(커밋 8bed79e·b86ac9f)**: 얼굴 사진=민감정보. 관리자가 정상 직원 얼굴까지 자유 열람 = 근로자 감시 우려 → **위조 의심(suspect)·판독 실패(error)만 열람 허용, 정상(ok)은 차단**. 화면 링크 숨김 + **서버 API 403**(URL 직접 접근도 차단·화이트리스트 fail-safe). 판정 배지("정상 98%")는 유지(수치라 감시 우려 없음). 직원 본인 화면(my-records)은 원래 사진 안 보임. code-reviewer 치명0.
- **⏳ 개인정보 ① 전건 저장·90일 보관 = 정책 결정(법무 검토 대기)**: 최소수집 원칙상 "위조 의심분만 보관" 또는 보관기간 단축이 더 안전하나, 사장님이 전건·90일로 정한 사안 → 임의 변경 금지, 개인정보 전문가·노무사 검토로 확정. 동의 문안도 초안(전문가 검토 필요).
- **✅ 얼굴 촬영 "3장 동일" 오탐 수정(A안, 커밋 35dd068·10c886d)**: 카메라 예열 전 같은 프레임 3장→서버 "정지영상" 오탐. `FaceClockPanel.tsx` 촬영부만 수정 — 예열 후 `requestVideoFrameCallback`로 새 프레임마다 3장 + 바이트 동일 안전망(유한 재캡처). rVFC 미지원(Firefox·구형)은 setTimeout 폴백=회귀 0. **서버 부정방지 장치 무수정**(정말 멈춘 카메라는 그대로 걸러냄). 촬영 중 [취소]·[일반방식] 잠금(취소 레이스 차단). code-reviewer 치명0·중간2 반영. tsc·eslint 0, 동일감지 로직 4/4. ⚠️ **진짜 오탐 해소는 사장님 웹캠 최종확인 필요**(이 환경 실카메라 없음).

## 🚧 건드리면 안 되는 부분
- **webapp/app/settings/OfficeMap.tsx**: 설정 저장에 쓰는 **대화형** 지도(드래그로 좌표 지정). 현황판은 이걸 안 건드리려고 읽기전용 `app/live/StatusMap.tsx`를 **별도로** 만듦 — 둘을 합치거나 OfficeMap을 읽기전용으로 바꾸지 말 것.
- **webapp/lib/anomaly.ts**: ① `badgeWindow()` — 배지·알림화면이 **둘 다** 이걸 써야 함(각자 계산하면 창이 어긋나 배지를 못 끄는 사고). ② **규칙별 따로 조회** 구조 — 한 덩어리 조회로 되돌리면 시끄러운 IP가 다른 경보를 굶긴다. ③ 차단 IP는 `groupBy`(상한 없음) 유지
- **webapp/lib/audit.ts `logAdminAction`**: 원칙 4가지(무차단·값 미기록·성공지점·사실대로) 주석에 박제 — 어기면 감사로그가 거짓말을 한다. 종류(AuditKind) 추가 시 **화면 ACCESS_KINDS·KIND_GROUPS도 함께** 고칠 것
- **webapp/lib/access-log.ts `ACCESS_RETENTION_DAYS=730`**: 법정 하한(안전성 확보조치 기준 §8 — 민감정보 시스템 2년). **줄이면 법 위반** — 변경 전 법무 확인 필수
- **webapp/lib/ip.ts (`getClientIp`·`ipMatches`)**: 공통 함수(호출처 5곳 — 출퇴근 사내망 판정 포함). 오탐 지적 있으나 **별도 승인 없이 수정 금지**
- **webapp/app/actions/attendance.ts clockIn/clockOut 본체**: 무수정 유지. 접속기록은 **맨 끝 add-only 1줄**만 허용(사장님 승인 옵션 A, 2026-07-16)
- **webapp/app/actions/authmethod.ts 파기 3함수**: 순서 규칙 = ①`wasEnrolled`/`had`를 update **전에** 캡처 ②`unenrollFaceSafely` **먼저**(GaonFR 삭제) ③성공(`unenrolled`)했을 때만 `faceEnrolledAt/Count` 해제 ④감사로그는 `redirect()` **앞**에서 `purged && unenrolled`로. 순서를 뒤집으면 "DB는 파기인데 서버엔 얼굴이 남는" 최악 잔존이 생기거나 감사가 거짓말을 한다. lib/face.ts의 unenrollFace는 **호출만**(무수정).
- **webapp/lib/liveness.ts**(+demo 원본)의 전처리 상수(배율 4.0/2.7·80×80·BGR·보간 linear): 원 모델 재현 — 변경 금지(점수 왜곡)
- webapp actions/attendance.ts clockIn/clockOut 본체: 이번 이식에서도 무수정 유지 — 계속 수정 금지
- lib/face.ts: recognizeFace에 faceRect·imageSize "추가만" 됨(7/11) — 기존 필드·판정 로직 수정 금지
- .env(FACE_*, CLOCK_PHOTO_KEY): 비밀값 — git 제외 유지. **CLOCK_PHOTO_KEY 분실 시 보관 사진 복호 불가**(백업 필요)
- webapp/storage/clock-photos/: 암호화 생체정보 — git 제외, 임의 삭제 금지(90일 자동 파기가 처리)

## 영향 범위 지도
- liveness-demo ↔ webapp: **현재 완전 분리**(공유는 가온 계정뿐). 이식 시 lib/liveness.ts+models/ 가 webapp으로 복사됨
- D:\사진판독 = liveness-demo의 복사본(독립) — 근태 저장소 밖, 여기 수정해도 근태와 무관
