# Research: 3조각 — 실근무 확인(옵션 B) + 관리자 선택 + 직원 참여 선택 (2026-07-10)

## 요구사항 (사장님 확정)
1. **관리자**가 회사 설정에서 "실근무 확인" 기능을 켜고 끌 수 있다.
2. **직원**은 ① 출퇴근 인증방식(얼굴 or GPS 택1 — 현행 유지) ② **실근무 확인 참여 여부**를 선택한다. 거부하면 출퇴근 기록만으로 계산(불이익 없음).
3. 참여 직원은 근무 중 주기적으로 웹캠 얼굴 확인 → 자리에 있는 시간을 관리자가 확인.

## 기획 명세(face-spec.md §4-3, 이미 확정된 경계) — 반드시 지킬 것
- **detect API만 사용** (recognize 아님) = 누구인지 식별 안 함, **얼굴 개수만**
- **이미지 미저장** — 시각 + 있음/없음만 DB 기록
- **사전 고지 배너** 필수 ("몇 분마다 자리확인용 카메라가 짧게 작동합니다")
- 얼굴인증 미선택 직원에겐 절대 동작 안 함 + 주기 샘플링만(상시 촬영 금지)
- 실근무 **산식 반영은 추후 확정** 사항(§10) → MVP는 기존 실근무시간 계산을 건드리지 않고 **별도 지표로 표시**

## 관련 파일과 역할
| 파일 | 역할 | 변경 |
|---|---|---|
| webapp/prisma/schema.prisma | DB 구조 | Company·User 필드 + PresenceSample 모델 추가 |
| webapp/lib/face.ts | 얼굴서버 연동 | detectFace 추가(추가만, 기존 무수정) |
| webapp/app/settings/* | 관리자 설정 | 실근무 확인 켜기/끄기 + 주기 카드 추가 |
| webapp/app/auth-method/page.tsx | 직원 인증방식 | "실근무 확인 참여" 선택 카드 추가 |
| webapp/app/attendance/page.tsx | 출퇴근 화면 | 근무 중 참여자에게 고지 배너 + 자리확인 컴포넌트 |
| webapp/app/actions/* | 서버 액션 | 참여/철회, 샘플 기록 액션 |
| webapp/app/records/[userId]·reports | 관리자 조회 | 자리확인 지표 표시 |

## 🔴 영향 범위
- `workedMinutes`(lib/worktime) — **수정하지 않음**. 실근무 숫자를 쓰는 리포트·정정·대시보드 전부 영향 없음.
- Company/User 스키마 — 필드 **추가만**(기존 필드 무변경) → 마이그레이션 1회 + **dev 서버 재시작 필요**(과거 2회 겪은 함정).
- lib/face.ts — 추가만. 등록/인식 기능 무영향.

## 기술 제약 (솔직한 한계 — 계획에 반영)
- **브라우저 탭이 열려 있어야 웹캠 확인 가능.** 출퇴근 화면을 닫아두면 확인이 안 됨 → 미확인 구간은 "자리비움"이 아니라 **"확인 안 됨"으로 구분**해야 억울한 판정이 없음 (미확인=결측, 없음=자리비움).
- 백그라운드 탭은 브라우저가 타이머를 늦출 수 있음 → 주기 지연 허용(±). 화면 꺼짐/절전은 확인 불가로 기록됨.
- 이 한계는 PC 사무직(모니터에 탭 상주) 전제로는 수용 가능. 파일럿에서 실측.

## 결론
- 스키마: `Company.presenceCheckEnabled(기본 꺼짐)` + `presenceIntervalMin(기본 10분)` / `User.presenceOptInAt(참여 동의 시각, null=미참여)` / `PresenceSample(attendanceId, at, present)` 
- 판정·표시: 근무 구간 내 샘플을 "있음/없음"으로 기록, 관리자 화면엔 **확인율·자리비움 구간**을 보조 지표로 표시. 기존 실근무 숫자는 유지.
