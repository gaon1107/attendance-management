# Research: 공휴일·회사휴무일 반영 (2026-07-17)

## 요청 요약
평일에 오는 **법정공휴일**과 **회사 지정 휴무일**에는 지각 판정을 하지 않고 "휴일근무"로 처리한다.
방식 C = 자동 공휴일(A) + 회사 수동 휴무일(B) 둘 다.

## 현재 구조 (핵심)
- "이 날이 근무일인가"는 오직 **요일**로만 판단: `lib/workdays.ts`의 `isWorkDay(date, workDays)` = `workDays.has(date.getDay())`.
- `workDays`는 회사 기본(Company.workDays, 기본 "1,2,3,4,5") + 직원 예외(User.workDays). **공휴일 개념·데이터가 코드에 전혀 없음.**
- "휴일근무" = 파생값 `holiday = !onWorkDay` (별도 저장 아님).

## 🔴 영향 범위 (isWorkDay / onWorkDay를 쓰는 모든 곳)
`isWorkDay`는 **공통 모듈**. 아래 4개 기능이 전부 이 판정에 의존한다:

1. **지각 판정** — `lib/dayentries.ts:55`, `app/records/page.tsx:77`, `app/dashboard/page.tsx:53` → 근무일 아니면 지각 안 함(요청의 핵심 대상).
2. **결근 판정** — `lib/dayentries.ts:75`, `app/reports/page.tsx:113` → 과거 근무일 무기록 = 결근. **공휴일을 근무일로 보면 그날 안 나온 사람이 "결근"으로 잘못 뜬다.** → 지각과 한 몸. 같이 고쳐야 일관됨.
3. **휴일근무 표시** — `dashboard/page.tsx:200`, `records/page.tsx:88`, `DetailTable.tsx:161`, `MonthCalendar.tsx:61`, `RecordsClient.tsx:142` → 근무일 아닌 날 출근 = 보라색 "휴일근무" 배지.
4. **연차 사용일수 계산** — `lib/leave.ts:35 countWorkdaysBetween` / `:47 computeLeaveDays`, `app/actions/leave.ts:34` → 휴가 신청 기간 중 **근무일 수만 연차에서 차감**. 현재는 평일 공휴일도 근무일로 세어 **연차가 더 많이 깎인다**(예: 광복절 낀 3일 연차 → 3일 차감. 공휴일 반영 시 2일이 맞음).

## 공통 모듈 여부 / 건드리면 안 되는 인터페이스
- `isWorkDay(date, Set<number>)`의 **시그니처(요일만 보는 순수함수)는 유지**한다 — 6곳이 호출. 시그니처를 바꾸면 전부 깨진다.
- 대신 "휴일 여부"를 **합성**하는 새 판정을 추가하는 방향이 안전: `근무일_실질 = isWorkDay(요일) && !isHoliday(날짜)`.
- `leaveLabelByDate`, `leaveDateSet`(휴가 날짜 펼치기)는 그대로 둔다.

## DB·마이그레이션
- DB = **SQLite**. 수동 휴무일(B) 저장 = **새 표 `CompanyHoliday` 추가 필요** → Prisma 마이그레이션.
- ⚠️ 마이그레이션 시 **사장님이 3000 서버를 끄고** 진행해야 함(EPERM, project-status.md 규칙).
- 자동 공휴일(A)은 저장이 아니라 "코드가 아는 날짜 목록"이라 표가 필요 없음(단, 데이터 출처 결정 필요 — 아래).

## 위험 요소
- **동시성/성능**: 조회 화면마다 회사 휴무일을 1회 읽어 Set으로 만들면 됨(N+1 없음). 자동 공휴일은 메모리 계산이라 부하 없음.
- **오탐 반대 케이스**: 병원·서비스업은 공휴일에도 정상근무. 자동 공휴일을 무조건 켜면 그들에겐 "정상근무가 휴일근무로" 뜬다 → **회사가 자동공휴일을 켜고 끌 수 있어야** 함(토글).
- **음력·대체·임시공휴일**: 설날·추석은 매년 날짜가 바뀌고 대체공휴일·임시공휴일(선거일 등)도 있음. 자동 데이터 출처가 이걸 어떻게 감당하느냐가 관건(아래 결정 A).
- **범위 경계**: 원 요청은 "지각"이지만 결근이 한 몸이라 같이 가야 함. 연차 차감은 별개 판단(사장님 결정 필요).

## 결론 (계획 전 사장님 결정 필요)
1. **자동 공휴일 데이터 출처** (A1 외부API vs A2 코드 내장표)
2. **적용 범위** (지각+결근만 vs 연차 차감까지)
3. **자동공휴일 ON/OFF 토글** 제공 여부(공휴일 정상근무 업종 대비)
</content>
