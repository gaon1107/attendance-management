# Research: 2차-3번 "실시간 접속 지도" (2026-07-16)

## 0. 이 기능의 정의가 확정 안 됨
WORKFLOW.md·PROGRESS.md 어디에도 상세 정의가 없다. 사장님과 구두로 묶은 "2차 화면 3종"의
마지막 항목("실시간 접속 지도")일 뿐. → **무엇을 만들지부터 정해야 한다.**

## 1. "지도"라는 말이 뜻할 수 있는 것 2가지
- (A) **세계지도에 접속 위치를 점으로** — "지금 서울/부산/해외에서 누가 접속 중"
- (B) **회사 위치 중심 지도** — 이미 있는 사무실 지도(OfficeMap) + 오늘 접속을 사내망/외부로 요약

## 2. 🔴 (A)를 지금 만들 수 없는 결정적 이유 4가지

### ① IP를 좌표로 바꾸는 "GeoIP"가 없다
IP(1.2.3.4)를 지도 위 점(위도·경도)으로 바꾸려면 GeoIP 데이터가 필요하다.
- `package.json` 확인: **geoip/maxmind/ipinfo 라이브러리 전혀 없음.**
- 방법은 둘뿐: ⓐ 유료 API(MaxMind·ipinfo 등, 월 구독·외부 전송) ⓑ 수백 MB DB 파일 내장(라이선스·갱신 부담).
- 둘 다 **외부 데이터/비용** — CLAUDE.md "외부 데이터는 승인 후" 대상.

### ② 지금 데이터로는 지도에 찍을 게 없다
실제 쌓인 접속 IP를 조회한 결과: **전부 `::1`·`::ffff:127.0.0.1`(내 컴퓨터/localhost).**
localhost는 지구상 위치가 없다. → **지금 만들면 텅 빈 지도이거나 가짜 위치**(CLAUDE.md 가짜 데이터 금지 위반).

### ③ IP를 아직 믿을 수 없다 (배포 필수 조건 미해결)
5·6단계에서 못 박은 🚨 배포 필수 조건: nginx가 XFF를 덮어써야 진짜 IP가 보인다.
그 전엔 모든 직원이 프록시 IP 하나로 뭉쳐 보인다 → **지도에 점 하나만 찍힘.**

### ④ 이 제품엔 애초에 값이 낮다
타겟 = **국내 중소기업 사무직.** 직원은 사무실 아니면 집(둘 다 한국)에서 접속한다.
세계지도를 그려도 **전원이 서울 한 점에 뭉친다.** GeoIP 국내 정확도도 "서울시" 수준(구·동 불가).
→ 비용(유료 API)·복잡도 대비 실질 보안 가치가 거의 없다.

## 3. ✅ 지금 우리가 **진짜로 가진** 위치 신호
- **사무실 좌표**(officeLat/Lng) — 관리자가 지도에서 찍은 **진짜 좌표 1개.** (OfficeMap이 이미 표시 중)
- **사내망/외부 판정** — 모든 접속에 대해 officeIps로 이미 계산 중(3·4·6단계). GeoIP 불필요·신뢰 가능.
- **근무형태**(office/home/field) — 출퇴근 기록에 저장됨.
- **GPS 원본좌표는 미저장**(프라이버시 원칙) — 직원 실제 위치는 원래 안 갖고 있다.

## 4. 이미 있어 재활용 가능한 것
- `OfficeMap.tsx` — Leaflet + OpenStreetMap(무료·키 불필요). 회사 위치·반경원 표시. `leaflet@1.9.4` 설치됨.
- `lib/ip.ts ipMatches` + `Company.officeIps` — 사내망/외부 판정(🚧 무수정 재사용).
- 대시보드 "실시간 근무 인원" 카드 — 이미 실시간 집계 패턴 존재.
- `AccessEvent` — 접속 시각·IP·사내망여부 조회 가능.

## 5. 결론 — 사장님 결정이 필요한 지점 (중요 결정 게이트)
(A) 세계지도형은 **지금 만들 수 없고**(GeoIP·비용·신뢰 안 되는 IP), **국내 제품엔 값도 낮다.**
그대로 밀면 "검증 불가능한 가짜 지도"가 되어 개발 규율(검증 없이 완료 없음)에 정면 충돌한다.

→ 세 갈래로 사장님께 확인:
  · (B) **회사 중심 "오늘 접속 현황" 화면**으로 만든다(진짜 데이터·검증 가능·무료). — 실속형
  · (A) 세계지도는 **배포 후**로 미루고, 지금은 2차-3번을 건너뛰어 마스터 플로우(얼굴 2phase)로 복귀.
  · (A)를 유료 GeoIP까지 감수하고 지금 강행(비추천 — 검증 불가).
