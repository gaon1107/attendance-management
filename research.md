# Research: 6단계 — 이상접속 알림 (2026-07-16) — 2차-2번 마지막

## 1. 목표
"수상한 접속이 있었는지"를 관리자가 **찾아다니지 않아도** 알게 한다.
지금은 관리자가 [보안로그]를 직접 열어 2,000줄을 눈으로 훑어야 이상을 발견한다.

## 2. 이미 있는 것 (새로 만들 필요 없음)
- `AccessEvent` — 로그인/실패/로그아웃/출퇴근/설정/파기/차단 전부 기록 중(1~5단계).
  - 색인: `@@index([companyId, createdAt])` · `@@index([kind])` · `@@index([createdAt])` → **기간 조회 빠름**
  - `device` 컬럼 = `deviceLabel()` 파생값("Windows PC"·"iPhone" 등 **거친 분류**)
- `lib/ip.ts ipMatches` + `Company.officeIps` → 사내망/외부 판정(🚧 무수정 재사용)
- 대시보드 "오늘 알림" 카드 = 미출근·지각·대기휴가·공지를 **한 줄씩 Link로** 나열하는 기존 패턴 → **여기에 한 줄 추가가 가장 자연스러움**
- `SecurityTabs` = 탭 배열에 1줄 추가하면 4번째 탭 생성(사이드바 무증설)
- `lib/audit.ts logAdminAction` = 설정 변경 감사 기록(config)

→ **판정에 필요한 데이터는 전부 이미 쌓여 있다. 6단계는 "읽고 판정해서 보여주기"가 본체.**

## 3. 🔴 조사에서 나온 중요한 사실

### ① "새 IP 알림"은 오탐 폭탄 — 쓰면 안 됨
재택·모바일은 IP가 수시로 바뀐다(통신사 동적 할당). 새 IP를 알리면 **매일 전 직원이 알림**에 뜬다.
→ 대신 **`device`(기기 종류)** 기준. "박성헌이 처음으로 iPhone에서 로그인" 은 드물고 의미 있는 신호.
→ ⚠️ **정직한 한계**: `deviceLabel`은 "Windows PC" 수준으로 거칠다. 공격자가 Windows를 쓰면 안 걸린다. 만능이 아니라 **보조 신호**다.

### ② 도입 첫날엔 전원이 "처음 보는 기기" — 필연
과거 기록이 없으니 모두 새 기기다. 이걸 안 다루면 첫 화면이 경고로 뒤덮인다.
→ 해결: **그 직원의 과거 성공 로그인이 1건도 없으면 판정 제외**(첫 로그인은 당연히 처음) + **[확인함] 버튼**으로 한 번에 정리.

### ③ "심야"는 회사마다 다르다 — 24시간 교대 회사는 심야가 정상
고정값(22~06)으로 박으면 교대 근무 회사에서 **알림이 100% 오탐**이 된다.
→ 시간대를 회사 설정으로 두고, **끌 수 있어야** 한다.

### ④ "읽음 처리"에는 상태 저장이 필요 — 스키마 변경 불가피
판정 자체는 조회 시점 계산이라 저장이 필요 없지만, "관리자가 확인했다"는 **기억해야** 한다.
안 그러면 대시보드 배지가 영원히 안 꺼진다.
→ 최소 비용: `Company.securityCheckedAt` **1개 컬럼**. "이 시각 이후 이상건만 배지에 센다."
→ ⚠️ **스키마 변경 = 마이그레이션 = 사장님 3000 서버 EPERM** → 서버 끄고 진행(1단계에서 실제 겪음).

### ⑤ 판정이 소급 재계산됨(3단계와 동일한 함정 — 이번엔 오히려 정상)
설정(심야 시간대 등)을 바꾸면 과거 기록의 이상 판정도 바뀐다. 3단계에선 함정이었지만,
**알림은 "지금 규칙으로 다시 본다"가 자연스러움** → 그대로 두되 화면에 명시.

### ⑥ 이메일·문자 발송은 지금 불가능 — 그리고 "죽은 스위치"는 만들면 안 됨
메일 발송에는 외부 서비스(SMTP/SES 등) + 도메인 + 인증이 필요 → **배포 후에만 가능**.
스위치만 만들고 아무 일도 안 하면 **CLAUDE.md "가짜 데이터 금지" 위반**이자 관리자를 속이는 UI.
→ **스위치를 아예 안 만들고**, 화면에 "이메일·문자 알림은 서버 배포 후 지원 예정" 안내 한 줄만. ← 사장님 확인 필요

## 4. 영향 범위 분석 (수정 전 검색 결과)

| 파일 | 수정 방식 | 위험 |
|---|---|---|
| `prisma/schema.prisma` `Company` | **컬럼 추가만**(전부 기본값 있음) | 기존 데이터 무손실. 단 **EPERM** → 서버 끄고 |
| `app/dashboard/page.tsx` | 알림 카드에 **줄 1개 add-only** | 기존 KPI·표 로직 무수정. 쿼리 1회 추가 |
| `app/security/SecurityTabs.tsx` | TABS 배열 **1줄 추가** + 타입에 `"alerts"` | 기존 3탭 영향 0 |
| `app/settings/*` | 폼 섹션 **1개 추가** | 기존 5개 폼 무수정 |
| `app/actions/settings.ts` | 함수 **1개 추가**(`saveAlertRules`) | 기존 5함수 무수정 |
| `lib/access-labels.ts` | 라벨 **1줄 추가**(`alert_rules`) | add-only |

### 변경 없음(🚧)
`lib/ip.ts` · `lib/access-log.ts` · `lib/audit.ts` · `lib/device.ts` · `actions/attendance.ts` · `actions/auth.ts` · `lib/ip-block.ts` · `Sidebar.tsx`

→ **읽기 전용 기능이다.** 기존 판정·기록 로직을 한 줄도 안 건드린다. 새 파일(`lib/anomaly.ts`)에 판정을 격리한다(4단계 `audit.ts`와 같은 위험 격리 방식).

## 5. 성능
- 이상 판정 = 기간 내 `AccessEvent` 조회 1회(색인 탐), 나머지는 메모리 계산.
- "처음 보는 기기" 기준선 = 최근 90일 성공 로그인의 `(userId, device)` 목록 → `distinct` 조회 1회.
- 대시보드 배지 = `count` 1회(가벼움). ⚠️ 대시보드는 **가장 자주 열리는 화면** — 무거우면 안 됨.

## 6. 범위 밖 (하지 않음 + 이유)
- **이메일·문자 실발송** — 외부 서비스·도메인 필요(배포 후)
- **해외/국가(GeoIP) 접속 알림** — 외부 데이터 필요. XFF 신뢰 문제(🚨 배포 필수 조건) 해결 전엔 무의미
- **자동 차단 연동** — 5단계 사장님 결정("보여주기만") 유지
- **실시간 푸시/웹소켓** — 2차-3번(실시간 접속 지도) 영역
