# Research: 얼굴 출퇴근 "3장 동일" 오탐 수정 (촬영 방식 개선) — 2026-07-16

## 0. 증상·원인 (확정)
사장님 실테스트: 진짜 얼굴인데 출근이 "위조 의심 93%"로 표시됨(퇴근은 정상 98%).
- 화면 % = "진짜일 확률"(높을수록 진짜). 93%면 두 판독 AI 모두 통과 = **얼굴은 진짜로 판정됨.**
- "위조 의심"은 별개 장치 = **"연속 3장이 바이트 단위로 완전히 동일 → 정지영상/가상카메라 의심"**(actions/face.ts:232-244 `buffers[i].equals(buffers[j])`).
- 수학적 확정: 표시점수는 3장 평균의 최솟값(또는 동일감지 시 첫 장 평균). 93%인데 모델 임계로 걸리는 건 불가능(93%면 두 모델 다 통과) → **무조건 "동일 3장" 장치.**
- 실데이터: 오늘 4장 중 카메라 켠 **직후 2건(21:02:01·21:02:17)=위조 93%**, 몇 초 뒤 예열된 **21:02:23=정상 98%**. 카메라 예열 전 같은 프레임이 3번 찍힌 패턴.

## 1. 근본 원인 (촬영부)
`FaceClockPanel.tsx:141-159` 촬영 루프:
```js
for (let i = 0; i < 3; i++) {
  if (i > 0) await new Promise((r) => setTimeout(r, 300)); // 0.3초 고정 간격
  const b = await captureOneBlob(video, canvas); // 현재 video 프레임 → JPEG
}
```
문제 2가지:
- **예열 보장 없음**: `openCamera()` 직후 `v.play()`만 하고, 실제 프레임이 흐르기 전에 촬영 가능. 카메라가 아직 첫 프레임만 물고 있으면 3장이 그 프레임으로 동일.
- **시간 기반(0.3초) 캡처**: 디코딩된 "새 프레임"을 기다리는 게 아니라 벽시계 0.3초 후 현재 프레임을 뜬다. 느린 카메라·예열 중이면 같은 프레임이 재캡처 → `canvas.toBlob`은 같은 픽셀 → **같은 바이트** → 서버 동일감지에 걸림.
→ 진짜 사람인데 오탐. (반대로 예열 후엔 프레임이 미세하게 달라 바이트가 달라짐 = 정상 통과.)

## 2. 영향 범위 (수정 전 전수 확인)
- **`FaceClockPanel.tsx`** — 3장 촬영은 **여기 한 곳뿐**(grep 확인). 출근/퇴근 공용(`action` prop). 사용처 = `attendance/page.tsx` 1곳.
- **`FaceCapture.tsx`(얼굴 등록)** — **1장 촬영**이라 이 문제와 무관 = **범위 밖**(안 건드림).
- **`FaceGuide.tsx`** — 촬영 안내 오버레이(표시만). 무관.
- **서버 `actions/face.ts`·`lib/liveness.ts`의 동일감지·모델 판정** — 🚧 **안 건드림**(부정 방지 장치는 그대로). 촬영이 진짜 다른 3장을 보내게만 고친다.
- `requestVideoFrameCallback` 기존 사용 0건(신규 도입).

## 3. 건드리면 안 되는 부분
- 서버 판정 로직(동일감지·모델A 0.60·모델B 회사기준·밝기) — 그대로.
- 1280×720 요청·품질 0.9·850KB 재압축(라이브니스 기준값 측정 조건) — 유지.
- submit 이중 잠금(submittingRef)·fallback(일반 방식)·카메라 정리(stopCamera)·429 재시도 — 유지.

## 4. 위험 요소
- **`requestVideoFrameCallback` 브라우저 지원**: Chrome/Edge/Safari O, **Firefox·구형 X** → 없으면 **기존 setTimeout 방식으로 폴백**(현행 동작 보존, 회귀 0).
- **예열 대기가 UX 지연**: 짧게(수백 ms) + "촬영 중" 피드백. 무한 대기 금지(타임아웃).
- **정말 정지된 카메라(가상카메라 공격)**: 재캡처해도 계속 동일 → 그건 **그대로 서버가 걸러야 정상**(안전장치 유지). 클라 재시도는 유한(bounded).

## 5. 결론 (계획 방향)
촬영을 ①**예열 후** ②**실제 새 프레임에 맞춰** 3장 뜨고 ③그래도 동일하면 **유한 재캡처**로 서로 다른 3장을 보장.
서버 안전장치는 무수정. rVFC 미지원 브라우저는 기존 방식 폴백. 범위 = `FaceClockPanel.tsx` 촬영부 한정.
