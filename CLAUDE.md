# 근태관리 SaaS 프로젝트 운영 규칙 (총괄 PM)

> 이 문서는 프로젝트의 "사규"다. Claude는 매 작업 시 이 규칙을 따른다.
> ※ 전역 규칙(~/.claude/CLAUDE.md)과 bkit PDCA가 우선하며, 이 문서는 본 프로젝트에 특화된 보완이다.

---

## 1. 프로젝트 정체성

- **제품**: 중소기업 사무직 대상 웹 근태관리 SaaS (구독)
- **1차 개발 범위(MVP)**: ① 웹 ② 얼굴인증 출퇴근(선택형) ③ 얼굴검출+인식 혼용 실근무시간 산출
- **전체 로드맵**: 위 3개 + 메신저 / 파일공유 / 시장조사 추가기능 (2차 이후)
- **핵심 차별점(가설)**: "정직한 실근무시간" + "법규 자동 준수" + "강제하지 않는 얼굴인증"
- 상세 근거: [01_시장경쟁사_조사보고서.md](01_시장경쟁사_조사보고서.md)

---

## 2. 조직(에이전트) 구성 — 하이브리드

대부분 bkit 기본 에이전트를 쓰고, 본 프로젝트 전용 전문직 2명만 직접 둔다.

| 역할 | 담당 | 비고 |
|---|---|---|
| 총괄 PM | 이 CLAUDE.md + 메인 Claude(나) | 직접 구현보다 위임·검수 우선 |
| 시장조사 | bkit `product-manager` / `pm-research` | 1차 보고서 완료 |
| 서비스 기획(PRD) | bkit `product-manager`, `pm-lead` | |
| UX/UI/디자인시스템 | bkit `frontend-architect` (+ `/phase-5-design-system`) | |
| 시스템 설계 | bkit `infra-architect` / `enterprise-expert` | |
| 보안·개인정보 | bkit `security-architect` | 생체정보 검토 필수 |
| 검수/QA | bkit `qa-lead`, `gap-detector` | |
| **얼굴인증·실근무시간** | **전용 `face-auth`** (.claude/agents) | bkit에 없어 직접 생성 |
| **GPS/위치 인증** | **전용 `gps-verify`** (.claude/agents) | bkit에 없어 직접 생성 |

---

## 3. 진행 순서와 "승인 게이트" (중요)

⚠️ **CLAUDE.md는 스스로 자동 실행하지 않는다.** 사람(사장님)의 목표 지시로 시작한다. (비개발자 보호 장치)

**진행 모드: 반자동 (기본값).** 여러 단계를 묶어 진행하되, **중요한 결정(예: 얼굴 API 선택, 기능 범위, 비용·법적 트레이드오프)에서만 멈춰 사장님께 확인**한다. 사소한 단계는 멈추지 않고 진행 후 4줄 보고한다. (모드는 사장님 요청 시 수동/완전자동으로 변경 가능)

```
시장조사 → 기획(PRD) → UX/UI → 설계(ERD/API) → [개발] → 검수(QA/보안)
   ✅완료      ←여기부터 진행→
```

- 개발 단계(backend/frontend) 직원은 **설계가 끝난 뒤** 만든다. 미리 만들지 않는다.
- 각 단계 종료 시 PASS면 다음, FAIL이면 재작업.

### 🧭 마스터 플로우 복귀 규칙 (필수)
- 큰 틀은 항상 [WORKFLOW.md](WORKFLOW.md)를 기준으로 한다. 현재 위치는 [PROGRESS.md](PROGRESS.md)에서 확인·갱신한다.
- **작업 시작 전 PROGRESS.md의 "현재 단계"를 먼저 확인**한다.
- 사장님이 중간에 다른 요청(곁가지)을 하면: ① PROGRESS.md `곁가지 로그`에 기록 → ② 처리 → ③ **반드시 "현재 단계"로 복귀**해 마스터 플로우를 이어간다.
- 큰 틀 자체를 바꾸는 요청이면 WORKFLOW.md 갱신 여부를 **먼저 사장님께 확인**한 뒤 반영한다.
- 각 단계 완료 시 PROGRESS.md(현재 위치·완료·대기)를 갱신한다.

---

## 4. 입출력 형식 고정 (직원 간 협업 규칙)

각 직원은 "앞 단계 산출물"을 입력받아 "정해진 산출물"을 낸다. 형식을 고정해 혼선을 막는다.

| 단계 | 입력 | 출력(파일) |
|---|---|---|
| 시장조사 | (없음) | docs/01_market/ |
| 기획 | 01_market | docs/02_prd/PRD.md, requirements.md |
| UX/UI | 02_prd | docs/03_design/ |
| 설계 | requirements.md | docs/04_architecture/ (ERD, API) |
| 얼굴인증 | requirements.md | docs/07_ai/face-spec.md |
| GPS | requirements.md | docs/07_ai/gps-spec.md |
| 보안 | 전체 | docs/08_security/review.md |
| QA | 구현물 | docs/09_test/result.md |

규칙: **ERD 없이 API 금지, API 없이 구현 금지, 보안 검토 없이 배포 금지.**

---

## 5. 단계별 보고 형식 (모든 직원 공통)

작업을 마치면 반드시 아래 4줄로 보고한다:

```
## 완료 작업
## 생성 파일
## 위험 요소
## 다음 단계 제안
```

---

## 6. 법적 가드레일 (절대 규칙)

- **얼굴 = 생체정보 = 민감정보.** 별도 동의·암호화 저장·접근통제·파기절차 없이 설계/구현하지 않는다.
- **얼굴인증을 직원에게 강제하지 않는다.** 반드시 GPS 등 대체 출퇴근 수단을 함께 제공한다 (국가인권위 기준).
- **얼굴 인식 = 사내 얼굴인식 서버에 API 연동**(등록·검출·인식). 백엔드·DB는 관리형(A안). → 얼굴 데이터가 사내 서버에 보관되므로 생체정보 의무(암호화·접근통제·동의·파기)가 **사내 서버에도 적용**된다. [5] 보안·법무에서 사내 서버 포함 점검 필수.
- 근로기준법: 5인 이상 사업장 근로시간 기록·3년 보존 기능을 기본 제공한다.
- 비밀키·비밀번호·인증서를 소스/문서에 기록하지 않는다.
