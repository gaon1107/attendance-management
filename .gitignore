# 의존성 (용량 큼 — 저장 안 함, 필요하면 npm install로 다시 받음)
node_modules/

# Next.js 빌드 산출물
.next/
out/
build/

# 로컬 DB (SQLite) — 실제 근태 데이터는 저장점에 넣지 않음
*.db
*.db-journal
*.sqlite
*.sqlite3

# 환경변수·비밀값 (절대 커밋 금지 — ClientToken 등)
.env
.env*.local
*.pem

# 로그
*.log
npm-debug.log*

# OS·에디터
.DS_Store
Thumbs.db
.vscode/
.idea/
