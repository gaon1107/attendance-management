# 진행 상황 추적기 (지금 어디?)

> 매 단계 시작·완료 시 갱신한다. "옆길로 샜다가 돌아올 좌표"가 여기다.
> 큰 틀은 [WORKFLOW.md](WORKFLOW.md) 참고.

---

## 🔜 다음 세션 시작점 (내일 여기서 이어가기) — 새 세션은 이 블록부터 읽기

> ⏱️ 최종 갱신: 2026-07-08. 앱은 `webapp/`에 실제 작동 중. 실행법: **루트의 `근태관리_실행.bat` 더블클릭** 또는 `cd webapp && npm run dev` → localhost:3000.
> **현재 로그인 계정: 관리자 `admin@admin.com` / `admin`** (회사=㈜하늘테크). 직원 계정은 관리자로 로그인해 [직원관리]에서 초대/등록해 만들면 됨. (DB엔 admin + psh083239@gmail.com(박성헌, 사장님 실계정) 2명. 직원 데이터는 사장님이 직접 만들어 테스트.)

### 🧪 사장님 직접 테스트 체크리스트 (다음 세션 전에 눌러보면 좋은 것 — 오늘 새로 만든 4가지)
> admin@admin.com / admin 으로 로그인 후:
- [ ] **보안**: 로그인 화면에서 일부러 비번 5번 틀려보기 → 6번째에 "10분 잠금" 뜨는지. (실제 잠기니 테스트 후 알려주면 풀어드림, 또는 10분 기다리면 자동 해제)
- [ ] **직원 관리**: [직원관리]에서 초대링크로 직원 1명 만들기 → 직원 상세에서 **연차 부여**·**비밀번호 재설정**·**퇴사 처리** 눌러보기 → 퇴사자가 "퇴사한 직원" 칸으로 내려가고 로그인 막히는지 → **복직**도.
- [ ] **휴가**: (직원으로 로그인해) [휴가]에서 연차 신청 → (관리자로) [휴가승인]에서 승인 → 직원 [내근태]에서 그 날이 **"휴가"로 표시**되고 결근이 아닌지, 잔여 연차가 줄었는지.
- [ ] **공지**: [공지]에서 공지 작성 → 직원 [출퇴근] 화면 위에 **배너**로 뜨는지, 관리자 [대시보드] "오늘 알림"에 뜨는지.
- [ ] 각 화면을 **휴대폰(좁은 화면)**으로도 열어 깨지지 않는지.
> 눌러보다 이상한 점/바꾸고 싶은 점을 메모해두면 다음 세션에 바로 고침.

- **✅ A단계(MVP 화면 완성) 완료(2026-07-07)** — 설계 P0 중 미구현이던 8개 화면을 추가(얼굴 3종 제외). 커밋 `31ab533`~`a80bfe4`:
  1. 근무제·기준시간 설정(설정 화면, Company에 workStartTime/workEndTime/lateGraceMin 추가) — 지각/정상 판정 근거
  2. 근태 현황 조회(전체) `/records` + 직원별 상세 `/records/[id]` (지각 표시, 회사격리)
  3. 내 근태 조회(직원) `/my-records` · 4. 직원 상세/수정 `/employees/[id]`
  5. 생체정보 동의·파기 관리(관리자) `/biometrics` · 6. 계정 설정/비번변경 `/account`(프로필 아바타로 진입)
  7. 회사 초기설정 온보딩 `/onboarding`(가입 후 이동) · 8. 에러화면 `not-found`/`error`
  - 공통: 로그아웃 버튼을 상단바 우측 공통화, consent 화면 AppShell 통일, PeriodNav·lib/labels 공통 컴포넌트.
  - ✅ 검증: `tsc --noEmit` 통과 + 실서버 스모크(신규 필드 안 쓰는 화면 200, 404/권한리다이렉트 정상). **⚠️ 근무기준 필드 쓰는 5개 화면은 dev 서버 재시작 1회 필요**(구동 중 서버가 옛 Prisma 클라이언트를 메모리에 물고 있어 500 → 재시작 시 해소, 새 프로세스 쿼리로 정상 확인함).

- **✅ P1-⑤ 직원 알림(공지) 읽음 표시 완료(2026-07-08)** — 사장님 결정=**새 공지사항만** 알림으로. 별도 중복 페이지 없이 기존 공지에 얹음: 직원 홈(출퇴근) 배너에 **🔔 새 알림 N 배지**(마지막 확인 이후 올라온 공지 수), [공지] 화면 열면 자동 읽음 처리 + 안 읽은 공지에 **NEW 태그**. User.noticesSeenAt(migration `notices_seen`). 브라우저 검증(공지 게시→홈 배지 1→공지 열람→NEW·배지 해제). ※ 휴가/정정 결과 알림은 사장님이 범위에서 뺌(추후 추가 가능).
- **✅ P1-④ 초과근무 관리 완료(2026-07-08)** — 기준=**하루 8시간 초과분**(사장님 결정). [설정 → 근무제·기준시간]에 "기준 일 근무시간"(기본 8h, 0.5 단위) 추가, [리포트]에 **초과근무 열 + 총 초과근무 KPI** 추가(일별 max(0, 그날 실근무−기준)의 합, 일/주/월 기간별). Company.standardWorkHours(migration `standard_work_hours`). 브라우저 검증(9h근무→기준8h=초과1h, 기준9h로 바꾸니 0). ※야간·휴일 가산수당 자동계산은 급여 영역=2차.
- **✅ P1-③ 월간 캘린더 뷰 완료(2026-07-08)** — 직원 [내근태]에 **표/달력 보기 전환** 추가. 달력은 월 그리드로 날짜별 상태(출근·지각·휴가·결근·휴일근무)를 색+라벨+실근무시간으로 표시(범례 포함), 월 이동(◀▶). 계산은 기존 buildDayEntries 재사용(표와 동일 데이터). 새 사이드바 메뉴 없이 토글로 넣음. 스키마 변경 없음. 브라우저 검증. ※ 테스트 계정(박성헌) 근무요일이 토·일(6,0)로 돼 있어 달력에 토·일 근무·평일 비번무로 보이는데 이는 데이터 설정 반영이지 오류 아님(직원 상세에서 근무요일 바꾸면 정상).
- **✅ P1-② 근태 정정 요청 완료(2026-07-08)** — 직원이 특정 날짜의 출근/퇴근 시각을 정정 요청(사유 필수) → 관리자 승인 시 **그 날 출퇴근 기록(Attendance)에 실제 반영**(기록 있으면 수정, 없으면 생성)·실근무시간 재계산. 화면=/corrections(직원, 사이드바 "정정" 추가)·/corrections/approvals(관리자, 사이드바 아이콘 없이 대시보드 오늘알림·근태현황 상단 버튼으로 진입). AttendanceCorrection 모델(migration `attendance_correction`). 브라우저 E2E 검증(요청→승인→07-06 09:00~18:00 기록 생성 확인). ※ 반차/외출 정정 등 세밀 정정은 추후.
- **✅ P1-① 부서 관리 완료(2026-07-08)** — 회사 안 부서(조직) 생성/이름변경/삭제 + 직원 배정. [직원관리] 화면에 "부서 관리" 섹션(부서별 인원수 표시)·직원표에 **부서 열** 추가, [직원 상세]에 부서 드롭다운 배정. 부서 삭제 시 소속 직원은 지워지지 않고 **미배정(null)** 으로. 회사 격리 적용. Department 모델 + User.departmentId(migration `department`). 브라우저 검증(부서생성→배정→목록반영). ※ 대시보드·근태현황의 부서별 필터는 추후.
- **✅ 비밀번호 재설정(관리자 발급식) 완료(2026-07-08)** — 직원이 로그인 화면 "비밀번호를 잊으셨나요?" → `/forgot-password`에서 이메일로 **재설정 요청** 접수(있는/없는 이메일 동일 안내로 존재여부 노출 차단). 관리자는 [직원관리] 상단 대기열 + [대시보드] 오늘알림에서 요청 확인 → **"임시 비밀번호 발급"** 클릭 시 읽기 쉬운 10자리 임시비번 자동생성·화면표시(복사) → 직원에게 전달. 직원이 임시비번으로 로그인하면 `mustChangePassword`로 **강제 `/change-password`** → 새 비번 설정 후 홈으로. 발급 시 기존 세션 무효화·실패잠금 초기화. **자동 메일발송·문자(SMS)인증은 2차**(사장님 결정). PasswordResetRequest 모델 + User.mustChangePassword(migration `password_reset`). 브라우저 E2E 검증 완료(요청→발급→임시로그인→강제변경→착지, 존재여부 노출 차단까지). ⚠️ 테스트 중 psh083239 계정 비번이 `newPass4567`로 바뀜(관리자 화면에서 재설정 가능).
- **✅ 부수기능 2탄: 알림·공지 완료(2026-07-08, 커밋 `1ce52ea`)** — ① 사내 공지사항(Announcement 모델, migration announcements): 관리자 작성/삭제·직원 열람전용(/notice), 최신공지 배너(직원 출퇴근). ② 대시보드 "오늘 알림" 카드: 미출근·지각 직원 명단+승인대기 휴가+최신공지(전부 실데이터). 무거운 알림엔진·푸시·이메일은 안 만듦(배포 단계). 브라우저 E2E 검증 완료.
- **✅ 부수기능 1탄: 휴가 신청·승인(연차 포함) 완료(2026-07-08, 커밋 `dbbe6c5`)** — 직원 신청→관리자 승인/반려→잔여 연차 자동계산. 종류=연차/반차/병가, 연차부여=관리자 직접입력(기본15). **승인된 휴가일은 결근이 아니라 "휴가"로 처리**(내근태·직원별상세·대시보드 미출근·리포트 결근 모두 연동). LeaveRequest 모델+User.annualLeaveDays(migration leave_requests). 화면=/leave(직원), /leave/approvals(관리자), 직원상세 연차카드. 브라우저 E2E 검증 완료. ⚠️ 자동 법정연차계산은 2차.
- **✅ 직원 관리 완성(2026-07-07, 커밋 `1ac7d79`)** — 퇴사(비활성화)·복직 + 관리자의 직원 비번 재설정. 퇴사 시 로그인차단·세션삭제·집계제외, 과거 근태 보존. User.deactivatedAt(migration employee_deactivation).
- **✅ 보안 점검·조치 완료(2026-07-07, 커밋 `b53132e`)** — 코드 전체 정독 + 실동작 검증. **다중회사 격리·비번해시·회사격리 안전 확인.** 추가 보강: ① 로그인 5회 실패→10분 잠금 ② 사내 IP 대역 매칭 정밀화 ③ 만료 세션 자동정리 ④ 웹 보안헤더 5종. **배포 때 마저 할 것**(IP헤더 신뢰설정·비번찾기·HTTPS)은 `docs/08_security/보안점검_2026-07-07.md`에 정리. ⚠️ 로그인에 User.failedLoginCount/lockedUntil 필드 추가됨(migration `login_lockout`).

- **지금까지 실제 개발 완료(2026-07-05, webapp/ 에 다 작동·브라우저 검증·git 커밋됨)**:
  1. 랜딩(완성형 시안) · 회사가입 · 로그인/로그아웃 (Prisma+SQLite, scrypt 해시, DB세션쿠키)
  2. 직원 등록(관리자) · 출퇴근(출근/퇴근) · 관리자 대시보드 오늘현황
  3. 실근무시간(=퇴근−출근−외출) · 외출/복귀(사유 드롭다운)
  4. 위치확인: 근무형태(사무실/재택/외근) — 사무실만 GPS 또는 **사내 IP** 확인, 재택·외근 면제. 회사위치 설정 화면 **+ Leaflet 지도(깃발·반경원)**. GPS 원본좌표 미저장(결과만).
  5. 근태 리포트(일/주/월 집계) + **법정기록 CSV 내보내기**(/reports)
  6. 얼굴인증 **1phase만**: 인증방식 선택(얼굴/GPS, 강제 아님) + 생체정보 동의(4항목)·철회. (동의 문안=초안, 전문가 검토 필요)
- **기술스택**: Next.js 16 + React 19 + Prisma **6**(7 아님-SQLite 이슈) + 로컬 SQLite. ⚠️ Next 16은 옛버전과 다름 → 코드 전 `webapp/node_modules/next/dist/docs/` 확인. 스타일=인라인+디자인토큰(globals.css), 지도=leaflet.
- **git**: 프로젝트 루트 git 저장소. **작업(마일스톤)마다 자동 커밋**(사장님 지시, 안 물어봄). 마지막 커밋들: 랜딩·로그인·출퇴근·실근무·위치·IP·리포트·얼굴1phase.

- **🚧 바로 다음 할 일 = 얼굴인증 2phase(실제 등록/인식) — 현재 "보류" 상태(막힘)**:
  - **막힌 이유(2026-07-05)**: ① GaonFR 서버 소스가 로컬에 없고 **SVN 서버(svn.newgaon.com)** 에만 있음 → 받으려면 **SVN 로그인(주소+계정) 필요한데 위치 불명** ② 개발자(김준우·정홍섭) 연락 불가 ③ 운영 얼굴서버 직접조작 리스크 ④ 법적 준비(동의서·위수탁) 미완.
  - **알아낸 발급 규약(확정)**: 앱이 `POST https://gaonfr.newgaon.com/login/` 에 헤더 `LicenseKey` + `Authorization: Basic base64("ClientId:Password")` 제시 → 서버가 **ClientToken(24h)** 발급 → `POST /get/token` 으로 **ApiToken(15분)** → `/v1/face/enrollment`·`/v1/face/unenrollment` 사용. 즉 한 계정 = **{ClientId + Password + LicenseKey}**.
  - **저장위치**: 클라이언트/토큰 = **MSSQL `server03.newgaon.com:1433`**(Gaon Face API DB). 라이선스 = `db.newgaon.com/lc`. 서버 = `gaonfr.newgaon.com`(.NET/C#/IIS, 소스는 SVN). 전체 서버지도 = `C:\Users\주인님\Desktop\가온 서버 및 정보\_서버목록_읽기.txt`.
  - **참고자료 위치**: API정의서=`가온 서버 및 정보\GaonFaceAPI 정의서.xlsx`. 개발용 계정예시(IONE_DEV_SERVICE, **업체 사용중이라 재사용 불가**)=`H:\김준우\온라인버전\웹서비스\GaonFaceAPI_20210413\IOne_Key.txt`. LMS 연동참고코드=`C:\Users\주인님\Desktop\newgaon-LMS\backend\services\faceEnrollService.js`.
  - **도구**: 이 PC에 **TortoiseSVN 1.14 설치 완료**(GUI). SVN 로그인만 확보되면 체크아웃 가능.
  - **⚠️ 보안 경고**: `가온 서버 및 정보\가온 서비스.20211025_2.xlsx` 에 **운영서버 실제 비번**들이 평문으로 있음 → 이 폴더/파일 보호 필요.
  - **다음 세션 재개 조건**: (a) SVN 로그인 확보 → TortoiseSVN으로 GaonFaceAPI(.NET) 소스 체크아웃 → 클라이언트 등록방법 찾기, 또는 (b) 잠깐이라도 개발자 손. **둘 다 어려우면 얼굴은 계속 보류.**

- **💡 얼굴 빼고는 근태 제품이 이미 잘 작동** → 다음 세션 옵션: ⓐ 얼굴 2phase(위 조건 갖춰지면) ⓑ 남은 화면 정식화(72개 시안 중) ⓒ QA·보안 점검 ⓓ 배포 준비(서버 임대+Docker).
- **개발 시 참고 문서**: 데이터/API=`docs/04_architecture/` · 얼굴=`docs/07_ai/face-spec.md` · GPS=`docs/07_ai/gps-spec.md` · 보안=`docs/08_security/review.md`. 화면지도=`docs/03_design/화면_웹모바일_구분.md`.
- **불변 규칙**: 회사별 격리(companyId), 비밀값 env 전용, GPS원본·얼굴원본 미저장, 얼굴 강제금지+GPS대체, 실근무=재실−자리비움−외출. **사장님 비개발자** → 쉬운 말·한 걸음씩(반자동). **마일스톤마다 git 커밋 자동.**
- 시작 방법: 새 세션에서 "PROGRESS.md 보고 이어서 하자".

---

## 📍 현재 위치

- **현재 단계**: [6] 개발 — **핵심 기능 1~7단계 실제 작동 완료**(webapp/). 얼굴인증만 1phase(동의·선택) 완료, **2phase(실제 등록/인식)는 SVN/개발자/법적준비 막혀 보류.**
- **다음 게이트**: 🚦 얼굴 2phase 재개조건(SVN 로그인 or 개발자) 확보 여부 → 안 되면 얼굴 보류하고 남은화면·QA·배포로 진행.
- **진행 모드**: 반자동
- 디자인 폴더: `근태 관리 디자인 스타일-2/` (파일 73개 = MVP 48 + 미래확장 24 + Canvas 1). canonical 4개: 근무제 기준시간 설정 · 관리자 근태현황 · 실근무시간 리포트 · 생체정보 동의·파기 관리.
- **창고(DB)**: 지금은 사장님 PC 안 로컬 SQLite로 개발·테스트. **정식 서버는 나중에 임대**해 이전. (클라우드/bkend.ai 보류)
- PC 개발환경: Node.js v24 · npm · git 설치 확인됨(추가 설치 불필요).
- 참고: `prototype/` 폴더의 예시 HTML은 미사용(무시).

### 🕒 열린 항목 (보류 — 나중에 결정)
- **도커(Docker) = 배포 단계에 적용**(2026-07-05 확인). 지금 로컬 개발(SQLite)엔 미사용. 서버 임대 후 앱을 도커로 포장해 이전 + 실제 DB(Postgres 등)도 컨테이너로. 앱은 처음부터 도커 담기 쉬운 구조(Next.js)로 개발 중이라 나중에 부담 적음.

### 🗺️ 추후 개발(2차 이후) 로드맵
- **메신저** — 화면 디자인(㉗)은 제작, **개발은 2차 확정(2026-07-04)**. 💡차별화 아이디어: 근태와 엮은 메신저(미출근자 즉시 메시지, 외출중 표시 등)로 카카오워크·슬랙과 차별화.
- 파일 저장/공유 · 급여 자동정산 · 근무일정/교대 스케줄 · 전자결재/휴가결재
- **MES 연동**(사장님 회사 자체 MES) — 최종 목표, 근태 MVP 이후.
- **[기록만 · 추후 확장] 경쟁조사(2026-07-05)에서 도출된 3가지** — 근거: [03_경쟁서비스_전체기능_조사.md](03_경쟁서비스_전체기능_조사.md). *지금 MVP엔 미포함, 확장 개발용 기록.*
  - ① **전자결재**(휴가·초과근무 승인 워크플로) — 경쟁사 대부분 기본 제공, 사무직이 근태 다음으로 요구. 향후 P1 격상 검토 후보(간단 승인 1~2단계로 최소화 가능).
  - ② **이상근태·워라밸 분석 리포트** — 우리가 쌓는 실근무시간 데이터 재가공. 경쟁사도 비어있는 영역=차별화. ⚠️'감시' 프레임 안 나게 설계 주의(인권위).
  - ③ **출입통제(세콤·캡스 등) API 연동** — 시장 표준은 '얼굴서버 자체구축'이 아닌 '기존 출입장비 연동'. 대기업 확장 시 필요. ⚠️사내서버 생체정보 보관 이슈 커짐→보안 사전검토 필수.
- (1차 MVP 핵심: 출퇴근·얼굴·GPS·PC출퇴근·실근무시간·외출복귀·리포트·법정기록)

### 🔑 확정된 주요 결정
- **직원 PC 출퇴근 지원** — 2026-07-04 확정. 사무직이 회사 PC에서 출퇴근. 위치는 **사내 WiFi/회사 IP**로 확인(PC엔 GPS 없음), 얼굴은 **PC 웹캠**. → **WiFi/IP 인증이 P1→P0로 승격**(PC 출퇴근의 핵심). 관리자 설정에 '사내 네트워크(WiFi/IP) 등록' 추가. ⚠️ 고정 IP 없는 회사는 얼굴+간이확인 병행.
- **점심/휴게 처리 = 외출/복귀 버튼 + 외출 사유 드롭다운(식사/외근/개인용무/기타)** — 2026-07-04 확정. 외출 시간은 실근무시간에서 제외(산식: 실근무 = 재실 − 자리비움 − 외출). ⚠️ '외근'을 근무로 인정할지는 관리자 정책으로 추후 결정.
- 실근무시간 산출 = **옵션 B**(주기적 얼굴검출 샘플링, 이미지 미저장) — 2026-06-30 확정. *5단계 보안·법무 검토에서 재확인 필요.*
- 화면 목록 = **P0 30개 / P1 9개 / 총 39개** — 2026-07-04 승인. 결제·요금제 3개 화면은 P1(파일럿 이후)로 조정.
- UX/UI = 와이어프레임 + 디자인 토큰(파랑 Primary, 예시) 승인 — 2026-07-04.
- 로그인/회원가입에 **소셜 로그인(카카오·네이버·구글) 추가**(2026-07-04). 디자인엔 지금 반영. ⚠️ 실제 연동은 개발 단계 작업(각 사에 앱 등록+OAuth 연결, Auth.js 등) — 표준·가능, 그때 구현.
- 기술스택 = **A안(관리형 백엔드/DB)** + **얼굴은 사내 얼굴서버(GaonFR) API 연동** — 2026-07-04 확정.
  - API: `https://gaonfr.newgaon.com` (enrollment/recognize/detect/unenrollment), 인증 ClientToken→ApiToken. 참조: `C:\Users\주인님\Desktop\newgaon-LMS` (faceEnrollService.js, sample02.js)
  - 저장: 우리 DB엔 **참조값만**(직원↔FaceId, Group, 동의상태). 얼굴 원본·템플릿은 GaonFR 보관.
  - ⚠️ **라이브니스(위조 방지) 미지원** → 대리출근 방지 보완책 별도 설계 필요. [5] 보안·법무에서 사내 서버 포함 + 라이브니스 공백 점검.
  - 🔑 ClientToken 등 비밀값은 소스/문서 미기록, env로만 관리.
- 보안·법무 결정(2026-07-04): 옵션 B=**1-A 유지**(전문가 자문 후 확정) / 라이브니스=**2-A 웹MVP** / GPS좌표=**미저장** / 보조확인=**정책 B**.

### 🔒 파일럿 확대 전 필수 이행 (아직 안 됨 — 잊지 말 것)
- GaonFR **위수탁 계약** + "회사→우리→뉴가온" 재위탁 구조 정리 (변호사)
- **개인정보 전문가·노무사 자문 1회** (옵션 B 법적 성격 · 동의서 · 위수탁 · 근로기준법 보존)
- **동의서 2종**(생체정보·위치정보) 실제 문안 작성 → 전문가 검토 → `consent_version` 연동
- 구현 시 **OWASP 체크리스트**(테넌트 필터 강제 · 이미지 미저장 검증 · nonce 서버검증 · 보안헤더 · rate limit)

---

## ✅ 완료
- [0] 시장조사 — `01_시장경쟁사_조사보고서.md`
- (사전준비) 조직 구성: CLAUDE.md, face-auth, gps-verify, WORKFLOW.md, PROGRESS.md
- [1] 기획(PRD) — `docs/02_prd/` (PRD, requirements, user-flow), 실근무시간 범위 옵션 B 확정
- [2] UX/UI 설계 — `docs/03_design/` (screen-inventory, wireframes, design-tokens) + 색 입힌 시안 승인
- [4] 전문 설계 — `docs/07_ai/` face-spec(GaonFR 연동), gps-spec(위치·지오펜스)
- [3] 시스템 설계 — `docs/04_architecture/` ERD, API, ARCHITECTURE
- [5] 보안·법무 검토 — `docs/08_security/review.md` (부적합 없음, 사장님 결정 6항목 도출)

## 🔄 진행 중
- [6] 개발 — **walking skeleton 착수함(2026-07-05).** 방식=가볍게 새로(Next.js 16 + 로컬 SQLite, 오픈소스 스타터 안 씀). 앱 폴더 `webapp/`.
  - ✅ 1단계: 앱 뼈대 설치 + 브랜드 랜딩 화면(우리 디자인 토큰 적용) 실제 작동 확인(localhost:3000).
  - ✅ 2단계: DB(Prisma+SQLite) + 회사가입/로그인/로그아웃 **실제 작동 검증 완료**. 테이블=Company/User/Session. 비번=scrypt 해시, 세션=DB토큰+httpOnly쿠키. 틀린 비번 차단 확인. (테스트계정: admin@skytech.co.kr / test1234)
  - ✅ 3단계: 직원 등록 → 출퇴근(출근/퇴근) → 관리자 대시보드 현황 **전체 흐름 작동 검증 완료(2026-07-05).** 
    - 검증: 관리자가 김직원 등록 → 직원 로그인(역할분기 /attendance) → 출근 → 관리자 대시보드에 "오늘 출근 1명·근무 중 1명, 김직원 16:51 근무 중" 실시간 반영. Attendance 테이블 추가.
    - **🎯 walking skeleton 핵심 목표 달성**(회사가입→로그인→직원등록→출퇴근→대시보드 실제 작동).
  - ✅ 4단계: **실근무시간 + 외출/복귀** 작동 검증 완료(2026-07-05). Break 테이블 추가. 산식=(퇴근−출근)−외출합. 외출 사유 드롭다운(식사/외근/개인용무/기타). 직원화면·관리자 대시보드 모두 실근무 표시. (외출→복귀→퇴근→실근무 9분 브라우저 검증)
  - ✅ 5단계: **위치 확인(GPS) + 근무형태(사무실/재택/외근)** 작동 검증 완료(2026-07-05). 
    - 사무실만 위치확인, 재택·외근 면제+기록표시. 위치 벗어나도 출근 허용(부드럽게). 회사 위치는 관리자 설정(/settings). **GPS 원본좌표 미저장**(결과만: 확인됨/벗어남/확인불가) — 프라이버시.
    - 검증: 사무실(회사안=확인됨/회사밖=벗어남)·재택(위치확인 없음) 모두 대시보드 반영. 재택 집위치 미추적.
  - ✅ 6단계: **사내 WiFi/IP 확인(PC 출퇴근)** 작동 검증 완료(2026-07-05). 사무실 출근 = GPS(휴대폰) OR 사내 IP(PC) 중 하나만 맞아도 확인됨. 브라우저는 WiFi 이름 못 읽으므로 회사 공인 IP로 확인(앞부분 일치=대역 허용). 관리자 설정에 '현재 접속 IP 표시 + 이 IP 추가'. 검증: GPS 끈 PC 상황에서 사내 IP(::1)로 "위치 확인됨". 지도(Leaflet)도 완료.
  - ✅ 7단계: **근태 리포트(일/주/월 실근무 집계) + 법정기록 CSV 내보내기** 작동 검증 완료(2026-07-05). /reports(관리자), 단위 탭·기간 이동, 직원별 실근무·근무일수·외출 집계. CSV=날짜별 상세(이름/날짜/근무형태/위치확인/출근/퇴근/실근무/외출) UTF-8 BOM(엑셀 한글). 브라우저 검증(표+CSV 내용 확인).
  - 🔄 8단계(얼굴인증) **1phase 완료**(2026-07-05): 비밀키·카메라 없이 되는 **생체정보 동의 화면 + 인증방식 선택(얼굴/GPS)** 작동 검증. User에 authMethod·faceConsentAt 추가. 얼굴 선택→동의(4항목+체크)→기록, 철회→GPS 전환. 동의 문안은 **초안**(전문가 검토 후 확정 표시).
    - GaonFR API 흐름 파악 완료: `newgaon-LMS/backend/services/faceEnrollService.js` 참조(토큰 get/token→ApiToken, enrollment/unenrollment, FaceId=직원ID·Group=회사, 비밀키 env).
  - ⏭️ 8단계 2phase(얼굴 등록/인식) — **필요: ① ClientToken을 webapp/.env(FACE_CLIENT_TOKEN)에 추가 ② 웹캠 실기기 테스트 ③ 위수탁계약·전문가자문**. 준비되면 착수.
  - ⚠️ 열린 항목: 재택 GPS 기록 여부 = **노무사·개인정보 전문가 자문 후 결정**(집 위치 추적 리스크). 지금은 미구현.
  - 실행법: `webapp`에서 `npm run dev`. (Next.js 16 + Prisma 6. Prisma 7은 SQLite에 driver adapter 필요해 6으로 고정함.)
  - ⚠️ 개발용 임시 계정/데이터는 `webapp/prisma/dev.db`에 있음(git 미포함). 실계정 아님.
  - (참고: `docs/05_backend/skeleton-plan.md`는 bkend.ai 기준 초안 → 로컬 SQLite로 변경됨. 데이터 모델·흐름은 유효.)

## ⏳ 대기 (backlog)
- [6] 개발 — walking skeleton 이후: 얼굴인증·옵션B·리포트·온보딩 정식화 등
- [7] 통합·QA
- [8] 파일럿 (⚠️ 확대 전: 위수탁 계약·전문가 자문·동의서 문안 필수)
- [9] 출시 준비

---

## 🌿 곁가지 로그 (큰 틀에서 잠깐 벗어난 작업)
> 사장님의 중간 요청을 여기 기록하고, 끝나면 위 "현재 단계"로 복귀.

- 2026-07-04 경쟁·참고 서비스 분석(시프티·지블·무료비교글) → **서비스 범위(무엇을 넣을지) 확정용** + 디자인 참고. 산출물 `02_경쟁서비스_분석.md`. ✅ **완료 → 범위 확정, [6] 화면 제작으로 복귀함.**
  - 확정 범위: 제안대로. **제외 = 전자계약 · 그룹웨어 통합 · 직원 모니터링/스크린샷**(감시성·인권위 리스크). 얼굴+GPS는 지블 참고, 관리자 화면은 시프티 참고.
- 2026-07-04 개발용 오픈소스 조사 → `docs/05_backend/오픈소스_조사.md`. **권고: Next.js SaaS Starter(MIT)를 뼈대로 + Turf.js(GPS) + GaonFR 연동.** 저장기능 구현 시 이 뼈대 위에 얹기.
- 2026-07-04 **최종 목표 = MES 연동** 확인. 순서: **근태 먼저 → MES는 2차.** MES는 사장님 회사 자체 보유(연동 규격 확보 가능). 지금 반영: **연동 친화적 설계**(안정적 직원ID·근태데이터 내보내기 지점)만 유지, MES 구현은 2차. → 지금 계획 변경 없음, [6] 화면 단계로 복귀.
