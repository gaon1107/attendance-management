# 아이원(KICC) 카드결제 연동 — 가온출결 이관용 핸드오프

> 근태관리에서 테스트로 검증 끝난 내용을 **가온출결 프로젝트로 옮겨 붙이기** 위한 정리 문서.
> 담당: 아이원 정진식 소장(010-3034-2828) · 작성 기준일 2026-07-09

---

## 0. 한눈에 (요약)

- **구조**: 우리(가맹점) → **아이원(대리점)** → **KICC(VAN)** → 카드사
- **대금 입금**: 전부 **뉴가온 계좌**(사업자 1178119949, 가맹점명 gaon)
- **두 부분으로 구성**
  1. **호출(결제창 띄우기)** — ✅ 근태관리에서 검증 완료 (실제 결제창 뜸, "근태관리 이용료" 표시)
  2. **받기(결제 결과 처리)** — ⬜ 가온출결에 만들어야 함 (아이원이 우리 서버를 호출)
- **인코딩**: UTF-8 그대로 OK (한글 상품명 정상 표시 확인됨)

---

## 1. 전체 흐름

```
1. 우리 서버/화면이  card_appr.ashx  호출 (아래 8개 파라미터)
2. 카드 결제 화면이 뜸 (IONE NETWORKS PAY)
3. 고객이 카드/카카오페이로 결제
4. 결제가 끝나면 → 아이원이 "우리 받는 주소(return URL)"를 호출해 결과 전달
       → https://lms.newgaon.com/payment/kicc/return
5. 우리(가온출결)가 그 결과를 받아서 처리 (성공/실패 반영)
```

---

## 2. 호출 규격 (결제창 띄우기) — ✅ 검증 완료

### 주소
```
http://tapprsys.ione24.com/_appr/card_appr.ashx      (테스트)
```
※ 운영 주소는 아이원에서 별도 확인 (운영은 https 필요)

### 파라미터 (8개) — POST, application/x-www-form-urlencoded

| 파라미터 | 예시값 | 의미 | 성격 |
|---|---|---|---|
| pkind | appr | 처리종류=승인 | 🔒 고정 |
| sitekey | 2008 | 사이트키(아이원 발급) | 🔒 고정 |
| terminal | 0788888 | KICC 터미널ID | 🔒 고정 |
| cpname | gaon | 가맹점(학원)명 | 🔒 고정 |
| cpnum | 1178119949 | 사업자번호 | 🔒 고정 |
| amt | 11000 | 결제금액(원) | 🔄 매번 |
| requestkey | 26070913275801 | 요청 고유번호(14자리, 중복금지) | 🔄 매번 새로 생성 |
| itemname | 근태관리 이용료 | 주문상품명(결제창에 표시) | 🔄 용도별 |

- **requestkey 생성 규칙**: `연월일시분초(12자리) + 순번(2자리) = 14자리`. 매 결제마다 새로.
- **itemname**: 근태관리 결제=`근태관리 이용료`, 학원 수강료=`학원비결제` 처럼 값만 바꿔 재사용.
- **응답**:
  - 성공 → **카드 결제 페이지(HTML)** 가 반환/표시됨
  - 실패 → **JSON** `{"code":"...", "message":"..."}` → **message를 사용자에게 보여줄 것** (아이원 지시)

### 호출 방법 2가지
1. **실제 결제화면 띄우기**: 브라우저에서 위 주소로 **폼(POST) 전송** → 결제 페이지가 새 창/현재 창에 뜸.
2. **서버에서 값만 확인**: 서버가 대신 호출해 응답을 받아봄(브라우저 CORS·혼합콘텐츠 우회용).

### 참고 코드 — 브라우저 폼 전송(결제창 열기)
```js
function openPaymentScreen(params) { // params = 위 8개
  const form = document.createElement("form");
  form.method = "POST";
  form.action = "http://tapprsys.ione24.com/_appr/card_appr.ashx";
  Object.entries(params).forEach(([k, v]) => {
    const inp = document.createElement("input");
    inp.type = "hidden"; inp.name = k; inp.value = String(v);
    form.appendChild(inp);
  });
  document.body.appendChild(form);
  form.submit();
}
```

---

## 3. 받기 규격 (결제 결과 처리) — ⬜ 가온출결에 만들 부분

### 받는 주소 (아이원에 등록됨 — 고정)
```
https://lms.newgaon.com/payment/kicc/return
```
- ❗ **인터넷에 공개된 https 주소여야 함.** (localhost 불가 — 아이원이 밖에서 호출)
- 이 주소를 **가온출결 서버에 실제로 만들어야** 아이원 결과를 받을 수 있음.

### 이 페이지가 해야 할 일 (언어 무관 로직)
1. 아이원이 보낸 **결과 값을 전부 받는다** (POST, form 또는 JSON — 둘 다 대비).
2. 받은 값을 **로그로 남긴다** (처음엔 "무슨 값이 오는지" 확인이 목적 — 정소장님 방식).
3. 값 안의 **requestkey**로 우리 주문을 찾는다.
4. **성공/실패 코드**를 보고 결제 상태를 업데이트한다 (성공=결제완료, 실패=실패+message 표시).
5. 아이원에 **200 OK** 로 응답한다.

> ⚠️ **결과 데이터의 정확한 항목(필드명)은 아직 미확정.** 정소장님 방식대로 "일단 만들어서 실제로 받아보고" 확인 → 처음엔 전부 로그로 찍어 실제 오는 값을 파악한 뒤 4번(상태처리)을 채운다.

### 참고 코드 — Next.js(App Router)인 경우
`app/payment/kicc/return/route.ts`
```ts
import type { NextRequest } from "next/server";

async function handle(request: NextRequest) {
  const ct = request.headers.get("content-type") || "";
  let data: Record<string, string> = {};
  if (ct.includes("application/json")) {
    data = await request.json();
  } else {
    const form = await request.formData();
    form.forEach((v, k) => { data[k] = String(v); });
  }

  // 1) 처음엔 무슨 값이 오는지 확인 (로그)
  console.log("[KICC RETURN]", JSON.stringify(data));

  // 2) TODO: data.requestkey 로 주문 찾아 결제상태 업데이트
  //    성공코드면 결제완료 처리, 실패면 실패 + message 표시

  // 3) 아이원에 응답
  return new Response("OK", { status: 200 });
}

export const POST = handle;
export const GET = handle; // 아이원이 GET으로 보낼 수도 있어 대비
```

### 참고 로직 — PHP 등 다른 언어인 경우
```
1. $_POST(또는 $_REQUEST) 전체를 읽는다
2. 파일/DB에 로그로 남긴다 (실제 오는 필드 확인용)
3. requestkey로 주문 조회
4. 성공/실패 코드로 상태 업데이트
5. echo "OK";  // 200 응답
```
> 가온출결의 언어에 맞춰 위 로직을 그대로 옮기면 됨.

### 결과 테스트 방법 (정소장님 제공)
- 실제 카드결제 없이 결과 콜백만 시험하는 트리거 페이지:
  ```
  http://tapprsys.ione24.com/_appr/kicc/test.aspx
  ```
  이걸 호출하면 아이원이 위 return URL로 결과를 쏴줌 → 우리 받는 코드가 잘 받는지 확인.

---

## 4. 정산 · 명의

- 대금은 **가맹점(gaon / 사업자 1178119949 = 뉴가온) 계좌**로 입금.
- 근태관리 사용료 · 학원 수강료 **둘 다 같은 가맹점**으로 받기로 결정(2026-07-09) → **별도 가맹점 등록 불필요.**
- 근태/학원 구분은 **itemname** 또는 **requestkey 규칙**으로 (정소장님과 구분 방법 확정 필요).

---

## 5. 보안 · 운영 주의

- **운영 전환 시 https 필수**: 지금 호출 주소는 http(테스트). 운영 사이트(https)에서 http로 호출하면 브라우저가 막음 → 아이원에서 **운영 https 주소**를 받아야 함.
- **비밀값 하드코딩 금지**: sitekey/terminal 등은 소스에 직접 박지 말고 환경변수/설정으로 관리 권장.
- **금액 검증**: 결과 받을 때 우리가 요청한 금액(amt)과 승인 금액이 같은지 반드시 확인(다르면 취소 처리) — KICC 원문 규격 권고.
- **requestkey 중복 금지**: 매 결제 유니크 보장.

---

## 6. 확인 대기 항목 (정소장님께)

- [ ] return URL로 오는 **결과 데이터 형식**(필드명 목록: 성공/실패코드, 승인번호, 금액, requestkey 등) — 또는 "일단 받아서 확인"
- [ ] 근태관리 / 학원 수강료를 **같은 결제창에서 구분하는 방법**
- [ ] **운영(실결제)용 주소**(https) 및 운영 sitekey/terminal
- [ ] itemname 외 추가 파라미터 유무

---

## 7. 현재 테스트 코드 위치 (근태관리 — 참고용)

- 화면(호출·값확인·결제창 열기): `webapp/app/kicc-test/page.tsx`
- 서버 프록시(값 확인용 호출): `webapp/app/api/kicc-test/route.ts`
- 위 2·3장의 코드는 이 파일들에서 뽑아 정리한 것. 가온출결 언어에 맞게 옮기면 됨.
